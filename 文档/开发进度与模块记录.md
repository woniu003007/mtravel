# 旅游接待管理系统开发进度与模块记录

更新时间：2026-07-08

## 记录规则

本文件只记录开发过程事实，不替代甲方需求文档、原型图册或汇报 PPT。

每完成一个正式功能模块后，必须补充或更新对应记录，避免后续忘记开发进度、接口位置、数据库表和关键业务逻辑。

状态说明：

- `已接接口`：前端页面、后端接口、数据库表已打通，具备真实增删改查或真实业务操作。
- `部分完成`：已有部分真实能力，但还有关键流程、页面或联调未完成。
- `静态页面`：页面已存在，但主要是展示或占位，没有完整接真实接口。
- `待开发`：菜单或需求已规划，但尚未正式开发。
- `已退役`：甲方确认不再需要，代码入口、接口和数据库结构已下线或计划下线。

## 当前总体进度

- 后端技术栈：Spring Boot 3.5、Java 21、Maven、PostgreSQL、MyBatis-Plus、Redis。
- 前端技术栈：Vben Admin、Vue、Ant Design Vue。
- 已完成基础工程：统一返回、登录认证、会话心跳、系统配置、操作日志、附件上传、租户软删除基础能力。
- 已完成主要业务域：客户管理基础链路、采购管理基础链路、企业资料基础链路。
- 大量销售、计调、财务、统计页面目前已有前端菜单和静态页面，但未全部接入真实后端接口。
- 前端列表页 UI 已开始统一，新增了业务查询区和按钮组件，后续新增页面必须复用。

## 模块进度台账

| 业务域 | 模块 | 状态 | 前端页面 | 后端接口 | 数据库表 | 关键业务逻辑 | 验证/备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 公共基础 | 登录、登出、心跳、刷新 | 已接接口 | `_core/authentication/login.vue` | `/auth/login`、`/auth/logout`、`/auth/heartbeat`、`/auth/refresh` | `system_users`、`system_configs` | 登录使用 JWT；支持无操作自动退出；同账号单点登录已暂时关闭，后续系统稳定后再恢复。 | 已有 auth 相关单测。 |
| 公共基础 | 统一响应和错误处理 | 已接接口 | 前端请求封装 | 全局 `ApiResponse`、`GlobalExceptionHandler` | 无独立表 | 后端统一返回结构，业务异常用统一异常处理。 | 后续新增接口必须沿用。 |
| 公共基础 | 系统操作日志 | 已接接口 | `system/operation-log/index.vue` | `/system/operation-log/page` | `system_operation_logs` | 记录用户访问接口、维护资料、异常访问等操作；请求体做脱敏处理。 | 已有日志服务和脱敏单测。 |
| 公共基础 | 系统登录配置 | 已接接口 | `system/params/index.vue` | `/system/config/auth`、`/system/config/auth/update` | `system_configs` | 租户可配置登录安全参数，例如无操作自动退出时间。 | 已有配置服务单测。 |
| 公共基础 | 公共附件上传 | 已接接口 | 合同、资源图片等页面复用 | `/common/attachment/upload`、`/common/attachment/list` | `common_attachments` | 保存合同文件、资源图片、确认单等附件元数据，支持按业务模块和业务 ID 绑定。 | 已用于合同和资源图片。 |
| 客户管理 | 客户分类 | 已接接口 | `customer/category/index.vue` | `/customer/category/page`、`/all`、`/create`、`/update`、`/delete` | `customer_categories` | 分类可维护默认授信额度；新增客户单位时可按分类带出额度。 | 前端已接真实接口；列表按钮已统一。 |
| 客户管理 | 客户单位 | 已接接口 | `customer/unit/index.vue` | `/customer/unit/page`、`/detail`、`/create`、`/update`、`/delete` | `customers` | 维护客户主档、业务代码、分类、授信额度、结款方式、所在地、部门、操作计调、合同有效期。 | 前端已接真实接口；所在地为级联选择。 |
| 客户管理/采购管理 | 统一合同管理 | 已接接口 | `customer/contract/index.vue`，客户和采购菜单共用 | `/contracts/page`、`/detail`、`/next-no`、`/create`、`/update`、`/delete` | `contracts`、`common_attachments` | 分销商和采购合同共用一张表、一套接口；按合同类型切换页签；客户与供应商绑定互斥；合同编号自动生成；附件统一归属合同管理。 | 已迁移旧客户合同 3 条和附件 3 个；旧 `customer_contracts`、`supplier_contracts` 已删除。 |
| 客户管理 | 客户授信与实时应收 | 已接接口 | `customer/credit/index.vue` | `/customer/credit/page`、`/detail`、`/create`、`/update`、`/delete` | `customer_credit_accounts` | 维护授信额度、已占用额度、可用额度、超限提醒参数。当前重点是排团提醒，不强制拦截。 | 后续需要接订单确认和费用变更占用额度。 |
| 客户管理 | 客户产品授权 | 已接接口 | `customer/product-auth/index.vue` | `/customer/product-auth/page`、`/detail`、`/create`、`/update`、`/delete` | `customer_product_authorizations` | 维护客户可售或可下单产品范围、授权期限和授权状态。 | 使用通用 CRUD 页。 |
| 采购管理 | 供应商管理 | 已接接口 | `purchase/supplier/index.vue` | `/purchase/supplier/page`、`/all`、`/detail`、`/create`、`/update`、`/delete` | `suppliers` | 供应商新增/编辑字段已按旧系统对齐：所在地、公司名称、关联采购商、负责人、商家分类、联系电话、传真号码、办公地址、备注信息、客户状态；关联采购商用于后续应收应付冲抵。 | 已同步前端、后端、线上数据库、本地 SQL 和表说明；schema 一致性校验通过。 |
| 采购管理 | 资源总览 | 已接接口 | `purchase/resource/index.vue` | `/purchase/resource/page`、`/detail`、`/bindings`、`/create`、`/update`、`/delete` | `purchase_resources`、`purchase_relations`、`common_attachments` | 维护景区、酒店、餐厅、购物等资源主档；支持绑定供应商和上传资源图片；供应商绑定只选择供应商，具体价格进入采购关系的价格管理。 | 已移除供应商绑定界面的成团数量字段，避免和采购关系老系统口径不一致。 |
| 采购管理 | 采购关系管理 | 已接接口 | `purchase/relation/index.vue` | `/purchase/relation/page`、`/detail`、`/create`、`/update`、`/delete`、`/purchase/relation/price/page`、`/create`、`/update`、`/delete` | `purchase_relations`、`supplier_resource_prices`、`resource_projects` | 按旧系统口径维护资源与供应商绑定关系；列表展示所在地、资源名称、供应商、负责人、电话、创建人和创建时间；具体门市、同行、团队价从行内“价格管理”维护。 | 已移除页面中的成团数量字段；所在地取资源主档，负责人和电话取供应商档案；价格项目按资源类型过滤。 |
| 采购管理 | 酒店资源基础库 | 已接接口 | `purchase/hotel/index.vue` | `/purchase/hotel/page`、`/detail`、`/create`、`/update`、`/delete` | `hotel_resources` | 维护酒店、房型、城市区域、供应商和采购价格等资料。 | 使用通用 CRUD 页。 |
| 采购管理 | 景区资源基础库 | 已接接口 | `purchase/scenic/index.vue` | `/purchase/scenic/page`、`/detail`、`/create`、`/update`、`/delete` | `scenic_resources` | 维护景区、票种、供应商、采购价、协议价、免票半票规则。 | 使用通用 CRUD 页。 |
| 采购管理 | 地接外委管理 | 已接接口 | `purchase/ground-agent/index.vue` | `/purchase/ground-agent/page`、`/detail`、`/create`、`/update`、`/delete` | `ground_agents` | 维护地接社档案、外委任务、行程要求、总预算和确认单文件。 | 使用通用 CRUD 页。 |
| 采购管理 | 车辆管理 | 静态页面 | `purchase/vehicle/index.vue` | 暂无正式后端接口 | 暂无独立车辆表 | 当前仅为前端展示页；后续需要按车辆资源、车型、路线估价和车调联动重新做。 | 待开发真实接口和数据库。 |
| 企业资料 | 企业银行账号 | 已接接口 | `enterprise/bank-account/index.vue` | `/enterprise/bank-account/page`、`/all`、`/detail`、`/create`、`/update`、`/print-enabled`、`/delete` | `enterprise_bank_accounts` | 维护企业收付款账号，支持确认件打印可用账号。 | 已接真实接口。 |
| 企业资料 | 公司信息 | 已接接口 | `enterprise/company-info/index.vue` | `/enterprise/company-info/current`、`/save` | `enterprise_company_infos` | 维护本企业作为合同甲方、确认件抬头、签约账号的基础信息。 | 采购/客户合同甲方信息依赖该模块。 |
| 企业资料 | 部门管理 | 已接接口 | `enterprise/department/index.vue` | `/enterprise/department/page`、`/all`、`/detail`、`/create`、`/update`、`/delete` | `enterprise_departments` | 维护企业组织架构，供员工、角色、计调归属和统计使用。 | 已接真实接口。 |
| 企业资料 | 角色管理 | 已接接口 | `enterprise/role/index.vue` | `/enterprise/role/page`、`/all`、`/detail`、`/create`、`/update`、`/delete`、`/permissions` | `enterprise_roles`、`enterprise_role_permissions` | 维护角色和权限入口；当前是权限管理基础，细粒度按钮权限后续再完善。 | 已接真实接口。 |
| 企业资料 | 员工管理 | 已接接口 | `enterprise/employee/index.vue` | `/enterprise/employee/page`、`/all`、`/detail`、`/create`、`/update`、`/disable`、`/reset-password`、`/delete` | `enterprise_employees`、`system_users` | 维护员工、部门、角色、登录账号和数据查看范围；新增员工会关联登录账号。 | 列表按钮已统一。 |
| 企业资料 | 导游管理 | 已接接口 | `enterprise/guide/index.vue` | `/enterprise/guide/page`、`/all`、`/detail`、`/create`、`/update`、`/send-enterprise-code-invite`、`/disable`、`/delete`、`/tags/page`、`/tags/all`、`/tags/create`、`/tags/update`、`/tags/delete`、`/common/attachment/upload` | `enterprise_guides`、`enterprise_guide_tags`、`enterprise_guide_tag_relations`、`common_attachments` | 导游作为企业资料独立维护；所属导管从员工管理选择，用于绩效归属和导管小组统计；导游标签用于能力分类和后续排团筛选；银行/支付宝账号、企业码邀请状态、导游证书和个人照片已纳入档案，证书和照片通过公共附件上传保存。 | 后端导游服务单测通过；前端 typecheck 通过；导游三张表线上与本地 SQL columns/constraints/indexes/comments 一致性校验通过。真实企业码发送接口、导游端账号密码和问卷管理暂未接入。 |
| 企业资料 | 站点、合同模板 | 静态页面 | `enterprise/station`、`enterprise/contract-template` | 暂无正式后端接口 | 暂无对应正式表或未接入 | 当前主要是前端占位展示。 | 待后续确认是否需要做真实模块。 |
| 企业资料 | 费用项目 | 已接接口 | `enterprise/expense-item/index.vue` | `/enterprise/expense-item/page`、`/all`、`/detail`、`/create`、`/update`、`/delete` | `resource_projects` | 维护不同资源类型下的项目类型，例如景区票种、酒店房型、车辆车型、大交通票种、地接费用和附加费用；采购关系价格管理按资源类型过滤这些项目。 | 已按旧系统费用项目页面做资源类型 tab 和项目维护；tab 对齐为全部、景区、酒店、餐厅、车队、大交通、其它、杂费、地接、附加费用、购物；各分类已补充常用真实费用项目种子数据。 |
| 企业资料 | 产品字典 | 已接接口 | `enterprise/product-dictionary/index.vue` | `/enterprise/product-dictionary/page`、`/all`、`/detail`、`/create`、`/update`、`/delete` | `product_dictionaries` | 维护产品模板可选择的业务类型、接待标准和产品主题；业务类型从产品页移到企业资料统一维护，接待标准和产品主题也做成可配置字典。 | 已初始化业务类型、接待标准、产品主题默认数据；后端 `mvn -q test` 通过，前端 `pnpm -F @vben/web-antd run typecheck` 通过，`db/010_product_dictionary_schema.sql` 与线上 schema 一致性校验通过。 |
| 销售管理 | 产品管理 | 已接接口 | `sales/product/index.vue`、`sales/product/form.vue`、`sales/product/team-arrangement.vue` | `/sales/product/page`、`/detail`、`/create`、`/update`、`/delete`、`/sales/product/roadbook/amap/tips`、`/sales/product/roadbook/amap/driving` | `sales_products`、`sales_product_itinerary_days`、`sales_product_roadbook_points`、`sales_product_arrangement_items` | 按旧系统产品管理口径维护产品模板；列表页只做查询和行操作，新增/修改进入独立产品编辑页面；产品编辑页保留基本信息、行程内容、产品说明、团队安排、团期管理页签，其中团队安排页签只作为老系统入口，点击后进入独立产品团队安排页面，不在产品表单内嵌大表；产品列表的“团队安排”也进入独立产品团队安排页面，用于维护产品生成团队时的默认安排参数；产品团队安排页按老系统 `Group Arrange` 操作感展示收客、排团、发团、结算、完成流程提示、产品摘要、内部备注、费用总览表、分类入口、分类单据按钮和当前分类明细，但仍只保存产品默认安排参数，不处理正式订单、单据、导游报账、计调审核和真实团队成本；基本信息页点击保存后先进入行程内容，不直接返回列表；旅游天数会自动同步对应天数的线路行程行，行程页不再单独提供“新增一天”和单行删除，避免行程天数和基本信息不一致；行程内容按老系统矩阵维护线路行程、住宿、关联酒店、用餐、路书；产品说明页维护收客须知、产品说明、费用包含/不含、儿童安排、购物安排、自费项目、赠送项目、注意事项和温馨提醒；路书已改为高德地点搜索和驾车路线计算，前端只保存路线点、总公里和预计车程，高德 Web 服务 Key 从系统配置或环境变量读取；路书编辑器已改为全屏地图工作台，地图为主画面，搜索和路线点位作为悬浮操作面板；点位字段显示为停留分钟、到下一站公里、车程分钟，其中到下一站公里和车程来自高德驾车路线，不是直线距离，后端按相邻两点逐段调用高德驾车接口计算，不再把总距离平均分配，后台仍按米和秒保存；抽屉关闭时会销毁高德地图实例，避免再次打开时复用旧容器导致地图空白；关联酒店下拉来自采购酒店资源。 | 产品团队安排已从产品编辑页拆成独立页面，并按老系统操作感保留费用总览表、图标分类入口和当前分类明细表；2026-06-17 已将团队安排页重构为更简洁的白底工作台，移除老系统顶部票据库、订单文件、备用金请款单和打印类工具按钮，保留团队摘要、操作计调/导游/领队/全陪字段、内部备注、人均坑位/自费加点率/人均购物、费用矩阵、分类 Tabs、订房单/订车单/预订单/确认单按钮和供应商/备注/价格信息/成本合计/现结/挂账明细列；产品编辑页已恢复老系统“团队安排”tab 入口，未保存产品点击该 tab 会提示先保存；行程内容已从卡片表单改为老系统矩阵式线路行程表；产品说明已恢复为独立页签；高德地点搜索、驾车路线计算、产品路书保存和详情回显接口冒烟通过；路书全屏地图工作台经 Playwright 验证，搜索、点位显示、计算路线回填公里和车程正常；关闭路书抽屉后再次打开，地图瓦片可重新渲染；真实接口验证 4 个点返回 3 段不同驾车距离，不再平均分段；`db/011_sales_product_schema.sql` 与线上 schema 一致性校验通过；后端 `mvn -q test` 通过，前端 `pnpm -F @vben/web-antd run typecheck` 通过；本次团队安排页目标测试 `product-form-utils.test.ts` 通过，并已用 Playwright 截图验证 `output/playwright/team-arrangement-old-system-overview-sections-clean.png`；已去掉顶部解释条和页头右侧操作按钮，团队信息改为紧凑字段条，分类 tab 改为旧系统轻量页签样式。团期管理、创建团队、返点规则仍待后续销售主链路接入。 |
| 系统设置 | 菜单与路由 | 部分完成 | 路由模块和菜单接口 | `/menu/all` | 无独立业务表 | 前端菜单已有客户、采购、销售、计调、财务、统计、企业资料、系统设置等模块。 | 仍有部分页面是静态占位。 |
| 前端基础 | 业务列表页 UI 统一 | 已完成第一批 | `components/business`、重点列表页 | 无后端接口 | 无数据库表 | 统一查询区网格和 `重置 / 查询 / 新增` 按钮；禁止新增按钮使用 ghost 描边。 | 已迁移资源总览、通用 CRUD、客户单位、客户分类、员工管理、导游管理。 |
| 销售管理 | 团队管理 / 团队操作 | 已接部分高风险动作 | `frontend-preview/apps/web-antd/src/views/sales/team/index.vue`、`frontend-preview/apps/web-antd/src/views/sales/team/operation.vue`、`frontend-preview/apps/web-antd/src/views/sales/components/MergeOrderModal.vue` | `GET /sales/team/page`、`GET /sales/team/{id}/operation`、`POST /sales/team/{teamId}/operation/merge`、`POST /sales/team/operation/merge`、`POST /sales/team/{teamId}/operation/move` | `sales_teams`、`sales_team_prices`、`sales_products`、`sales_product_descriptions`、`sales_product_itinerary_days`、`sales_orders`、`sales_order_charge_lines`、`sales_order_guests`、`sales_order_transfer_logs`、`dispatch_team_arrangements`、`dispatch_team_guides` | 按旧系统 `/sale/GroupManage.aspx` 的团队列表口径实现；列表数据复用现有 `sales_teams` 并批量关联 `sales_products`；已接入团号/团队名称/备注、操作计调、出发地、业务类型、出团日期、天数和团队状态筛选；团队列表按当前页团队 ID 批量汇总导游、大交通、住宿、用车、景区、用餐、其它、自费、购物、地接安排状态，已确认显示已安排，已录入未确认显示待确认；点击团号进入团队操作页，团队操作页按旧系统 `TeamOperation.aspx` 保留收客/排团/发团/结算/完成流程、顶部工具、团队信息、产品说明、收客须知、内部备注、团队价格、操作按钮和收客订单表结构；总里程数和查看行程读取产品每日行程路书数据，未维护路书公里时显示 `--`；拼团操作已按老系统闭环实测口径支持多个来源订单拼到多个目标团队，生成“来源订单 x 目标团队”拼团子订单；拼团价 `0` 时目标子单应收为 `0`，手填价按单价 x 人数计入目标团收入；来源订单保留在来源团且继续参与来源团统计；转团操作支持转入已有团队或复制当前团队生成新团队；拼团展示已按老系统口径调整，来源团保留原订单并显示“拼团订单信息”，目标团显示拼入订单并显示“原始订单信息”。 | 团队操作页已展示真实团队、产品、说明、价格、产品每日行程和订单；收客订单、拼团操作、转团操作、内部备注、修改团队、团队安排已接入真实操作或跳转；团队操作页和订单管理页共用拼团弹窗；取消订单和拼入子订单不可作为来源订单再次拼团，历史 `merge_source` 仅作为来源留痕并继续参与人数、收入和毛利统计；复制团队、暂停收客、取消团队、导游报账等动作仍待后续接入正式审批和状态流转。 |
| 销售管理 | 团期管理 | 已接接口 | `sales/product/schedule.vue`，从产品列表和产品编辑页签进入 | `/sales/team/schedule/page`、`/batch-create`、`/team/save`、`/price/save`、`/price/delete`、`/status/change` | `sales_teams`、`sales_team_prices`、`sales_team_status_logs`、`sales_team_no_logs` | 从产品模板生成正式散拼团队；同一团队支持多个客户类型价格；团号按日期后缀递增；支持修改团期主信息、维护价格行、取消/恢复/停收/启用/删除团队；删除团队要求先取消。 | 已按老系统团期页核心字段实现：团号、发团日期、操作计调、状态、总位/余位、单房差、客户类型、成人、儿童、儿童不占床、老人、附加费用；页面布局参考老系统操作顺序，视觉样式跟随新系统 Card/Table/Button 默认风格；接口和 schema 已验证。 |
| 销售管理 | 收客订单与游客名单 | 已接接口 | `sales/booking/form.vue` | `/sales/booking/team/{teamId}`、`/sales/booking/detail`、`/sales/booking/save`、`/sales/booking/fee-change/create`、`/sales/booking/fee-change/cancel`、`/sales/booking/guest-export` | `sales_orders`、`sales_order_charge_lines`、`sales_order_guests`；`sales_order_price_lines`、`sales_order_fee_changes` 仅保留兼容 | 按团队进入收客订单页，维护行程、导游、客户、订单级业务员/收客计调、价格信息、费用变更和游客名单；游客类型按 18 岁以下儿童、60 岁及以上老人自动判定；身份证校验通过程序派生性别、出生年月、年龄；客源地按老系统规则取第一位有效游客身份证前六位行政区划码生成，第一位无身份证或地区码无法识别时置空，不继续取后续游客；客户类型价格和附加费自动生成 `base_price` 应收明细；费用变更写入 `adjustment` 明细，按加收/退减正负金额刷新订单应收；游客名单导出为老系统 `.xls` 列格式。 | 后端 `mvn -q test` 通过；前端 `pnpm -F @vben/web-antd run typecheck` 通过；`db/013_sales_booking_order_schema.sql` 已执行到 PostgreSQL，schema guardian 校验 `columns/constraints/indexes/comments=match`。 |
| 销售管理 | 订单、团队、游客、拼团等其他页面 | 静态页面 | `sales/*` | 暂无正式后端接口 | 团队主表、收客订单表和订单流转日志表已建立 | 除产品、团期、团队操作和收客订单外，其他销售页面当前主要是前端占位或原型页；团队操作页的拼团/转团主链路已先行接入，`拼团订单` 菜单仍未做完整列表和待拼团订单池。 | 后续应继续设计拼团订单菜单、电子合同、收款记录、订单附件和导游报账联动。 |
| 计调操作 | 房源与房态库存 | 已退役 | 已删除 `/dispatch/room-status` 页面和菜单入口 | 已删除 `/dispatch/room-status/**` 接口 | 已删除 `controlled_room_resources`、`controlled_room_types`、`controlled_room_units`、`room_inventories`、`controlled_room_lock_records`、`controlled_room_day_statuses` | 甲方确认没有自营房源，也不需要独立房态库存、锁房和房号管理。酒店基础资料统一维护在采购酒店资源中，产品和团队创建页的关联酒店只从采购酒店资源读取。正式团队住宿成本继续走团队安排成本表族。 | `db/009_dispatch_room_status_schema.sql` 已改为退役清理脚本；已备份旧表到 `output/db-backups/room-status-retirement-20260702114952.sql` 并执行退役删表；schema guardian 校验 `columns/constraints/indexes/comments=match`；后端 `mvn -q test` 通过，前端 `pnpm -F @vben/web-antd run typecheck` 通过；Playwright 验证 `/dispatch` 登录后进入 `/dispatch/vehicle-quote`，菜单不再显示房态库存；旧接口返回 404。后续如重新建设酒店库存，需要重新调研建模，不能沿用旧自控房源设计。 |
| 计调操作 | 用车报价测算 | 已接接口 | `dispatch/vehicle-quote/index.vue`、`sales/product/team-arrangement.vue` | `/dispatch/vehicle-quote-rules/page`、`/all`、`/create`、`/update`、`/delete`、`/calculate`；地图报价复用 `/sales/product/roadbook/amap/tips`、`/js-config`、`/driving`；产品保存接口同步保存用车报价快照和询价记录 | `vehicle_quote_rules`、`sales_product_vehicle_quote_snapshots`、`sales_product_vehicle_inquiries` | 维护不同座位数的基础价、基础公里、超公里单价、最低价和浮动系数；配置规则弹窗只允许录数字，页面显示单位“座”，提交接口时自动转成 `39座` 这类规则值以兼容现有数据；快速测算、地图报价和产品团队安排用车弹窗都属于使用规则，座位数必须从 `vehicle_quote_rules` 启用规则下拉选择，不再手填；当前报价规则先不启用地区，后端保存时也统一清空预留地区字段；报价规则弹窗已补字段说明和计算公式；用车报价页保留手填公里的快速报价，并新增全屏地图报价抽屉，支持搜索地址或直接点地图生成出发地、途经地、目的地，按高德驾车路线计算总里程和预计车程后自动调用报价规则；地图报价顶部使用紧凑结果栏展示路线、总里程、预计车程和参考报价，保证结果明显但不挤压地图；产品团队安排用车弹窗可同步路书公里，并新增“编辑路书地图”入口，可打开产品行程对应天的全屏路书地图修改地址；产品模板阶段不再展示车队询价记录，真实车队询价应放到后续正式团队安排或车调派车模块；用车开始/结束天变更后自动计算“共几天”，按首尾天都包含的规则保存；产品安排保存报价快照，后续规则调整不反向影响历史产品模板。 | 后端 `mvn -q test` 通过；前端 `pnpm -F @vben/web-antd run typecheck` 通过；相关 Vitest 40 项通过；`db/011_sales_product_schema.sql` 与线上 schema 一致性校验 `columns/constraints/indexes/comments=match`；线上 `vehicle_quote_rules` 唯一索引已调整为同租户同座位数未删除规则唯一；已补 15 条座位数报价测试数据，并通过 Playwright 验证列表表头和数据行正常显示；2026-06-18 已将报价规则页和产品团队安排用车弹窗的座位数输入统一改为数字输入框，并补充地图报价全屏抽屉；2026-06-18 已将快速测算和地图报价的座位数改为读取启用报价规则下拉；2026-06-18 已优化地图报价顶部结果区为紧凑指标栏；2026-06-18 已将产品团队安排用车弹窗座位数改为启用报价规则下拉，并补充跳转全屏路书地图的入口；2026-06-18 已移除产品模板用车弹窗中的车队询价记录，并修正用车“共几天”自动计算。 |
| 计调操作 | 导游排班与请假 | 已接接口 | `dispatch/guide-schedule/index.vue`、`dispatch/guide-leave/index.vue`、`guide/my-leave/index.vue`、`sales/team/arrangement.vue` 导游分区 | `GET /dispatch/guide-schedule/calendar`、`GET /dispatch/guide-leaves/page`、`POST /dispatch/guide-leaves/direct`、`POST /dispatch/guide-leaves/{leaveId}/approve`、`POST /dispatch/guide-leaves/{leaveId}/reject`、`GET /guide/my-leave`、`POST /guide/my-leave`、`POST /guide/my-leave/{leaveId}/withdraw`、`GET /sales/team/{teamId}/guides`、`POST /sales/team/{teamId}/guides/create`、`POST /sales/team/{teamId}/guides/{recordId}/field`、`POST /sales/team/{teamId}/guides/{recordId}/delete` | `dispatch_team_guides`、`dispatch_guide_leave_records` | 导游排班汇总以启用导游档案为主行数据，默认展示所有启用导游；没有团队安排或已通过请假的导游显示为空闲行，有占用时再聚合团队导游安排和已通过请假；团队安排页导游分区保存导游、导服费、备用金、操作费、上团/下团时间、费用说明、导游备注和待定中；计调可直接设置导游不可上团，导游可提交请假，计调审批通过后才进入排班占用；新增/修改导游安排、审批通过请假、计调直设请假都会按同导游时间段做冲突校验。 | 后端 `mvn -q test` 通过；前端新增导游排班功能结构测试通过；前端 `pnpm -F @vben/web-antd run typecheck` 通过；已执行 `db/015_dispatch_guide_schedule_schema.sql` 到当前联调库，schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。 |
| 计调操作 | 团队安排、接送、审核、导游报账等页面 | 部分完成 | `dispatch/*`、`sales/team/arrangement.vue` | 导游安排已接真实接口；正式团队安排成本已接 `GET/POST /sales/team/{teamId}/arrangements`；其它审核和报账页面暂未完整接入 | `dispatch_team_guides`、`dispatch_guide_leave_records`、`dispatch_team_arrangements`、`dispatch_team_arrangement_price_lines`、`dispatch_team_arrangement_order_allocations`、`dispatch_team_arrangement_flow_records` | 正式团队安排页已接导游分区和大交通成本保存；成本底座支持团队公共成本、单订单归属、多订单均摊、无需导游报账同步流水。住宿、用车、景区、用餐、其它、自费、购物、地接、附加后续复用同一表族接入，不再另建各自独立成本表。 | 后续重点是把非大交通分类逐步接入正式保存，并继续开发导游报账、计调审核、应付付款、财务审核和车调派车联动。 |
| 财务管理 | 收款、应收应付、发票、备用金、导游结算等页面 | 静态页面 | `finance/*` | 暂无正式后端接口 | 暂无财务主链路正式表 | 当前主要是前端占位或原型页。 | 后续需要先确定团队审核、财务审核、导游备用金和报账逻辑。 |
| 数据统计 | 经营统计、利润、接待、支付、资源采购等页面 | 静态页面 | `statistics/*` | 暂无正式后端接口 | 暂无统计汇总表 | 当前主要是前端展示页。 | 等销售、计调、财务主数据落地后再接真实统计。 |

## 待确认问题

- 合同管理中导游、财务费用、现收现退、附加费用等 tab 在老系统中的真实录入入口仍需向甲方确认。
- 车辆管理应按“自有车辆管理”和“外部车辆估价参考”拆清楚，暂未建立正式车辆表和派车链路。
- 客户授信当前按“排团提醒，不强制拦截”口径处理；后续订单确认、费用变更如何占用额度需要在订单模块落地时再接。
- 供应商的关联采购商已按旧系统作为一对一关系落库；后续财务应收应付冲抵是否需要审批、多对多或历史冲抵记录，需要开发财务冲抵模块时再确认。
- 同账号只允许同时在线一次的功能已先关闭，系统开发稳定后再决定是否恢复。
- 真实 `计调操作 / 团队安排` 需要后续单独开发，不能继续放在产品模板里；开发时应参考老系统 `GroupOverview.aspx` 的总览分类，以及用餐、住宿、用车、景区等分类的添加记录弹窗字段。
- 销售、计调、财务、统计多数页面目前是菜单和静态页面，不能视为已完成真实业务闭环。

### 2026-06-17 销售管理 / 产品管理页面布局调整

- 状态：已完成前端调整。
- 前端页面：`sales/product/form.vue`、`sales/product/team-arrangement.vue`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：新增/修改产品的基本信息页改为贴近老系统的表格式字段布局，左侧固定“基本信息”，右侧按业务类型、国内/国际、线路名称、接团城市、出团类型、接待标准、产品主题、旅游天数、截止收客、单人房差、预控人数、状态、备注排列；去掉底部浮动保存条，避免遮挡录入字段；产品团队安排页移除顶部票据库、订单文件、备用金请款单和打印类按钮，避免把产品模板页误做成正式团队执行页。
- 关键业务逻辑：产品创建阶段只维护产品模板基础资料；团号、出团日期等正式团期/排团字段不放在创建产品基本信息中。
- 验证结果：`product-form-utils.test.ts` 20 项通过；`pnpm -F @vben/web-antd run typecheck` 通过；已生成页面截图 `output/playwright/product-create-old-system-basic-layout-v2.png`、`output/playwright/team-arrangement-no-top-tool-buttons.png`。
- 待确认问题：后续正式团期管理页需要单独承接团号、出团日期、操作计调、总位/余位和价格库存字段。

### 2026-06-17 销售管理 / 产品团队安排总览分区

- 状态：已完成前端调整。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：团队安排页不再只显示单一选中分类表格，改为参照老系统 `GroupOverview.aspx` 的总览式布局；分类页签包含总览、大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加；下方按大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加全部纵向展开，每个分区保留 `无需 / 完成 / +` 操作，住宿、用车、景区、地接分别保留订房单、订车单、预订单、确认单入口。
- 关键业务逻辑：本页仍属于产品模板的团队安排参考，不是正式团队执行页；正式排团、订单、单据、导游报账和团队成本后续在真实团队安排模块落地。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，21 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；Playwright 已验证页面存在 11 个分类 tab、10 个安排分区、无顶部 `arrangement-tool-bar`，截图为 `output/playwright/team-arrangement-old-system-overview-sections-clean.png`。
- 待确认问题：真实团队安排模块开发时，需要继续调研各分类添加弹窗字段、保存规则和费用归属规则。

### 2026-06-17 销售管理 / 产品团队安排页面视觉优化

- 状态：已完成前端调整。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：以后台业务操作台为目标重做团队安排页首屏视觉；将产品标题、业务标签、关键指标、收客/排团/发团/结算/完成阶段和团队管理/查看行程操作合并为紧凑顶部工作台；内部备注改为一行弱提示；费用矩阵、分类 tab、图标入口和各安排分区整体压缩高度；主色从橙色/青色装饰改为跟系统一致的蓝白灰色系。
- 关键业务逻辑：本次只优化视觉层级、间距、字号和颜色，不改变产品团队安排的保存逻辑和接口。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，22 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；Playwright 验证 `arrangement-command-bar`、`workflow-rail`、`team-note-row`、`compact-category-strip` 均存在，页面仍保留 11 个分类 tab 和 10 个安排分区；截图为 `output/playwright/team-arrangement-operations-console-redesign-clean.png`；视觉质检评分 92。
- 待确认问题：真实团队安排模块开发时，再根据正式录入弹窗和数据状态补交互细节。

### 2026-06-17 销售管理 / 产品团队安排大交通弹窗

- 状态：已完成前端页面。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：参照老系统大交通弹窗，在产品团队安排页的大交通分区点击 `+` 后打开 `添加/修改大交通信息` 弹窗；弹窗包含 `全团/订单均摊`、`多订单均摊成本`、交通类型、日期行程、供应商、价格信息、结算方式、订单信息、备注信息和 `无需导游报账，同步更新导游报账和计调审核数据` 选项。
- 关键业务逻辑：当前仍属于产品团队安排前端页面能力，保存时先把大交通记录加入当前页面的安排明细，用户再点击 `保存团队安排` 写入产品安排项；供应商、费用归属、订单归属和导游报账同步口径暂存在备注中，正式团队安排模块落地后应拆成结构化字段。
- 验证结果：先补充 `product-form-utils.test.ts` 结构测试并确认失败，再实现页面；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，23 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；Playwright 已打开 `http://127.0.0.1:5666/sales/product/team-arrangement/3` 并点击大交通 `+`，截图为 `output/playwright/team-arrangement-traffic-modal.png`。
- 待确认问题：正式团队安排开发时，需要继续调研老系统大交通保存接口、供应商来源、订单关联规则、多订单均摊成本拆分方式，以及导游报账同步是否真实自动生成审核数据。

### 2026-06-17 销售管理 / 产品团队安排分类弹窗字段对齐

- 状态：已完成前端调整。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：重新登录旧系统 `GroupOverview.aspx?GroupId=270648#Menu` 核对团队安排总览和弹窗字段，确认旧系统不是所有分类共用一个通用资源弹窗；新系统已新增 `arrangementEditorConfigs`，按大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加分别控制弹窗标题、日期字段、资源字段、负责人字段、人数/销售价/成本价/人头费/消费总额/导游提成、订单信息和是否显示“无需导游报账”。
- 关键业务逻辑：大交通保留交通类型、日期行程、出发地、抵达地；住宿保留酒店名称、已确认、确认号、早餐基金、入住退房、责任房调；用车保留开始结束、车型、司机、车牌、责任车调；景区、用餐、自费、购物分别按老系统资源名称和金额字段显示；地接标题按旧系统口径显示为“添加/修改拼团信息”；附加标题显示为“添加/修改附加费用”。结算方式中的预付款已改为可录入，挂账按合计减现结减预付款计算。
- 验证结果：先补充 `product-form-utils.test.ts` 结构测试并确认失败，再实现分类字段配置；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，26 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：本页当前仍是产品模板的团队安排参考页，不是正式团队执行页；正式 `计调操作 / 团队安排` 开发时，还需要继续按旧系统保存接口核对每个弹窗的必填校验、订单关联、多订单均摊和导游报账同步真实规则。

### 2026-06-18 销售管理 / 产品团队安排住宿下拉与资源测试数据

- 状态：已完成前端修复和测试数据补充。
- 前端页面：`sales/product/team-arrangement.vue`、`purchase/resource/index.vue`。
- 后端接口：复用 `/purchase/resource/page`、`/purchase/resource/create`、`/purchase/supplier/create`、`/purchase/relation/create`。
- 数据库表：无表结构变更；新增测试数据写入 `purchase_resources`、`suppliers`、`purchase_relations`。
- 已实现功能：住宿弹窗的 `酒店名称` 下拉改为读取资源总览中的酒店资源，不再读取当前为空的 `hotel_resources`；酒店名称旁新增 `添加酒店` 按钮，打开资源总览新增页并预填资源类型为酒店；资源总览补充酒店、景区、餐厅、购物测试资源，并给多条资源绑定多个供应商。
- 关键业务逻辑：团队安排住宿选择的是资源总览中的酒店主档；供应商仍从酒店类供应商中选择；资源与供应商的多对多关系通过采购关系维护，列表中的 `已绑定` 数量来自采购关系。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，29 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；接口验证资源总览酒店返回 4 条，Playwright 验证住宿弹窗酒店下拉可见 `宁波东钱湖度假酒店`、`杭州钱江新城商务酒店`、`杭州西湖四钻酒店`、`苏州中心酒店`。
- 待确认问题：正式团队安排模块开发时，住宿选择酒店后是否要自动带出默认供应商、房型/价格项目和采购关系价格，需要继续按旧系统保存逻辑确认。

### 2026-06-18 销售管理 / 产品团队安排用车弹窗字段对齐

- 状态：已完成前端调整。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：无变更，继续复用产品安排保存接口。
- 数据库表：无变更。
- 已实现功能：用车分区点击 `+` 后不再使用通用弹窗，改为用车专用布局；字段顺序按旧系统截图调整为 `车型`、`用车时间`、`司机车号`、`供应商`、`价格信息`、`结算方式`、`责任车调`、`订单信息`、`备注信息`；司机信息和车牌号按甲方确认口径均改为手动输入。
- 关键业务逻辑：本页仍保存为产品团队安排模板，不是正式团队执行成本；用车供应商从车队供应商读取，价格项目从车辆费用项目读取，责任车调从员工读取；司机信息和车牌号不强制依赖独立司机档案或车辆档案。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，30 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；Playwright 验证用车弹窗可见 `添加/修改用车信息`，字段包含车型、用车时间、司机车号、供应商、价格信息、结算方式、责任车调、订单信息和备注信息；2026-06-18 已根据甲方确认将司机信息、车牌号调整为手动输入。
- 待确认问题：正式团队安排模块开发时，是否需要把手动录入过的司机和车牌沉淀成历史候选，可后续按甲方使用习惯再确认。

### 2026-06-18 计调操作 / 用车历史候选库

- 状态：已完成前后端和数据库落库。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：`GET /dispatch/vehicle-usage-histories`、`POST /dispatch/vehicle-usage-histories/record-use`。
- 数据库表：新增 `vehicle_usage_histories`，已同步线上 PostgreSQL 和 `db/011_sales_product_schema.sql`、`db/数据库设计-销售产品模板说明.md`。
- 已实现功能：司机信息和车牌号仍允许手动输入；输入框改为 `AutoComplete`，可按历史候选下拉；用车安排保存成功后自动沉淀司机信息和车牌号；同一租户、同一类型、同一归一化内容累计使用次数，候选按使用次数和最近使用时间排序。
- 关键业务逻辑：历史候选不是司机档案或车辆档案，不限制用户手动输入；只作为老系统风格的常用输入沉淀，后续正式团队安排模块可复用同一接口。
- 验证结果：新增后端服务测试和控制器路径测试，`cd backend && mvn -q -Dtest=VehicleUsageHistoryServiceTest,VehicleUsageHistoryControllerMappingTest test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，30 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；线上表字段、约束、索引、注释已用 `psql` 查询确认。
- 待确认问题：正式团队安排模块开发时，是否需要对历史候选提供后台清理/合并入口，当前先不做。

### 2026-06-18 销售管理 / 产品团队安排已保存明细修改

- 状态：已完成前端调整。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：无新增，继续复用 `/sales/product/update` 保存产品安排明细。
- 数据库表：无变更，继续保存到 `sales_product_arrangement_items` 及相关明细表。
- 已实现功能：产品团队安排各分类明细表的 `操作` 列不再显示 `--`，改为显示 `修改`；点击后打开对应分类的添加/修改弹窗，并回填原记录的供应商、资源、日期、价格信息、结算方式、备注、用车报价快照等字段；顶部快捷信息已按老系统改为字段级快捷修改，点击 `业务类型`、`部门`、`操作计调`、`全陪` 分别打开 `修改业务类型`、`修改业务部门`、`修改操作计调`、`团队全陪信息` 小弹窗，`内部备注` 单独打开内部备注小弹窗。
- 关键业务逻辑：新增和修改共用同一个弹窗；编辑已有明细时按原记录位置替换，保存成功提示 `安排信息已修改`；点取消或保存成功后清理编辑状态，避免下一次新增误覆盖旧记录；`备注信息` 只保存用户手写内容，不再自动拼接费用归属、订单信息、价格备注或导游报账同步提示，旧数据打开修改时会自动剔除这些系统字段；业务类型同步产品正式字段，所属部门、操作计调、全陪和内部备注当前先保存在产品备注扩展块中，产品基本信息页会隐藏该扩展块，避免污染产品备注。
- 验证结果：先新增 `product-form-utils.test.ts` 断言并确认旧实现失败，再改为字段级快捷编辑；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，44 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：当前仍属于产品模板的团队安排明细修改；正式团队安排模块落地时，还需要补订单关联、审核状态、真实团队成本和已结算后是否允许修改的规则。

### 2026-06-22 采购管理 / 采购关系游客名单模板配置

- 状态：已完成前后端和数据库落库。
- 前端页面：`purchase/relation/index.vue`，采购关系行内新增 `模板配置`，打开游客名单模板配置抽屉。
- 后端接口：`GET /purchase/relation/ticket-template/detail`、`GET /purchase/relation/ticket-template/headers`、`GET /purchase/relation/ticket-template/system-fields`、`POST /purchase/relation/ticket-template/save`、`POST /purchase/relation/ticket-template/delete`。
- 数据库表：新增 `purchase_relation_ticket_templates`、`purchase_relation_ticket_template_fields`；补充 `common_attachments(tenant_id, id)` 唯一约束用于同租户附件外键引用。
- 已实现功能：在采购关系行上配置游客名单 Excel 模板；上传模板附件；读取 Excel 表头；配置表头行、数据开始行、工作表、启停状态和备注；维护模板列到系统游客字段的映射。
- 关键业务逻辑：模板挂在采购关系，也就是 `景区资源 + 供应商/票务渠道` 这一行，不挂在供应商全局或景区资源全局；首版只做模板和字段映射，不保存外部票务系统账号密码，不自动登录票务系统，不自动下单。
- 验证结果：新增 `TicketTemplateHeaderParserTest` 并先确认失败后实现；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；线上 PostgreSQL 已执行 DDL；使用 schema guardian 一致性脚本校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。
- 待确认问题：后续团队景区安排生成游客名单 Excel 时，需要确认游客基础名单来源、不同票务渠道是否允许一条采购关系绑定多份模板，以及导出文件是否需要保留原模板样式。

### 2026-06-22 采购管理 / 游客名单模板填充方式增强

- 状态：已完成前后端和数据库同步。
- 前端页面：`purchase/relation/index.vue` 的游客名单模板配置抽屉。
- 后端接口：新增 `GET /purchase/relation/ticket-template/fill-modes`；`POST /purchase/relation/ticket-template/save` 支持字段级填充方式。
- 数据库表：`purchase_relation_ticket_template_fields` 新增 `fill_mode`、`fixed_value`；`system_field` 改为可空；线上 PostgreSQL 和 `db/005_customer_purchase_expansion_schema.sql` 已同步。
- 已实现功能：字段映射支持 `游客字段`、`自动序号`、`固定值`、`不填充` 四种方式。牛首山这类模板可以把 `序号` 设为自动序号，把 `备注` 设为不填充保留模板原说明；拙政园这类模板可以把 `票型名称`、`证件类型` 配成固定值。
- 关键业务逻辑：只有 `游客字段` 填充方式要求选择系统游客字段；`固定值` 要填写固定内容；`自动序号` 和 `不填充` 不要求系统字段。
- 验证结果：新增 `TicketTemplateFillModeValidatorTest` 并先确认失败后实现；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`；重启后端后 `/api/purchase/relation/ticket-template/fill-modes` 返回 200。
- 待确认问题：下一步生成游客名单 Excel 时，需要实现按模板复制样式、按填充方式写入游客数据，并确认是否要支持多 sheet 模板。

### 2026-06-22 采购管理 / 景点预约模板初始化

- 状态：已完成数据初始化。
- 前端页面：`purchase/relation/index.vue` 的采购关系行内 `模板配置` 抽屉。
- 后端接口：复用 `/purchase/resource/create`、`/purchase/supplier/create`、`/purchase/relation/create`、`/common/attachment/upload`、`/purchase/relation/ticket-template/save`、`/purchase/relation/ticket-template/detail`、`/purchase/relation/ticket-template/headers`。
- 数据库表：无表结构变更；新增业务数据写入 `purchase_resources`、`suppliers`、`purchase_relations`、`common_attachments`、`purchase_relation_ticket_templates`、`purchase_relation_ticket_template_fields`。
- 已实现功能：已将 `网上预定门票系统/景点预约模板` 下的 9 份景点预约 Excel 配置到系统中；缺失的景区资源、景区票务供应商和采购关系已自动补齐；模板附件已通过公共附件接口上传并绑定到对应采购关系。
- 关键业务逻辑：模板仍挂在 `景区资源 + 供应商/票务渠道` 的采购关系上；游客姓名、证件号、手机号等字段映射系统游客字段；序号使用自动序号；票型、证件类型、组团社等固定要求使用固定值；乌镇报备属于团队级报备表，当前先配置固定值和保留原值字段，后续导出时如需自动带导游、抵达日期、团队人数，需要扩展团队字段填充。
- 验证结果：已通过接口逐条校验 9 个模板配置；`/purchase/relation/ticket-template/detail` 均能返回模板和字段映射；`/purchase/relation/ticket-template/headers` 均能读取对应 Excel 表头。已配置景点：上海自然、科技博物馆、上海豫园、东方明珠、中山陵、乌镇、总统府、拙政园、灵隐寺、牛首山。
- 待确认问题：后续生成游客名单 Excel 时，需要确认乌镇这类团队报备表是否也要由系统自动填团队字段；外部票务系统账号密码仍不写入系统，后续如要自动下单需单独设计安全存储和接口对接。

### 2026-06-22 销售管理 / 产品团队安排景区弹窗接入票务模板

- 状态：已完成前端联动。
- 前端页面：`sales/product/team-arrangement.vue`、`purchase/relation/index.vue`。
- 后端接口：复用 `/purchase/resource/page`、`/purchase/relation/page`、`/purchase/relation/ticket-template/detail`。
- 数据库表：无表结构变更；读取 `purchase_resources`、`purchase_relations`、`purchase_relation_ticket_templates`。
- 已实现功能：产品团队安排页的景区/自费弹窗不再读取旧 `purchase/scenic` 景区基础库；景区名称改为读取采购资源总览的景区资源；选择景区后，供应商下拉只显示该景区在采购关系中绑定的供应商；选中景区和供应商后显示游客名单模板状态，已配置时展示模板名称，未配置时提示未配置；点击 `配置模板` 会打开采购关系页并自动进入对应采购关系的模板配置抽屉。
- 关键业务逻辑：老系统景区弹窗是 `景区名称 -> 供应商 -> 价格信息/预订单` 链路；新系统当前按 `景区资源 + 供应商/票务渠道` 的采购关系找到游客名单模板，为后续生成景区预约 Excel 做准备；模板配置仍在采购关系模块维护，团队安排弹窗只做选择和状态提示。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，46 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；已修复景区弹窗采购关系请求 `pageSize=500` 超过后端最大 200 导致 `参数校验失败` 的问题，并增加测试防止再次传 500。
- 待确认问题：后续正式团队安排模块需要实现按已选景区采购关系导出游客名单 Excel；是否在产品模板阶段也允许导出预订单，需要再确认。

### 2026-06-22 销售管理 / 产品团队安排用餐其它自费购物地接弹窗对齐

- 状态：已完成前端字段对齐。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：无新增，继续复用 `/sales/product/detail`、`/sales/product/update`、`/purchase/resource/page`、`/purchase/relation/page`、`/purchase/relation/ticket-template/detail`、`/purchase/supplier/all`、`/enterprise/expense-item/all`。
- 数据库表：`sales_product_arrangement_price_lines` 新增 `company_rebate_rate`，本地 SQL 和说明文档已同步；其余继续复用产品安排明细、采购资源、采购关系、供应商和费用项目表。
- 已实现功能：按老系统团队安排弹窗口径补齐 `用餐 / 其它 / 自费 / 购物 / 地接` 五类专用弹窗；用餐包含餐厅名称、用餐日期、时间、供应商、价格信息、结算方式、订单信息和备注；其它包含使用日期、供应商、价格信息、结算方式、订单信息和备注；自费包含景区名称、游玩日期、供应商、销售价、成本价、导游提成、费用合计、订单信息和备注；自费导游提成已按老系统补为 `金额 元/人 或 比例 %` 两种录入；费用合计区已从单行汇总改为多行明细，支持新增、删除并汇总人数、收入、成本、现结、挂账和提成；购物包含购物店、购物日期、供应商、费用设置、消费详情、人数、人头费、公司返佣、导游提成、品类、消费金额、现结、备注和加号，消费详情已支持多行明细新增、删除和汇总，并调整为新系统统一卡片与网格样式，避免字段横向溢出；地接包含拼团日期、开始、结束、共几天、供应商、价格信息、结算方式、订单信息和备注，其中 `共几天` 已按开始/结束含首尾自动计算，例如第1天到第3天自动显示3天；住宿 `共几晚` 已按入住/退房自动计算，例如第1天入住、第3天退房自动显示2晚；团队安排日期下拉已按产品旅游天数动态生成，不再写死第1到第3天；各安排表格行已支持删除，删除前会弹出二次确认，确认后才立即保存到产品团队安排；顶部阶段条已改为产品模板口径 `产品模板 / 安排配置 / 生成团队 / 团队执行 / 财务结算`，不再在创建产品时高亮正式团队的 `排团` 状态。
- 关键业务逻辑：用餐和购物资源从采购资源总览读取，资源类型分别为餐厅和购物；餐厅、购物店、自费景区都必须通过采购关系过滤供应商，选择资源后只显示该资源已绑定的供应商，避免出现餐厅和供应商错配；自费继续沿用景区资源和采购关系供应商联动，并显示游客名单模板状态；自费多行费用明细保存到价格明细，汇总金额回写安排项，提成比例保存到价格明细 `guideCommissionRate`；购物多行消费详情保存到价格明细，公司返佣金额保存到 `companyRebateAmount`，公司返佣比例保存到 `companyRebateRate`，导游提成金额和比例保存到对应提成字段，用于后续导游结算和购物返佣扩展；地接按老系统弹窗只选地接类供应商，不再额外读取地接档案作为资源；地接开始/结束日期变更、打开已有记录和保存前都会重新同步天数；用餐、其它、地接使用老系统固定费用项目选项。
- 验证结果：先补充 `product-form-utils.test.ts` 结构测试并确认失败，再实现五类弹窗；2026-06-22 又补充餐厅/购物供应商按采购关系过滤的回归断言并确认失败后修复；2026-06-22 继续补充自费导游提成和费用合计回归断言并确认失败后修复；2026-06-22 继续补充购物弹窗字段和样式回归断言并确认失败后修复；2026-06-22 继续补充地接 `共几天` 自动计算回归断言并确认失败后修复；本次继续补充日期动态下拉、住宿晚数自动计算、自费/购物多行明细、安排表格删除、删除二次确认和产品模板阶段条口径的回归断言并修复；修复安排表格操作列中删除按钮打断 Vue `v-if/v-else` 链导致的 Vite 编译报错；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，54 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run build` 通过；`cd backend && mvn -q -Dtest=SalesProductServiceTest test` 通过。
- 待确认问题：正式 `计调操作 / 团队安排` 开发时，还需要继续核对五类弹窗的真实保存接口、订单关联规则、导游报账同步规则、已结算后是否允许修改，以及自费/购物提成后续如何进入导游结算。

### 2026-06-22 销售管理 / 产品团队安排弹窗性能优化

- 状态：已完成前后端性能优化。
- 前端页面：`sales/product/team-arrangement.vue`。
- 后端接口：`/purchase/resource/page`、`/purchase/relation/page`、`/enterprise/employee/all`、`/enterprise/employee/page`、`/sales/product/update`、`/sales/product/update-arrangements`、`/sales/product/arrangement/save`、`/sales/product/arrangement/delete`。
- 数据库表：无结构变更，继续使用 `purchase_resources`、`purchase_relations`、`suppliers`、`enterprise_employees`、`enterprise_departments`、`enterprise_roles`、`sales_product_arrangement_items`。
- 已实现功能：团队安排弹窗打开时，按安排类型缓存供应商、费用项目、资源、采购关系和车辆报价规则等下拉数据，同一页面内重复打开同类弹窗不再重复请求全部下拉接口；用餐、景区、自费、购物等不需要责任人的弹窗不再请求员工下拉；住宿、用车等需要责任人的弹窗才加载员工；团队安排整组保存保留 `/sales/product/update-arrangements`，但弹窗新增、修改已改走 `/sales/product/arrangement/save` 单条保存，弹窗删除改走 `/sales/product/arrangement/delete` 单条删除，不再重写产品主表、行程内容、产品说明，也不再重建其它团队安排。
- 关键业务逻辑：景区、用餐、购物、自费仍按 `资源 -> 采购关系 -> 供应商` 联动过滤，只是下拉基础数据改为复用缓存；采购资源列表原来每行统计一次绑定供应商数量，采购关系列表原来每行分别查询资源和供应商，现在均改为当前页批量查询，避免 pageSize=200 时产生 N+1 数据库查询；员工下拉和员工分页原来每个员工单独查询部门和角色，现在改为当前结果集批量查询部门、角色，避免员工数增加后弹窗被员工接口拖慢；单条安排修改时先校验安排归属当前产品和租户，再只软删除该安排及其价格明细、用车报价快照、询价记录，插入新版本后返回新安排 ID，前端同步更新本地记录 ID，后续继续编辑仍按单条更新。
- 验证结果：先补充前端缓存、员工懒加载、轻量安排保存和单条安排保存/删除回归断言并确认失败，再实现缓存和按类型加载；先补充采购资源、采购关系、员工列表、团队安排轻量保存、单条安排保存边界单测并确认失败，再实现后端优化；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，56 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd backend && mvn -q test` 通过；重启后端后实测 `/enterprise/employee/all?includeDisabled=false` 从优化前约 1.07-1.29 秒降到约 0.36-0.39 秒；`/sales/product/update?id=3` 全量保存约 2.27-2.43 秒，`/sales/product/update-arrangements?id=3` 整组轻量保存约 1.32-1.48 秒，`/sales/product/arrangement/save?id=3` 单条保存约 0.63-0.70 秒。
- 待确认问题：如果新增供应商、资源或采购关系后要求不刷新页面立即出现在弹窗下拉中，后续可在弹窗内增加 `刷新下拉` 按钮或在新增页返回时清空当前分类缓存；如果单条保存还要继续降到 300ms 左右，需要进一步做批量 SQL、减少远程数据库往返，或把数据库部署到同一内网环境。

### 2026-06-22 销售管理 / 团期管理

- 状态：已完成前后端、数据库和产品入口联动。
- 前端页面：`sales/product/schedule.vue`；产品列表 `团期管理` 按钮和产品编辑页 `团期管理` tab 均进入该页面。
- 后端接口：`GET /sales/team/schedule/page`、`POST /sales/team/schedule/batch-create`、`POST /sales/team/schedule/team/save`、`POST /sales/team/schedule/price/save`、`POST /sales/team/schedule/price/delete`、`POST /sales/team/schedule/status/change`。
- 数据库表：新增 `sales_teams`、`sales_team_prices`、`sales_team_status_logs`、`sales_team_no_logs`，已同步线上 PostgreSQL、`db/012_sales_team_schema.sql` 和 `db/数据库设计-销售团队团期说明.md`。
- 已实现功能：按产品批量生成团期，默认生成散拼团队；团号按 `CS-SP-BK-yyMMdd` 加后缀递增；团期列表展示团号、发团日期、操作计调、状态、总位/余位、单房差、客户类型和成人/儿童/儿童不占床/老人/附加费用；同一团队可维护多条客户类型价格；支持修改团期主信息、修改/新增/删除价格行；支持批量取消、恢复取消、团队停售、团队恢复收客和删除团队；页面已按老系统操作顺序拆为顶部 `批量添加团队`、中部团期列表、表格底部批量按钮、底部 `添加/修改团期信息`，避免把新增团期和批量修改已有团期混成一个入口。
- 关键业务逻辑：产品模板不是正式团队，只有团期管理批量生成后才落 `sales_teams`；一个团队主表对应多条客户类型价格，不能用单表重复保存团队字段；同一团号下出现 `默认`、`公司费用`、`A类客户`、`B类客户` 等多行时，表示同一个团面向不同客户类型的销售价格，不是多个团；后续销售下单应根据客户单位所属客户类型匹配 `sales_team_prices` 价格行，并用于订单应收、客户报价、收客金额和按客户类型统计收入/利润；删除团队要求先取消，普通状态不能直接删除；删除价格行只删除价格，不删除团队；列表价格按当前页团队 ID 批量查询，避免 N+1。
- 验证结果：先新增 `SalesTeamScheduleServiceTest` 并确认缺失实现导致失败，再实现后端；本次布局调整先补充前端断言并确认缺少 `批量添加团队` 等老系统入口时失败，再修正页面；2026-06-23 已将团期管理页调整为“布局参考老系统、视觉跟随新系统”：结构保留标题、产品页签、单行筛选、表格式团期价格维护和底部批量操作按钮，样式使用新系统 Card/Table/Button/Input 默认风格，不再使用旧系统青色皮肤，截图为 `output/playwright/sales-product-schedule-new-system-style.png`；`cd backend && mvn -q -Dtest=SalesTeamScheduleServiceTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，57 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；已执行 `db/012_sales_team_schema.sql` 到联调库，schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`；重启后端后登录并调用 `/sales/team/schedule/page?productId=3&page=1&pageSize=10` 返回 200；通过 `/sales/team/schedule/batch-create?productId=3` 生成 2026-07-01 至 2026-07-03 三条测试团期，并能从分页接口查回团队和价格行；本次样式复核调用 `/sales/team/schedule/page?productId=3&page=1&pageSize=20` 返回 200，耗时约 0.41 秒。
- 待确认问题：当前只做老系统团期管理页核心能力，不包含团队详情、订单收客、游客名单、拼团/转团、导游报账和财务结算；后续开发正式订单链路时，需要决定余位由订单确认、退团和转团如何实时维护。

### 2026-06-23 销售管理 / 团期管理特定团期弹窗

- 状态：已完成前后端联动、前端布局调整和弹窗视觉统一。
- 前端页面：`sales/product/schedule.vue`。
- 后端接口：复用并增强 `POST /sales/team/schedule/batch-create`。
- 数据库表：无新增表，继续使用 `sales_teams`、`sales_team_prices`、`sales_team_status_logs`、`sales_team_no_logs`。
- 已实现功能：`批量添加团队` 弹窗已按老系统拆成 `批量团期 / 特定团期` 两个页签；`特定团期` 改为老系统式左右日期框，中间为 `添加>>`、`<<删除`，右侧保留 `客户类型`、`操作计调`，底部保留 `预控人数`、`单房差`、`附加费用`、`成人价格`、`儿童价格`、`儿童不占床价格`、`老人价格` 和 `提交保存`；弹窗视觉已统一为产品团队安排大交通弹窗的浅色分组表单风格，补齐页签、输入框、金额前缀、复选框、关闭按钮和中间操作按钮样式，避免团期弹窗和其它业务弹窗割裂。
- 关键业务逻辑：前端选择多个不连续日期后一次提交；后端 `batch-create` 支持 `dates` 日期列表，有值时优先按指定日期生成团队和默认价格行，不再按起止日期和星期推算。
- 验证结果：已先补充 `product-form-utils.test.ts` 断言并确认缺少 `可选团期 / 已选团期 / 未选择特定团期` 时失败，再实现页面；本次继续补充团期弹窗统一样式回归断言，先确认缺少关闭按钮、页签、金额前缀、复选框和特定团期按钮样式时失败，再修复；本次补充提交成功后关闭弹窗的回归断言，并确认 `批量团期` 和 `特定团期` 两个提交入口都会关闭弹窗并清空已选日期；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，57 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；Playwright 已验证打开 `http://127.0.0.1:5666/sales/product/schedule/3`，切换 `特定团期` 后能看到老系统式左右日期框，并验证点击 `添加>>` 后日期从左侧进入右侧已选框，截图为 `output/playwright/sales-product-schedule-specific-dates-old-layout.png` 和 `output/playwright/sales-product-schedule-specific-dates-selected.png`；本次视觉复核截图为 `output/playwright/sales-schedule-batch-modal-unified-light-v2.png` 和 `output/playwright/sales-schedule-specific-modal-unified-light-v3.png`；本次行为复核已实际点击 `添加团期`，页面提示 `已生成 1 条团期` 后返回列表且不再显示 `批量添加团队` 弹窗，验证产生的测试团期已按状态流程取消并删除。
- 待确认问题：当前日期池默认生成从今天开始 90 天的候选日期；如果甲方希望按产品有效期、团期销售周期或指定年月生成候选日期，后续可再调整日期池来源。

### 2026-06-23 销售管理 / 团期管理添加修改团期信息

- 状态：已完成前后端联动。
- 前端页面：`sales/product/schedule.vue`。
- 后端接口：新增并接入 `POST /sales/team/schedule/batch-edit`。
- 数据库表：无新增，继续使用 `sales_teams`、`sales_team_prices`。
- 已实现功能：`添加/修改团期信息` 弹窗已按老系统补齐 `可选团期 / 已选团期`、`可选客户类型 / 已选客户类型` 两组左右选择框；支持批量选择团队、批量选择客户类型，并勾选 `同步产品内容`、`同步产品内容（不含单房差）`、`删除价格`、`预控人数`、`单房差`、`成人/儿童/儿童不占床/老人/附加费用` 后一次提交；前端不再循环调用单条团队保存和单条价格保存，统一提交到批量接口。
- 关键业务逻辑：选中客户类型时，只新增或修改这些客户类型价格；未选择客户类型时，修改团队下已有全部价格，若团队原先没有价格则新增默认价格；未选择客户类型并勾选删除价格时，会删除所选团队下全部价格。一个团号下面出现多条客户类型价格仍表示同一个团队的不同客户报价，不是多个团队。列表行内支持直接修改总位、单房差和成人/儿童/儿童不占床/老人/附加费用，失焦后保存当前单元格；客户类型价格删除不再放在列表单行操作里，统一按老系统入口从 `添加/修改团期信息` 弹窗勾选 `删除价格` 后执行，避免误删单行价格。
- 验证结果：`cd backend && mvn -q -Dtest=SalesTeamScheduleServiceTest test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，57 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；2026-06-23 现场联调时发现浏览器保存返回 `服务器内部错误`，后端日志为 `No static resource sales/team/schedule/batch-edit`，根因是 8080 后端进程未重启加载新增接口；执行 `./start-backend.sh` 后重新验证，临时创建团期 `CS-SP-BK-261229A`，调用 `/sales/team/schedule/batch-edit` 写入 A/B 两类客户价格返回 200，并已取消和删除该临时团期；本次行内保存和删除入口调整后，前端回归测试、前端类型检查和后端团期服务测试均已重新通过。
- 待确认问题：`同步产品内容` 和 `同步产品内容（不含单房差）` 当前只保留入口，实际覆盖产品内容的范围需要后续结合老系统再确认，避免误覆盖行程、说明或单房差。

### 2026-06-23 销售管理 / 团队操作页第一版

- 状态：已完成只读详情页和团队管理跳转。
- 前端页面：`sales/team/index.vue` 点击团号跳转；新增 `sales/team/operation.vue`。
- 后端接口：新增 `GET /sales/team/{id}/operation`。
- 数据库表：无新增表；读取 `sales_teams`、`sales_team_prices`、`sales_products`、`sales_product_descriptions`。
- 已实现功能：团队操作页按老系统 `TeamOperation.aspx` 展示团队标题、散拼/业务类型/操作计调/导游/领队/全陪标签、收客/排团/发团/结算/完成流程、预控人数、实收人数、剩余人数、旅游天数、出团日期、总里程数、订单文件/打印/导出工具、产品说明、收客须知、内部备注、电子合同、复制团队、拼团操作、转团操作、修改团队、暂停收客、收客订单、取消团队、团队安排、导游报账和订单表结构。
- 关键业务逻辑：该页第一版只读聚合真实团队、产品说明和客户类型价格；不加载无关员工、供应商、资源下拉；订单模块未开发前订单表显示空状态；拼团/转团未选择订单时只提示；暂停收客、取消团队等高风险动作暂不直接修改数据。
- 验证结果：先新增 `SalesTeamControllerMappingTest` 和 `SalesTeamScheduleServiceTest` 团队操作断言并确认缺少 DTO/接口时报错，再实现后端；`cd backend && mvn -q -Dtest=SalesTeamControllerMappingTest,SalesTeamScheduleServiceTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：订单列表、游客名单、收客订单、电子合同、复制团队、拼团、转团、暂停收客、取消团队、导游报账和打印导出仍需要在后续销售订单与团队执行链路中逐项接入真实业务规则。

### 2026-06-23 销售管理 / 团队操作页视觉统一优化

- 状态：已完成前端视觉优化。
- 前端页面：`sales/team/operation.vue`。
- 后端接口：无新增，继续使用 `GET /sales/team/{id}/operation`。
- 数据库表：无变更。
- 已实现功能：将团队操作页改为更接近产品团队安排页的紧凑操作台布局；顶部信息卡保留团队标题、标签、指标和流程条；内部备注改为单行备注条；产品说明和收客须知压缩为轻量标签；打印导出工具、主操作按钮、次级操作和危险操作重新分层；团队价格改为一行价格条；订单表提前上移，第一屏能更早看到订单结构。
- 关键业务逻辑：本次只改视觉和布局，不改接口、不执行真实拼团/转团/暂停/取消、不新增假订单。
- 验证结果：`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/router/routes/__tests__/visible-menu.test.ts --dom` 通过；Playwright 打开 `/sales/team/operation/3` 后关键文字完整，截图保存为 `output/playwright/sales-team-operation-optimized.png`。
- 待确认问题：如果后续要进一步贴近老系统操作习惯，需要在真实订单链路完成后确认订单表行内操作、订单选择、拼团/转团弹窗和收客订单入口的交互细节。

### 2026-06-23 销售管理 / 团队操作页老系统顺序重设计

- 状态：已完成前端布局重设计和自有视觉风格收敛。
- 前端页面：`sales/team/operation.vue`。
- 后端接口：无新增，继续使用 `GET /sales/team/{id}/operation`。
- 数据库表：无变更。
- 已实现功能：按老系统 `TeamOperation.aspx` 的页面顺序重排团队操作页，但视觉皮肤使用新系统自有业务控制台风格，不复刻旧系统金色按钮和青色标题条。顶部标题右侧放订单文件、打印行程单、打印团队名单、打印团队结算单、导出接送机名单；流程按钮独立置顶；团队名称、团队类型、业务类型、部门、操作计调、导游、领队、全陪、预控人数、实收人数、剩余人数、旅游天数、团号、出团日期、总里程数按旧系统顺序展示；`查看价格`、`查看行程` 放在指标行右侧；产品说明、收客须知、内部备注恢复为正文顺序；电子合同、内部备注、复制团队、拼团操作、转团操作、修改团队、暂停收客、收客订单、取消团队、团队安排、导游报账、返回上页改为订单表上方的紧凑工具条；订单区标题改为 `该团相关订单`。本次进一步统一为浅色卡片、蓝灰主色、浅色业务标签、细边框表格和横向轻按钮，避免页面像老系统换皮，也避免和产品团队安排页割裂；指标栏为团号单独加宽，保证 `CS-SP-BK-260708A` 这类团号完整显示。
- 关键业务逻辑：本次只改前端布局和视觉层级，不改接口、不新增数据库、不直接执行暂停收客、取消团队、拼团、转团等高风险动作；这些动作仍保留入口和提示，等待订单与团队执行链路完成后接真实规则。
- 验证结果：新增 `operation-layout.test.ts`，先确认旧实现缺少新结构断言失败，再完成重排；测试同时禁止旧系统金色和青色皮肤色值回流；本次更新后 `cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/team/operation-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；Playwright 登录后打开 `/sales/team/operation/17`，确认团号完整显示、操作按钮区高度约 56px、单个按钮高度约 34px，截图保存为 `output/playwright/sales-team-operation-compact-toolbar.png`。
- 待确认问题：正式订单链路开发后，需要继续补充收客订单、电子合同、复制团队、拼团、转团、暂停收客、取消团队、导游报账和打印导出的真实交互。

### 2026-06-23 销售管理 / 团队操作页状态与查看弹窗修正

- 状态：已完成前后端联动和页面验证。
- 前端页面：`sales/team/operation.vue`。
- 后端接口：增强 `GET /sales/team/{id}/operation`。
- 数据库表：无新增，新增读取 `sales_product_itinerary_days`。
- 已实现功能：未收客团队不再默认高亮 `排团`，仅高亮 `收客`；团队操作页 `总里程数` 改为读取产品每日行程路书总距离，没有维护公里数时显示 `--`，不再写死 `0公里`；`查看价格` 改为表格弹窗，按客户类型展示成人价、儿童占床、儿童不占床、老人价、附加费和备注；`查看行程` 改为按天展示 D1/D2 的详细行程弹窗，包含日期、行程内容、用餐、住宿、路程、车程和路书摘要。
- 关键业务逻辑：当前团队操作页读取产品模板行程作为团队行程来源；后续如果团队实际行程允许单独调整，应增加团队行程快照，不能反向修改产品模板。排团阶段当前按 `usedSeats > 0` 判断是否高亮，不新增复杂状态表。
- 验证结果：先运行后端和前端 RED 测试，确认缺少 `itineraryDays`、`routeSummary`、`stageState`、价格表格和行程弹窗时失败；实现后 `cd backend && mvn -q -Dtest=SalesTeamScheduleServiceTest#operationDetailShouldAggregateTeamProductDescriptionsAndPrices test` 通过；`cd backend && mvn -q -Dtest=SalesTeamControllerMappingTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/team/operation-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；已重启后端并调用 `/api/sales/team/17/operation` 返回 200，包含 `routeSummary` 和 5 条 `itineraryDays`；Playwright 登录后打开 `/sales/team/operation/17`，确认实收 0 时只有 `收客` 为 active、总里程显示 `--`、价格表格弹窗和 D1-D5 行程弹窗正常。
- 待确认问题：产品行程里已有路书摘要但部分历史数据的路程/车程为 0，需要后续在产品路书中补算并保存，团队操作页才会显示实际总公里。

### 2026-06-23 销售管理 / 团队属性继承与团队操作页编辑

- 状态：已完成前后端、数据库和接口联调。
- 前端页面：`sales/team/operation.vue`；API 类型 `src/api/sales/team.ts`。
- 后端接口：增强 `POST /sales/team/schedule/batch-create`、`POST /sales/team/schedule/team/save`、`GET /sales/team/{id}/operation`。
- 数据库表：`sales_teams` 新增 `business_type`、`department_id`、`department_name`、`escort_employee_id`、`escort_employee_name`；已同步线上 PostgreSQL、`db/012_sales_team_schema.sql` 和 `db/数据库设计-销售团队团期说明.md`。
- 已实现功能：团期创建仍默认生成 `sanpin` 散拼团；生成团队时从产品业务类型和产品团队安排资料中继承业务类型、部门、操作计调、全陪，落为正式团队快照字段；团队操作页顶部 `团队类型 / 业务类型 / 部门 / 操作计调 / 全陪 / 内部备注` 支持点击编辑并保存当前团队；保存后只影响当前团队，不反写产品模板；旧数据没有快照值时继续显示空值或产品业务类型兜底。
- 关键业务逻辑：产品是线路模板，团队是正式执行记录；团队属性生成时从产品带默认值，生成后允许团队级覆盖。当前兼容读取产品备注里的 `[[TEAM_PROFILE_JSON]]` 团队安排资料，后续如产品模板字段正式化，可改为读取正式产品模板字段。保存接口支持用户主动清空团队属性字段，同时避免团期列表行内只保存总位/房差时误清空其它属性。
- 验证结果：先补充团队属性继承、详情返回、保存和清空字段的后端 RED 测试，再实现；`cd backend && mvn -q -Dtest=SalesTeamScheduleServiceTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/team/operation-layout.test.ts apps/web-antd/src/views/sales/product/product-form-utils.test.ts --dom` 通过，59 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`；重启后端后调用 `/api/sales/team/17/operation` 返回 200，调用 `/api/sales/team/schedule/team/save?teamId=17` 可保存并清空业务类型、部门、全陪等团队属性。
- 性能记录：团队属性保存接口本地经前端代理实测约 1.11 秒，超过 800ms。当前原因是远程 PostgreSQL 环境下保存后又查询团队及价格行返回完整 `SalesTeamResponse`；后续可按接口性能规则优化为保存只返回必要 ID，前端再显式刷新详情。
- 待确认问题：产品团队安排资料目前仍兼容存放在产品备注 JSON 标记中；是否要把产品默认部门、默认计调、默认全陪正式拆成产品模板字段，后续再结合产品模板维护页统一确认。

### 2026-06-23 销售管理 / 确认单 AI 辅助录入

- 状态：已完成第一版后端识别接口、订单页预览入口和系统配置支持。
- 前端页面：`sales/order/index.vue` 新增 `AI辅助录入` 抽屉；隐藏路由 `/sales/order` 已接入，暂不显示到侧边栏菜单。
- 后端接口：新增 `POST /sales/booking/ai-import/recognize`；系统配置新增 `GET /system/config/ai`、`POST /system/config/ai/update`。
- 数据库表：无新增表；AI 配置复用 `system_configs`；确认单附件复用 `common_attachments`。
- 已实现功能：支持粘贴文本识别；支持通过公共附件上传 Word、Excel、文本后传 `attachmentId` 识别；识别草稿按 `行程说明 / 导游相关 / 客户信息 / 价格信息 / 附加说明 / 游客名单` 六块展示；游客名单识别支持分房字段、领队/疑似领队标记、领队证据文本；身份证合法性由程序校验并推导出生日期、性别和年龄，异常行在前端标红。
- 关键业务逻辑：该功能只辅助人工录入，不自动保存订单、游客名单、费用、导游或房态数据；点击 `填入表单` 只回填当前页面草稿并提示用户，不触发保存接口。百炼 Key 可通过系统配置保存，但接口只返回脱敏值；真实密钥不得写入代码、SQL、Markdown 或提交记录。环境变量 `BAILIAN_API_KEY` 优先于系统配置，系统配置作为兜底。
- 验证结果：先补充 `IdCardValidatorTest`、`BookingAiImportServiceTest`、`AiConfigServiceTest` 和前端 `ai-import-layout.test.ts` 并确认缺失实现时报错；实现后 `cd backend && mvn -q -Dtest=IdCardValidatorTest,BookingAiImportServiceTest,AiConfigServiceTest,SystemConfigControllerMappingTest test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/order/ai-import-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：当前百炼客户端已完成文本配置边界和视觉/OCR 调用边界；正式收客订单表、游客名单表和费用表稳定后，再把草稿字段逐步接入真实保存。

### 2026-06-23 销售管理 / 收客订单第一版

- 状态：已完成前后端主链路、团队操作页入口和数据库文件。
- 前端页面：新增 `sales/booking/form.vue`；路由新增 `/sales/team/booking/:teamId` 和 `/sales/team/booking/:teamId/:orderId`；团队操作页 `收客订单` 按钮进入新增收客订单，订单表行点击进入修改收客订单。
- 后端接口：新增 `GET /sales/booking/team/{teamId}`、`GET /sales/booking/team/{teamId}/orders`、`GET /sales/booking/detail?id=`、`POST /sales/booking/save`；团队操作详情 `GET /sales/team/{id}/operation` 已接入真实订单行。
- 数据库表：新增并维护 `sales_orders`、`sales_order_price_lines`、`sales_order_guests`、`sales_order_fee_changes`；已同步 `db/013_sales_booking_order_schema.sql`、`db/数据库设计-销售收客订单说明.md` 和 `db/README.md`。
- 已实现功能：第一版完成收客订单主保存链路和 AI 辅助录入草稿；页面布局后续已按 `收客订单旧系统字段对齐修正` 记录重新调整，正式口径以修正记录为准。
- 关键业务逻辑：新增订单编号由系统自动生成 `SO-`；订单应收按价格明细计算，余额按应收减已收计算；订单人数优先按成人、儿童、儿童不占床、老人、全陪等价格明细数量统计，游客名单可后补；身份证合法性由程序校验并保存提示；已确认订单占用团队实收和余位，已取消订单不占用；团队停收时不允许新增或确认订单；费用变更记录当前只展示，不在收客保存接口中直接生成审批记录。
- 验证结果：先补充 `SalesBookingOrderServiceTest` 和 `booking-form-layout.test.ts` 并确认缺少实现时报错；实现后 `cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest test` 通过；`cd backend && mvn -q -Dtest='SalesBookingOrderServiceTest,SalesTeamScheduleServiceTest,SalesTeamControllerMappingTest' test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；已执行 `db/013_sales_booking_order_schema.sql` 到线上 PostgreSQL，并通过 schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。
- 待确认问题：客户单位下拉、所在地级联、订单文件真实附件列表、打印单据、电子合同、收款记录、费用变更审批、拼团/转团和导游报账仍需后续按老系统逐项接入；本次只完成收客订单主保存链路和 AI 辅助录入回填。

### 2026-06-23 销售管理 / 收客订单 AI 辅助录入报错修复

- 状态：已修复。
- 前端页面：`sales/booking/form.vue` 的 AI 辅助录入抽屉。
- 后端接口：`GET /sales/booking/team/{teamId}`、`POST /sales/booking/ai-import/recognize`。
- 数据库表：无变更。
- 已实现功能：修复后端重启后 `BailianAiModelClient` 因多构造器未标记运行时注入构造器导致 Bean 创建失败的问题；将 AI 辅助录入操作日志类型从自定义 `AI辅助录入` 调整为日志表允许的 `导入`，避免接口成功但操作日志落库约束报错。
- 关键业务逻辑：AI 辅助录入仍只生成草稿，不自动保存订单、游客名单、费用或导游安排。
- 验证结果：新增 `BailianAiModelClientWiringTest`，先确认能复现 `No default constructor found` 后再修复；新增 `BookingAiImportControllerTest`，先确认日志类型不是允许值时失败后再修复；`cd backend && mvn -q -Dtest=BailianAiModelClientWiringTest,BookingAiImportServiceWiringTest,BookingAiImportServiceTest,BookingAiImportControllerTest test` 通过；`cd backend && mvn -q test` 通过；执行 `./start-backend.sh` 后后端健康；通过前端代理验证 `/api/sales/booking/team/28` 返回 200，耗时约 0.44 秒；`/api/sales/booking/ai-import/recognize` 返回 200，耗时约 0.26 秒；后端日志尾部未再出现 500 或操作日志约束错误。
- 待确认问题：百炼真实模型 HTTP 调用仍未接入，本次只修复当前内部错误和日志约束问题。

### 2026-06-23 销售管理 / 收客订单旧系统字段对齐修正

- 状态：已修正前端布局。
- 前端页面：`sales/booking/form.vue`；API 类型 `src/api/sales/booking.ts`、`src/api/sales/team.ts`。
- 后端接口：无新增接口；继续使用 `GET /sales/booking/team/{teamId}`、`GET /sales/booking/detail?id=`、`POST /sales/booking/save`；订单页顶部团队信息复用团队操作接口 `GET /sales/team/{teamId}/operation`，团队类型、业务类型、部门、操作计调、全陪、内部备注修改复用 `POST /sales/team/schedule/team/save`。
- 数据库表：无变更。
- 已实现功能：参考旧系统 `BookingAgent.aspx?GroupId=272115`，将收客订单主体区调整为 `行程说明 / 导游相关 / 客户信息 / 价格信息 / 附加说明 / 费用变更记录 / 游客名单`；去掉误加的 `订单文件`、打印类、拼团类顶部按钮，只保留 `AI辅助录入`；去掉误加的 `订单信息` 独立分区；补齐旧系统字段展示，包括 `接机标识`、`业务员`、`收客计调`、`占位`、`收客须知`；按最新业务口径移除 `从分项报价获取价格`、`转到分项报价` 两个入口；客户单位改为客户档案下拉，按最新旧系统复核移除误加的 `客源地` 级联；顶部团队信息接入团队操作页同源数据，支持修改 `团队类型 / 业务类型 / 部门 / 操作计调 / 全陪 / 内部备注`，`查看价格` 展示团队客户类型价格，`查看行程` 展示团队行程和路书，`总里程数` 展示团队路书汇总里程；来程/返程出发和抵达时间按旧系统改为日期时间选择组件；行程说明区只保留参团时间、来程信息、返程信息，不再额外增加独立大文本框；费用变更记录和游客名单在当前订单页提交前可见，底部按钮为 `提交订单`、`返回团队`。
- 关键业务逻辑：价格信息不再从 Word/确认单费用明细或分项报价带入；订单选择客户单位后，前端按客户单位 `客户分类` 匹配当前团期客户类型价格，未匹配时回退默认价格行；游客名单提供成人、老人、儿童、儿童不占床数量，系统自动带出对应价格行，成人、老人、儿童默认占位，儿童不占床不占位；自动带出后允许人工修改，保存时仍按最终价格行汇总订单应收。AI 识别仍只回填草稿和游客名单，不再回填价格信息；来程、返程拆分字段第一版前端展示并拼回现有 `pickupInfo`、`dropoffInfo` 保存，暂不扩表。订单页顶部团队属性保存的是团队主信息，不是订单自身字段。附加费自动计入应收、订单级业务员/收客计调、费用变更新增和名单导出已在 2026-06-24 后续记录中接入。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`curl -s -m 2 http://127.0.0.1:5666` 已确认前端服务返回页面；`curl -s -m 2 http://127.0.0.1:8080/actuator/health` 返回 `{"status":"UP"}`。Playwright 旧截图 `output/playwright/sales-booking-form-with-guests-fees.png` 已暴露费用变更空表格暗底和游客名单按钮暗色问题，本次已通过 CSS 修正；后续截图需在浏览器真实登录态下复核。
- 待确认问题：当前后端价格明细表没有保存“是否占位”字段，页面先按前端规则展示占位标识；如果后续库存要严格排除儿童不占床，需要补后端占位字段和团队余位计算规则。电子合同、名单导入、订单附件、收款记录还需继续接真实业务。

### 2026-06-23 销售管理 / 收客订单顶部风格统一

- 状态：已完成前端样式统一。
- 前端页面：`sales/booking/form.vue`；布局测试 `sales/booking/booking-form-layout.test.ts`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：收客订单页顶部改为复用团队操作页视觉结构，包含 `订单处理` 标题区、`收客 / 排团 / 发团 / 结算 / 完成` 阶段条、团队档案标签、指标条和 `查看价格 / 查看行程` 入口；顶部业务按钮只保留 `AI辅助录入`，不引入团队操作页的打印、订单文件、拼团等按钮。
- 关键业务逻辑：本次只统一顶部视觉和布局，不改变收客订单保存、客户单位下拉、行程说明、费用变更、游客名单和 AI 辅助录入逻辑。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；Playwright 登录后打开 `/sales/team/booking/28`，已生成截图 `output/playwright/sales-booking-form-team-operation-style.png`；参考团队操作页截图 `output/playwright/sales-team-operation-reference-28.png`，视觉判定记录 `.omx/state/mtravel-booking-form/ralph-progress.json` 得分 92。
- 待确认问题：`查看价格`、`查看行程` 当前保持入口样式，是否在收客订单页接入与团队操作页相同弹窗，需要后续确认。

### 2026-06-23 销售管理 / 确认单 AI 辅助录入真实样例验收

- 状态：已完成真实样例测试和前端 AI 工作台优化。
- 前端页面：`sales/booking/form.vue`。
- 后端接口：增强 `POST /sales/booking/ai-import/recognize`。
- 数据库表：无变更，继续复用 `common_attachments` 和 `system_configs`。
- 已实现功能：AI 辅助录入弹窗由抽屉改为宽屏工作台，支持一次上传多个 Word、Excel、PDF、图片或粘贴文本；前端展示识别游客数、证件异常、字段缺失、疑似漏识别、六个模块评分、游客名单、分房、领队标记、身份证校验、识别提醒和识别依据；填入表单时可选择 `行程说明 / 导游相关 / 客户信息 / 价格信息 / 附加说明 / 游客名单`，并保持“只填空字段”，不覆盖已有人工录入内容。
- 关键业务逻辑：AI 识别结果仍是草稿，不自动保存订单；游客名单保存前必须人工确认。Word/Excel 使用本地文本提取和规则解析兜底；图片/PDF 走百炼 OpenAI 兼容接口的 `qwen-vl-ocr-latest` 视觉/OCR 模型，未配置或模型无返回时返回明确业务提示。身份证合法性继续由程序校验，模型不得覆盖程序校验结果。
- 验证结果：新增真实样例验收测试 `BookingAiImportGoldenSampleTest`，使用 `确认单/06.25-悦色江南（地接确认）杭州百缘.docx` 验证 17 个可见游客不漏行，使用 `确认单/MZ-HD-20260606-CZ杭州【零购江南】客人名单表.xls` 验证领队+9 个游客不漏行；新增百炼视觉请求测试，验证图片附件会以 base64 data URL 调用 `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions` 并读取 `choices[0].message.content`；新增真实 PNG OCR 文本金标准，使用 `确认单/8ad0abf45b2cda679e1d68f7971d3fca.png` 的百炼返回文本验证 11 个游客不漏行、去程 `CZ6503` 和返程 `CZ6510` 可识别；修复图片/PDF OCR 调用未传 `tenantId` 导致系统配置 Key 读取不到的问题；真实接口联调已上传该 PNG 到 `common_attachments`，再调用 `POST /sales/booking/ai-import/recognize` 返回 11/11 名游客、11/11 身份证、`missingRequiredCount=0`、`invalidIdCardCount=0`，接口耗时约 6.03 秒。`cd backend && mvn -q -Dtest=BookingAiImportGoldenSampleTest test` 通过；`cd backend && mvn -q -Dtest=BookingAiImportGoldenSampleTest,BookingAiImportServiceTest,BookingAiImportServiceWiringTest,BookingAiImportControllerTest,BailianAiModelClientWiringTest,IdCardValidatorTest,AiConfigServiceTest test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：后续应继续用甲方更多确认单样本扩充金标准，重点覆盖图片/PDF、游客名单、分房、领队、价格和附加说明；如果 PDF 多页识别效果不稳定，需要再增加 PDF 转图片逐页 OCR 的增强方案。

### 2026-06-24 销售管理 / 收客订单游客名单分房字段修正

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`；API 类型 `src/api/sales/booking.ts`。
- 后端接口：增强 `POST /sales/booking/ai-import/recognize` 的 Excel 游客名单识别结果；增强 `POST /sales/booking/save` 的游客名单保存字段。
- 数据库表：`sales_order_guests` 新增 `room_remark`；`room_group` 明确只表示房间组号。已同步 `db/013_sales_booking_order_schema.sql` 和 `db/数据库设计-销售收客订单说明.md`。
- 已实现功能：游客名单表格支持横向滚动，身份证号、出生年月、分房备注等宽字段可完整查看；前端表格拆成 `房间组号` 和 `分房备注` 两列，不再把 `1大床（必须保证大床）` 这类房型要求塞进组号；AI 填入时同住一间房的游客使用同一个组号，房型和特殊要求写入分房备注。
- 关键业务逻辑：Excel 确认单继续优先用 Java/Apache POI 解析，图片和 PDF 才走百炼视觉/OCR；程序识别出的分房信息必须拆分为“同房组号”和“分房备注”，方便后续排房联动和人工核对。`确认单/MZ-HD-20260606-CZ杭州【零购江南】客人名单表.xls` 中 `王瑛`、`丁伟` 现在同为 `6房`，分房备注均为 `1大床（必须保证大床）`。
- 验证结果：`cd backend && mvn -q -Dtest=BookingAiImportGoldenSampleTest,SalesBookingOrderServiceTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；已通过真实接口样例验证王瑛/丁伟同房组号和分房备注可正确返回并填入页面。
- 待确认问题：后续如果甲方确认分房要进入正式排房模块，需要把 `room_group` 与房源/房间占用记录建立关联规则；当前先作为收客订单游客名单的人工核对字段。

### 2026-06-24 销售管理 / Word 确认单分房块识别优化

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`。
- 后端接口：增强 `POST /sales/booking/ai-import/recognize`。
- 数据库表：无变更。
- 已实现功能：修复 Word 确认单分房识别错误，避免把所有同样写着 `全程家庭房` 或 `1间双标间` 的游客合并到同一个房间组；游客名单表格继续保留紧凑列宽、固定列宽比例和完整铺满容器。
- 关键业务逻辑：Word/Excel 确认单里的 `分房` 是住宿备注，不等于系统房号。解析器现在先按原始分房块边界拆分，再按 `1间/2间/家庭房` 估算同房组；能明确识别的同住人员写入同一组，不能确定的内容保留在分房备注中，避免静默乱分。当前真实样例中 `张百全 / 张磊 / 曹月焕` 为同一组，`刘松 / 曹睿格` 为同一组，两个家庭房块不再混成一个组。
- 验证结果：先新增失败测试复现 `张百全` 和 `刘松` 被错误分到同一组；修复后 `cd backend && mvn -q -Dtest=BookingAiImportGoldenSampleTest#docxGoldenSampleShouldSplitRoomGroupsByMergedContactBlock test` 通过；`cd backend && mvn -q -Dtest=BookingAiImportGoldenSampleTest test` 通过；`cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：分房规则会随甲方确认单格式继续补金标准样例；遇到图片/PDF 或甲方手写备注时，仍应在页面标记为需要人工确认，不能完全自动确认排房。

### 2026-06-24 销售管理 / 确认单接送站城市兜底识别

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`。
- 后端接口：增强 `POST /sales/booking/ai-import/recognize` 的行程说明识别结果。
- 数据库表：无变更。
- 已实现功能：确认单只写 `大连-上海`、`上海-大连` 这类城市航班信息时，后端按航班到达城市/出发城市兜底生成接送站草稿；前端填入 AI 结果时同步回填 `来程接送站` 和 `返程接送站` 输入框。
- 关键业务逻辑：确认单未明确写机场或火车站时，系统不能虚构具体站点；当前只按城市填入接送站草稿，并提示具体机场/车站需要人工确认。真实样例中来程 `大连-上海 CZ6533` 的接送站填 `上海`，返程 `上海-大连 CZ6536` 的接送站填 `上海`。
- 验证结果：先新增失败测试复现接送站为 `null`；修复后 `cd backend && mvn -q -Dtest=BookingAiImportGoldenSampleTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；已重启后端，`http://127.0.0.1:8080/actuator/health` 返回健康。
- 待确认问题：后续如果要自动识别 `浦东机场`、`虹桥站`、`杭州东站` 等精确站点，需要接入站点字典或在确认单文本中明确出现站点名后再匹配，不能只按城市猜。

### 2026-06-24 销售管理 / 确认单费用表识别与费用说明回填

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`；布局测试 `sales/booking/booking-form-layout.test.ts`。
- 后端接口：增强 `POST /sales/booking/ai-import/recognize` 的价格信息识别结果。
- 数据库表：无变更。
- 已实现功能：Word 确认单里的 `接待标准及注意事项` 费用表现在可识别 `房 / 车 / 门 / 餐 / 导游 / 总计`，例如 `房 9280元`、`车 7000元`、`门 6740元`、`餐 2960元`、`导游 2400元`、`总计 28380元`；AI 弹窗会展示这些费用说明行，用户选择填入 `价格信息` 后，会同步生成价格明细草稿行：`房费 / 车费 / 门票 / 餐费 / 导服`，数量默认为 1，原始计算说明写入备注；同时把完整费用表回填到订单 `费用说明`。
- 关键业务逻辑：这类费用表更像地接成本和接待标准说明，不直接等同于客户应收单价，所以生成的价格明细只作为人工确认草稿；`总计` 只进入费用说明，不再生成明细行，避免重复计算；同时修复 `1.2米以下儿童` 被误识别为 `儿童 1.2元` 的问题，避免把身高当成价格。
- 验证结果：先用真实 Word 样例复现 `priceLines` 只有 `儿童 1.2元`、缺失费用表的问题；修复后 `cd backend && mvn -q -Dtest=BookingAiImportGoldenSampleTest test` 通过；`cd backend && mvn -q -Dtest=BookingAiImportServiceTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：后续如果甲方要求把费用表拆成正式成本明细，需要单独设计成本项目、应收/应付归属和审核逻辑，不能直接把确认单费用表静默写入客户应收价格。

### 2026-06-24 生产部署 / mtravel.apexplore.cn

- 状态：已完成并验证。
- 前端页面：生产构建产物部署到 `/opt/mtravel/frontend/dist`，域名 `http://mtravel.apexplore.cn/`。
- 后端接口：后端 jar 部署到 `/opt/mtravel/backend/app.jar`，通过 `mtravel-api.service` 运行，监听 `127.0.0.1:3002`。
- 数据库表：未执行 DDL。线上使用 PostgreSQL 容器 `postgres-yanxue` 中已存在的独立数据库 `mtravel`，未使用 `yanxue` 业务库。
- 已实现功能：新增宝塔 Nginx 站点 `mtravel.apexplore.cn`，静态文件直接托管，`/api/`、`/uploads/`、`/actuator/health` 反向代理到本机后端；前端生产配置改为 `VITE_GLOB_API_URL=/api`，避免请求 Vben mock 地址。
- 关键业务逻辑：服务器为共享环境，部署只新增 `/opt/mtravel`、`mtravel-api.service` 和独立 Nginx vhost；未重启或修改 `yanxue-api`、`yanxue-web`、Dify、PostgreSQL、Redis 等现有服务。
- 验证结果：`mvn -q -DskipTests package` 通过；`pnpm -F @vben/web-antd run build` 通过；服务器 `curl http://127.0.0.1:3002/actuator/health` 返回 `{"status":"UP"}`；本机外网访问 `curl http://mtravel.apexplore.cn/actuator/health` 返回 `{"status":"UP"}`；`POST http://mtravel.apexplore.cn/api/auth/login` 使用默认管理员账号返回 `code=0`；首页 HTML 标题为 `旅游接待管理系统`。
- 待确认问题：当前站点暂为 HTTP；如果需要 HTTPS，需要后续在宝塔为 `mtravel.apexplore.cn` 申请并绑定证书。

### 2026-06-24 登录认证 / 临时关闭强制自动退出

- 状态：已完成并部署到线上。
- 前端页面：全局登录状态与空闲守卫，涉及 `store/session-idle.ts` 和 `packages/stores/src/modules/access.ts`。
- 后端接口：`POST /auth/login`、JWT 过滤器。
- 数据库表：无变更。
- 已实现功能：新增后端配置 `SESSION_TIMEOUT_ENABLED=false`，当前默认关闭 Redis 会话超时强校验；登录接口在关闭强制退出时返回 `idleTimeoutMinutes=0`；前端收到 0 后不再启动无操作自动退出计时器，也不再定时发送心跳。
- 关键业务逻辑：手动退出仍然有效，退出后 token 会进入黑名单；本次只关闭“长时间不操作自动退出”和“Redis session TTL 到期强制未登录”。JWT 默认有效期调整为 30 天，减少开发阶段频繁登录。
- 验证结果：`mvn -q test` 通过；`pnpm -F @vben/web-antd run typecheck` 通过；`pnpm -F @vben/web-antd run build` 通过；线上 `http://mtravel.apexplore.cn/actuator/health` 返回 `{"status":"UP"}`；线上登录接口返回 `code=0` 且 `idleTimeoutMinutes=0`。
- 待确认问题：系统正式上线前，需要重新讨论是否恢复自动退出，以及是否按客户可配置项开启。

### 2026-06-24 销售管理 / 收客游客类型与身份证校验联动

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`；新增规则测试 `sales/booking/guest-identity.test.ts`。
- 后端接口：无接口变更；保存订单时后端仍会按身份证二次校验并保存校验结果。
- 数据库表：无变更。
- 已实现功能：游客名单导入和页面手工录入都按门票年龄规则自动判定游客类型；18 岁以下为儿童，60 岁及以上为老人，其余为成人；手工修改身份证号后自动回填性别、出生年月、年龄和校验结果；游客表格新增 `身份证校验` 列，显示通过、异常或未校验。
- 关键业务逻辑：身份证号通过前端校验后即时派生性别、出生日期和周岁年龄，客户类型随年龄同步刷新；AI 或人工明确选择 `儿童[不占床]`、`全陪` 时不被普通年龄规则覆盖。订单价格自动生成继续按游客类型数量匹配团队客户类型价格。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/booking/guest-identity.test.ts --dom` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：当前先按甲方本次确认的固定门票年龄规则执行；后续如要支持不同景区的身高、免票、半票规则，需要把景区门票规则配置接入收客游客类型判定。

### 2026-07-01 销售管理 / 收客订单客源地自动生成

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`、`sales/order/index.vue`，前端无新增录入字段。
- 后端接口：`/sales/booking/save` 保存订单时自动回写 `sales_orders.source_province/source_city/source_district`。
- 数据库表：无表结构变更，继续使用 `sales_orders.source_province/source_city/source_district`。
- 已实现功能：新增身份证行政区划资源 `backend/src/main/resources/data/id-card-region-map.json` 和后端解析器，按游客名单第一位有效游客的身份证前六位解析客源地。普通省市区保存省、市、区县；直辖市避免重复城市，例如 `310101` 保存为 `上海市 / 空 / 黄浦区`，列表展示为 `上海市黄浦区`。
- 关键业务逻辑：对齐老系统实测规则，只取第一位有效游客身份证地区。第一位游客无身份证、证件格式不符或行政区划码无法识别时，订单客源地置空，不继续取第二位游客，也不取客户单位所在地。
- 验证结果：先补失败测试确认保存订单不会自动生成客源地；实现后 `cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest#createOrderShouldSaveOldSystemSectionsAndRefreshTeamSeats test` 通过；`cd backend && mvn -q -Dtest=IdCardSourcePlaceResolverTest,SalesBookingOrderServiceTest test` 通过。
- 待确认问题：行政区划数据来自现有前端 `@vant/area-data` 码表，后续如客户要求按最新民政区划更新，需要统一更新前后端区域数据源。

### 2026-06-24 销售管理 / 收客附加费自动计入应收

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`；布局测试 `sales/booking/booking-form-layout.test.ts`。
- 后端接口：无接口变更；继续使用订单价格明细保存附加费行。
- 数据库表：无变更。
- 已实现功能：团队客户类型价格中的 `附加费用` 不再只显示提示；收客订单价格信息会自动生成 `附加费` 明细行，单价取客户类型价格的附加费，数量按占位游客数计算，并计入订单应收。
- 关键业务逻辑：附加费属于团队客户类型价格的一部分，当前按成人、儿童、老人等占位游客人数汇总；儿童不占床和全陪不计入附加费数量。若用户手工维护了附加费或 surcharge 明细，系统不再重复自动生成。
- 验证结果：`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/booking/booking-form-layout.test.ts --dom` 通过。
- 待确认问题：如果后续甲方确认附加费不是按人收，而是按订单或按特殊项目收，需要再增加附加费计价方式字段。

### 2026-06-24 销售管理 / 收客费用变更与名单导出

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`；API 类型 `src/api/sales/booking.ts`；布局测试 `sales/booking/booking-form-layout.test.ts`。
- 后端接口：新增 `POST /sales/booking/fee-change/create`、`POST /sales/booking/fee-change/cancel`、`GET /sales/booking/guest-export`；增强 `POST /sales/booking/save` 和 `GET /sales/booking/detail` 的订单级业务员、收客计调字段。
- 数据库表：`sales_orders` 新增 `salesperson_employee_id/name`、`booking_operator_employee_id/name`；新增统一应收明细表 `sales_order_charge_lines`，用 `line_kind=base_price` 保存订单原始价格，用 `line_kind=adjustment` 保存加收/退减流水；`sales_order_fee_changes` 新增 `fee_project_id/name` 并保留兼容，状态支持 `cancelled`，金额按加收正数、退减负数保存。已同步 `db/013_sales_booking_order_schema.sql`、`db/数据库设计-销售收客订单说明.md` 和 `db/README.md`。
- 已实现功能：收客页 `业务员`、`收客计调` 改为员工下拉并随订单保存；`添加变更费用` 弹窗支持加收/退减、费用项目、费用说明、金额和备注，保存后立即刷新费用变更表和订单应收；费用变更表展示序号、客户名称、变更方向、费用项目、费用说明、金额、登记人、登记时间、状态和作废操作，金额按正负颜色区分；`导出名单` 改为真实下载老系统 `.xls` 格式，列为序号、客人姓名、组号、证件号、性别、出生年月、客户类型、年龄、联系电话、单人备注、领队、组备注，并追加行程、去程、回程合并行。
- 关键业务逻辑：底部订单动作按旧系统口径展示为新增页 `提交订单 / 确认订单 / 取消订单`、修改页 `修改订单 / 确认订单 / 取消订单`；修改/提交保存当前订单状态，确认保存为 `confirmed`，取消二次确认后保存为 `cancelled`。订单价格信息和费用变更现在统一落在 `sales_order_charge_lines`，但仍用 `line_kind` 区分原始价格和后续调整；修改订单只重建 `base_price` 行，不覆盖 `adjustment` 历史流水。费用变更的 `change_type` 只表示方向；统计口径使用企业资料 `resource_projects.resource_type='extra_fee'` 的费用项目 ID 和名称快照；收客页新增变更直接保存为 `approved` 生效记录，没有审批流；退减金额入库为负数，加收入库为正数；订单费用合计只汇总 `approved` 生效变更，作废记录保留历史但不再计入应收；名单导出按提供的老系统 NPOI `.xls` 样式仿制，证件号、手机号等标识字段按文本写入。
- 验证结果：`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts src/views/sales/booking/guest-identity.test.ts` 通过；已执行 `db/013_sales_booking_order_schema.sql` 到 PostgreSQL，并通过 schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。
- 待确认问题：名单导入、电子合同、收款记录和费用变更审批流仍待后续模块接入；真实收款不写入 `sales_order_charge_lines`，后续财务收款应单独设计收款记录和核销分配表。

### 2026-06-24 销售管理 / 团队操作订单列表修正

- 状态：已完成并验证。
- 前端页面：`sales/team/operation.vue`；布局测试 `sales/team/operation-layout.test.ts`。
- 后端接口：团队操作页订单行仍使用团队操作详情接口；修正 `SalesBookingOrderService.toOperationRows` 的订单行组装。
- 数据库表：无变更，继续读取 `sales_order_charge_lines`。
- 已实现功能：`该团相关订单` 表格按老系统字段顺序保持订单信息、接送备注、客源地、客人、人数、价格详情、应收、已收、余额、费用说明、订单备注、日期/预定人、状态，并新增右侧 `修改` 入口；表格增加横向滚动、固定列宽、金额/人数对齐、状态标签和长文本截断，避免订单备注或价格详情把行高撑乱。
- 关键业务逻辑：价格详情不再错误使用订单 `fee_remark`，改为按订单批量读取 `sales_order_charge_lines` 中 `line_kind=base_price` 的原始价格明细并格式化展示；`feeRemark` 和 `orderRemark` 继续分别展示费用说明和订单备注。点击订单行或右侧 `修改` 都进入 `/sales/team/booking/{teamId}/{orderId}`。
- 验证结果：`cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest#operationRowsShouldUseChargeLinesForPriceDetailWithoutMixingRemarks test` 通过；`cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest,SalesTeamScheduleServiceTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：当前只修正订单列表展示和修改入口；取消订单、收款记录、电子合同等仍按后续模块继续接入。

### 2026-06-26 系统设置 / 客户风控审批与系统配置

- 状态：已完成并验证。
- 前端页面：新增 `system/config/index.vue` 系统配置页；新增 `system/risk-approval/index.vue` 总经理审批页；增强 `sales/booking/form.vue` 客户单位选择、订单提交和修改前的客户风控校验。
- 后端接口：新增 `GET/POST /system/config/business-risk`、`GET/POST /system/config/map`；增强 `SystemConfigController` 的系统配置能力；新增 `GET /customer/risk-approval/check`、`POST /customer/risk-approval/apply`、`GET /customer/risk-approval/page`、`GET /customer/risk-approval/detail`、`POST /customer/risk-approval/approve`、`POST /customer/risk-approval/reject`；增强 `POST /sales/booking/save` 支持 `riskApprovalRequestId` 并保存前复查。
- 数据库表：新增 `customer_risk_approval_requests`，保存客户合同到期、授信超限时的总经理审批流水和申请时快照；`system_configs` 新增配置项 `customer_risk_approval_enabled`，并纳入百炼、高德、登录安全配置页面。已同步 `db/014_customer_risk_approval_schema.sql`、`db/003_system_config_schema.sql`、`db/数据库设计-客户风控审批说明.md`、`db/数据库设计-系统配置说明.md` 和 `db/README.md`。
- 已实现功能：收客页选择客户单位后自动校验合同到期和授信额度；配置关闭时只显示风险提醒，不阻断订单保存；配置开启且存在风险时，后端强制要求传入已通过的总经理审批单；收客页可发起“申请总经理审批”；系统配置页可配置客户风险总经理审批开关、百炼 Key/模型、高德 Key 和登录无操作退出时间；总经理审批页可分页查看申请、查看风险快照、同意或拒绝审批。
- 关键业务逻辑：客户风控审批单只作为订单保存授权流水，不写入订单价格明细、费用变更、真实收款、成本或发票表；审批申请保存申请时的合同有效期、授信额度、已占用、审批中、可用额度和超限金额快照，方便后续查账解释风险订单为什么被放行；订单保存时会重新计算当前订单应收并复查审批单客户、团队、订单和金额是否匹配，审批金额小于当前订单金额时要求重新申请。
- 验证结果：`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts src/views/system/system-config-layout.test.ts src/views/system/risk-approval-layout.test.ts src/router/routes/__tests__/visible-menu.test.ts` 通过；已执行 `db/014_customer_risk_approval_schema.sql` 和 `db/003_system_config_schema.sql` 到 PostgreSQL；schema guardian 校验 `customer_risk_approval_requests` 与 `system_configs` 均为 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`；本地后端已重启，`http://127.0.0.1:8080/actuator/health` 返回 `UP`，通过前端代理登录、读取业务风控配置、审批列表和真实客户风控校验接口。
- 待确认问题：当前审批权限按角色编码 `boss` 或 `admin` 控制；后续如果要做更细的权限菜单、审批消息通知、审批撤回或多级审批，需要在企业角色权限和消息中心模块继续扩展。

### 2026-06-26 系统设置 / 风控审批通过后收客页复用修正

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`。
- 后端接口：增强 `GET /customer/risk-approval/check` 返回可复用的已通过审批单 ID、编号、状态和审批金额；`POST /customer/risk-approval/apply` 在已有同客户、同团队、同订单或未绑定订单且金额足够的已通过审批单时直接返回该审批单，不重复生成待审批申请。
- 数据库表：`customer_risk_approval_requests` 新增唯一索引 `uk_customer_risk_approval_tenant_order_approved_active`，约束同一租户同一订单只能有一张未删除且已同意的风控审批单。
- 已实现功能：修复总经理审批已同意后，收客页仍显示“审批通过后可提交订单”、再次点击申请会生成新的待审批单并导致提交仍被拦的问题。现在收客页刷新客户风控时会自动绑定最新可用的已通过审批单，已通过时不再显示申请按钮。
- 关键业务逻辑：审批复用必须满足同租户、同客户、同团队，订单为当前订单或审批单尚未绑定订单，且审批金额大于等于当前订单预计应收金额；已绑定其他订单的审批单不能被新订单复用。审批单绑定订单时还必须与订单当前客户一致，订单客户为空或客户不一致时不能作为有效授权。历史重复已同意审批保留最新一张有效，其余转为 `cancelled`，不物理删除。
- 验证结果：`cd backend && mvn -q -Dtest=CustomerRiskApprovalServiceTest#checkShouldReturnReusableApprovedApprovalForCurrentRiskAmount test` 通过；`cd backend && mvn -q -Dtest=CustomerRiskApprovalServiceTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts src/views/system/risk-approval-layout.test.ts` 通过；schema guardian 校验 `customer_risk_approval_requests` 为 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。
- 待确认问题：无。

### 2026-06-26 销售管理 / 收客交通时间回填与名单导入

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`；新增交通信息解析工具 `sales/booking/traffic-info.ts` 和游客导入去重工具 `sales/booking/guest-dedupe.ts`；增强布局测试 `sales/booking/booking-form-layout.test.ts`。
- 后端接口：新增 `GET /sales/booking/guest-import/template` 下载空白游客名单导入模板；新增 `POST /sales/booking/guest-import/preview`，用于 Excel 游客名单导入预览；保存订单仍由 `POST /sales/booking/save` 统一落库。
- 数据库表：无变更。
- 已实现功能：修复 AI 辅助识别出的出发/抵达日期时间保存后重新打开丢失的问题；旧订单保存的接送交通字符串会解析回城市/站点、航班/车次、出发时间、抵达时间，并统一为 `YYYY/MM/DD HH:mm` 供 DatePicker 显示。游客名单工具栏新增 `下载模板`，可直接下载空白 `.xls` 导入模板，不需要先导出已有订单再删除游客信息。`导入名单` 从占位按钮改为真实 Excel 上传，支持 `.xls/.xlsx`，按老系统导出模板的 `序号、客人姓名、组号、证件号、性别、出生年月、客户类型、年龄、联系电话、单人备注、领队、组备注` 表头解析，追加到当前游客名单，并按证件号或姓名+电话去重。
- 关键业务逻辑：名单导入预览不直接写数据库，前端追加到当前订单游客草稿，用户点击提交/修改订单后才通过订单保存接口落库；身份证号由程序校验并推导性别、出生年月、年龄和校验状态；客户类型按当前门票年龄规则计算，18 岁以下儿童，60 岁及以上老人，其余成人，避免导入文件里把所有人都写成成人导致价格统计错误；价格信息会在名单导入后重新按客户类型价格和游客数量同步。Excel 导入、AI 辅助录入和已有订单游客回显共用同一套前端去重规则，优先按证件号去重，没有证件号时按姓名 + 电话去重；导入前和详情回显时都会先收敛当前表格内已存在的重复游客，保留第一条、跳过后续重复行，避免同一身份证重复填入游客表。AI 辅助录入不再回填客户单位、联系人和联系电话，也不再提供 `客户信息` 模块；客户信息必须从系统客户档案下拉选择或由客户档案带出，避免 AI 识别结果影响风控审批、合同和授信校验。
- 验证结果：先补失败测试确认 `traffic-info.ts`、`importGuestWorkbookPreview`、空白模板下载、AI 客户信息禁填规则和 AI 游客去重规则缺失；实现后 `cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/guest-dedupe.test.ts src/views/sales/booking/traffic-info.test.ts src/views/sales/booking/booking-form-layout.test.ts src/views/sales/booking/guest-identity.test.ts` 通过；本地后端已重启，`http://127.0.0.1:8080/actuator/health` 返回 `UP`；通过前端代理登录后，调用 `GET /api/sales/booking/guest-import/template` 返回 `application/vnd.ms-excel`、文件头为 Excel 97-2003 格式；用 `/Users/woniu/Downloads/名单导出CS-DJ-BK-260625A_张三_1人_20260624171006.xls` 调用 `POST /api/sales/booking/guest-import/preview` 返回 `code=0`，可解析出 1 条游客草稿并返回身份证校验提示。
- 待确认问题：当前导入支持老系统样式和常见名单表头；如果后续客户提供更多非标准 Excel 模板，需要接入字段映射配置或模板管理，不应继续在收客页里堆特殊解析规则。

### 2026-06-26 销售管理 / 团队操作相关订单字段对齐

- 状态：已完成并验证。
- 前端页面：`sales/team/operation.vue`；API 类型 `src/api/sales/team.ts`、`src/api/sales/booking.ts`；布局测试 `sales/team/operation-layout.test.ts`。
- 后端接口：团队操作详情接口的订单行新增 `pickupInfo`、`dropoffInfo`、`originalOrderInfo`、`guestCountText`；收客订单保存和详情接口新增 `originalOrderInfo`。
- 数据库表：`sales_orders` 新增 `original_order_info`，用于保存拼团、转团或来源订单摘要；已同步 `db/013_sales_booking_order_schema.sql` 和 `db/数据库设计-销售收客订单说明.md`。
- 已实现功能：`该团相关订单` 表格的 `订单信息` 按旧系统内容展示订单号、客户单位/客户团号、接站、送站和原始订单信息；`人数` 显示为 `总人数[成人/儿童/老人]`，儿童占床和儿童不占床合并到儿童数量；价格详情、费用说明和订单备注支持多行显示。
- 关键业务逻辑：`original_order_info` 是来源追溯摘要，不参与应收、实收、余额、费用变更、成本或财务统计计算；修改订单时如果前端未传该字段，后端保留原值，避免历史来源信息被误清空。
- 验证结果：`cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest#operationRowsShouldUseChargeLinesForPriceDetailWithoutMixingRemarks test` 通过；`cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest,SalesTeamScheduleServiceTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；已执行 `sales_orders.original_order_info` DDL 到本地 PostgreSQL；schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。
- 待确认问题：后续如要做正式拼团、转团流程，需要在订单流转模块补来源订单 ID、目标订单 ID、流转日志和权限控制，不能只依赖摘要文本。

### 2026-06-26 销售管理 / 团队操作订单备注省略展示

- 状态：已完成并验证。
- 前端页面：`sales/team/operation.vue`；布局测试 `sales/team/operation-layout.test.ts`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：`该团相关订单` 表格的 `订单备注` 改为最多三行省略显示，鼠标悬停通过浮层查看完整备注，避免长备注把整行和页面高度撑开。
- 关键业务逻辑：只调整前端展示，不改变订单备注保存、返回和统计逻辑。
- 验证结果：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：无。

### 2026-06-26 销售管理 / 修改订单客户单位下拉修正

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`；布局测试 `sales/booking/booking-form-layout.test.ts`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：修复修改收客订单时客户单位下拉只显示当前客户、看起来不能切换其他客户的问题。现在打开客户单位下拉会重新加载可选客户列表，搜索时也会强制刷新候选项。
- 关键业务逻辑：修改订单仍允许切换客户单位；切换客户后会清空原风险审批单并重新校验新客户合同和授信风险，避免旧客户审批被错误复用。
- 验证结果：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：无。

### 2026-06-26 销售管理 / 修改订单切换客户审批单隔离

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`；布局测试 `sales/booking/booking-form-layout.test.ts`。
- 后端接口：无变更，继续依赖后端校验审批单客户与订单客户一致。
- 数据库表：无变更。
- 已实现功能：修复修改订单切换客户后保存时报“审批单与订单客户不一致，不能绑定”的问题。前端现在记录审批单对应的客户 ID，保存时只提交当前客户对应的审批单；切换客户后旧客户审批单不会随新客户订单保存。
- 关键业务逻辑：总经理审批单不能跨客户复用。切换客户后必须按新客户重新校验合同和授信风险；若新客户仍需审批，需要重新申请或复用后端返回的同客户可用审批单。
- 验证结果：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：无。

### 2026-06-26 销售管理 / 团队操作拼团转团闭环

- 状态：已完成完整订单拼团和转团核心链路，并验证单元测试和前端类型检查。
- 前端页面：`sales/team/operation.vue`；API 类型 `src/api/sales/team.ts`。
- 后端接口：`POST /sales/team/{teamId}/operation/merge` 执行拼团，继续使用原路径，请求体为 `orderIds + targetTeamId + tagFlag + remark + remarks`；`POST /sales/team/{teamId}/operation/move` 执行转团。
- 数据库表：`sales_order_transfer_logs` 保存来源订单、原团队、目标团队、目标订单、流转类型、状态、打标、操作人、操作时间和备注；`sales_orders` 新增 `order_role`，取值 `normal`、`merge_source`、`merge_child`，用于区分普通订单、拼团来源留痕订单和拼团目标子订单；已同步 `db/013_sales_booking_order_schema.sql`、`db/数据库设计-销售收客订单说明.md` 和老系统拼团调研文档。
- 已实现功能：团队操作页 `拼团操作` 改为标准系统风格弹窗，支持选择多个来源订单，但只能选择一个目标团队；弹窗展示已选订单摘要、目标团队、标记和备注，不再做游客/金额拆分。`转团操作` 支持转到已有团队或复制当前团队生成新团队后转入；提交后迁移订单当前归属，并同步订单应收明细和游客名单的团队 ID。
- 关键业务逻辑：一个来源订单不能拼到多个目标团队；多个来源订单可以批量拼到同一个目标团队。后端强制来源订单属于当前团队、未取消、未执行过拼团、不是拼团子订单且必须有游客名单；来源订单保留并标记为 `merge_source`，团队操作页按老系统继续显示该来源订单和“拼团订单信息”，但不参与来源团队人数、游客名单、收入和毛利统计；目标团队下生成 `merge_child` 子订单，复制完整来源订单游客名单和 `base_price` 应收明细，`received_amount` 默认 `0`，`balance_amount` 等于子订单应收。转团会迁移订单归属，不生成子订单，订单收入、应收明细和游客名单随订单迁到目标团队。两个动作都必须校验不能拼/转到当前团队，取消或停收团队不能作为目标团队；操作后刷新原团队和目标团队实收/余位。`sales_orders.original_order_info` 只保存页面追溯摘要，完整审计以 `sales_order_transfer_logs` 为准。
- 验证结果：先补失败测试确认旧拆分方案不符合当前业务口径；实现后 `cd backend && mvn -q -Dtest=SalesOrderTransferServiceTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过。2026-06-26 已补执行联调库 DDL：`sales_orders.order_role`、`sales_order_transfer_logs` 及相关约束、索引、注释；调用 `/api/sales/booking/team/28` 和 `/api/sales/team/28/operation` 均返回 200；schema guardian 校验 `sales_orders,sales_order_transfer_logs` 为 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。
- 待确认问题：`拼团订单` 菜单的待拼团订单池、已拼团、子订单、按团展示列表仍未接入，本次只完成团队操作页按钮闭环。

### 2026-06-26 销售管理 / AI辅助录入OCR错误提示修正

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`，页面调用链路无变更。
- 后端接口：`POST /sales/booking/ai-import/recognize`。
- 数据库表：无变更。
- 已实现功能：排查收客页图片确认单识别时提示“当前文件需要AI视觉/OCR识别服务，请先配置百炼视觉模型后使用”的问题；当前系统配置接口已能读取脱敏后的百炼 Key，图片附件和 Word 附件通过真实接口均可识别。后端将 OCR 失败原因拆分为缺少百炼 API Key、上传文件为空、百炼接口调用失败、百炼未返回内容、视觉服务未启用等明确提示，避免把外部调用失败误报为“未配置视觉模型”。
- 关键业务逻辑：文本、Word、Excel 仍可走本地解析兜底；图片/PDF 必须依赖百炼视觉/OCR，缺少 Key 或外部服务失败时不应静默返回空文本。系统配置只允许返回脱敏 Key，真实 Key 不能写入代码、SQL、Markdown 或日志。
- 验证结果：先补失败测试确认旧实现会把缺 Key、百炼空返回、百炼 HTTP 失败都吞成 `Optional.empty()`；实现后 `cd backend && mvn -q -Dtest=BailianAiModelClientWiringTest test` 通过；`cd backend && mvn -q -Dtest=BailianAiModelClientWiringTest,BookingAiImportServiceTest,BookingAiImportServiceWiringTest,BookingAiImportControllerTest,AiConfigServiceTest test` 通过；`cd backend && mvn -q test` 通过。
- 待确认问题：如果用户页面仍出现 OCR 报错，需要以新提示为准继续判断是百炼服务瞬时失败、模型名称不可用，还是当前浏览器会话未刷新到最新后端。

### 2026-06-26 销售管理 / 团队操作拼团订单标识与互跳

- 状态：已完成并验证。
- 前端页面：`sales/team/operation.vue`；API 类型 `src/api/sales/team.ts`；布局测试 `sales/team/operation-layout.test.ts`。
- 后端接口：`GET /sales/team/{teamId}/operation` 的订单行新增 `sourceOrderInfos`，`mergeOrderInfos` 继续表示来源订单已拼至的目标团。
- 数据库表：无新增表和字段，继续使用 `sales_order_transfer_logs` 作为来源订单、来源团、目标团、目标订单的一一对应关系。
- 已实现功能：来源团页面的拼团来源订单标签从“拼团来源”改为“已拼出”，关系文案改为“已拼至：目标团”；目标团页面的拼入订单标签改为“拼入订单”，关系文案显示“来源订单：来源团”。两类关系文字均可点击跳转到对方团队操作页。
- 关键业务逻辑：页面跳转不解析摘要文本，后端通过 `sales_order_transfer_logs` 组装 `teamId/orderId/summary` 结构化关系，避免团号改名或文案变化导致跳转失效。来源团订单仍不参与来源团人数和收入统计，目标团拼入订单参与目标团统计。
- 验证结果：先补失败测试确认前端缺“已拼至/来源订单/goRelatedTeam”，后端缺 `sourceOrderInfos` 和按目标订单查拼团日志；实现后 `cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest test` 通过；`cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest,SalesOrderTransferServiceTest,SalesTeamControllerMappingTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。已重启后端并通过真实接口验证 team 28 返回 2 条 `已拼出` 订单且可跳 team 27，team 27 返回 2 条 `拼入订单` 且可回跳 team 28；Playwright 只读验证页面文字和跳转正常。
- 待确认问题：目标团页面目前仍同时显示旧 `originalOrderInfo` 文本追溯和新的结构化“来源订单”跳转信息，如认为重复，可下一步在存在 `sourceOrderInfos` 时隐藏旧文本。

### 2026-06-26 销售管理 / AI辅助录入粘贴文本游客识别

- 状态：已完成并验证。
- 前端页面：`sales/booking/form.vue`，粘贴文本提交链路无变更。
- 后端接口：增强 `POST /sales/booking/ai-import/recognize`。
- 数据库表：无变更。
- 已实现功能：修复 AI 辅助录入弹窗左侧文本框直接粘贴微信确认内容后，右侧识别结果没有游客名单的问题。后端本地解析器新增对 `姓名+身份证号+可选手机号` 无序号名单行的识别，并支持 `27号济南-南京南G75 (08.15-10.21)` 这类只有“几号”的车次行。
- 关键业务逻辑：前端已正常提交 `text` 字段，问题在后端规则未覆盖微信文本格式。本次仍只返回可编辑草稿，不自动保存订单或游客名单；身份证合法性、出生日期、性别和年龄继续由程序从身份证反推。只有“几号”的交通日期按当前年月推断，遇到后续日期号变小则按下月处理，避免 27 号去程和 1 号返程排序反转。
- 验证结果：先补失败测试 `recognizeShouldExtractWechatTextGuestsWithoutSequenceNumber` 复现截图文本识别不到车次/游客；实现后 `cd backend && mvn -q -Dtest=BookingAiImportServiceTest#recognizeShouldExtractWechatTextGuestsWithoutSequenceNumber test` 通过；`cd backend && mvn -q -Dtest=BookingAiImportServiceTest,BookingAiImportGoldenSampleTest,IdCardValidatorTest,BailianAiModelClientWiringTest,BookingAiImportServiceWiringTest,BookingAiImportControllerTest test` 通过；`cd backend && mvn -q test` 通过。已重启 8080 后端，并用真实接口粘贴同一段文本验证返回去程 `G75`、返程 `G804`、游客数 4、疑似漏识别 0，4 名游客身份证均校验通过。
- 待确认问题：无。

### 2026-06-26 销售管理 / 已拼出来源订单禁止修改

- 状态：已完成并验证。
- 前端页面：`sales/team/operation.vue`；布局测试 `sales/team/operation-layout.test.ts`。
- 后端接口：增强 `POST /sales/booking/save`、`POST /sales/booking/fee-change/create`、`POST /sales/booking/fee-change/cancel` 的业务校验。
- 数据库表：无变更。
- 已实现功能：团队操作页中 `已拼出` 的来源留痕订单不再显示 `修改`，改为 `去目标团`；点击来源留痕订单行也跳转到目标团队，不再进入来源订单修改页。
- 关键业务逻辑：`merge_source` 是来源团队留痕订单，不参与来源团队人数、收入和毛利统计，因此不能继续修改订单主体、价格明细、游客名单或费用变更。后端保存订单、新增费用变更、作废费用变更时统一拦截 `merge_source`，提示“已拼出的来源订单不能修改，请到目标团队处理拼入订单”。目标团队的 `merge_child` 拼入订单仍允许修改，作为实际执行和统计订单。
- 验证结果：先补失败测试确认前端缺少 `去目标团` 分支、后端仍允许修改来源留痕订单；实现后 `cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest#saveOrderShouldRejectMergeSourceOrderModification+createFeeChangeShouldRejectMergeSourceOrder+cancelFeeChangeShouldRejectMergeSourceOrder test` 通过；`cd backend && mvn -q test` 通过。
- 待确认问题：后续如果需要“撤销拼团”，应新增独立撤销流程恢复来源订单参与统计并作废目标团拼入订单，不能通过直接修改来源留痕订单实现。

### 2026-06-27 销售管理 / 团队管理直接创建散拼整团散团

- 状态：已完成并验证。
- 前端页面：新增 `sales/team/create.vue` 和 `sales/team/arrangement.vue`；团队管理列表 `sales/team/index.vue` 顶部 `散拼`、`整团`、`散团` 入口跳转到 `/sales/team/create/:type`；团队列表点击团号进入团队操作页，点击团队名称进入 `/sales/team/edit/:id` 修改团队；团队操作页 `修改团队` 也跳转 `/sales/team/edit/:id` 并复用同一添加/修改团队页面；`单项` 仍保留为后续独立入口。
- 后端接口：新增 `POST /sales/team/create`；新增 `GET /sales/team/{teamId}/edit` 查询团队直接编辑详情，`POST /sales/team/{teamId}/edit` 保存团队直接编辑页。请求体为团队基础信息、行程内容、产品说明和操作备注，返回团队信息。
- 数据库表：无新增表和字段；保存时写入已有 `sales_products` 产品快照、`sales_product_descriptions` 产品说明、`sales_product_itinerary_days` 每日行程、`sales_product_roadbook_points` 路书点位、`sales_teams` 正式团队，团号日志继续写 `sales_team_no_logs`；直接创建团队不写 `sales_team_prices`，避免生成无业务意义的默认 0 元价格行。
- 已实现功能：按老系统 `LineAdd.aspx?LineType=2/3/4` 和团队操作页 `TeamOperation.aspx -> LineAdd.aspx?GroupId=...` 的“添加/修改产品团队”业务流程，新增散拼、整团、散团共用创建页，修改团队也复用该页面。页面保留新系统视觉风格和 `BusinessPillTabs`，页签为 `基本信息 / 行程内容 / 产品说明 / 团队安排`。不带老系统的 `业务类型管理` 页签，因为新系统业务类型在产品字典/基础配置中维护。基本信息页复用产品表单字段口径，页面文案使用团队口径 `添加/修改团队` 和 `保存团队`；行程内容和创建产品页保持同款能力，按旅游天数自动生成每日行程，支持线路行程、住宿说明、关联酒店下拉、用餐、旺季附加费、路书摘要、全屏路书地图、地点搜索、地图点选和驾车路线计算；关联酒店下拉来自采购酒店资源；产品说明保存收客须知、费用包含/不含、儿童安排、购物、自费、赠送、注意事项和温馨提醒；`团队安排` 页签不再显示空白内嵌面板，点击后先保存团队，再按老系统 `GroupOverview.aspx?GroupId=...` 逻辑跳转到独立团队安排页面 `/sales/team/arrangement/{teamId}`。独立团队安排页当前已完成页面结构，复用产品团队安排页的顶部摘要、阶段条、内部备注、成本总览、总览分类入口和大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加分区；每个分区保留 `无需 / 完成 / +` 操作，`+` 可打开对应分类弹窗结构。2026-06-28 已将产品团队安排页和正式团队安排页的外壳样式抽为共享 `src/views/sales/team-arrangement-layout.css`，两个页面共同使用 `team-arrangement-card / arrangement-command-bar / arrangement-overview-table / arrangement-section-card` 等布局类，避免后续再维护两套团队安排视觉；正式团队安排页另加团队页专属顶部排版，按老系统补回 `票据库 / 订单文件 / 备用金请款单 / 打印付款单 / 打印行程单 / 打印团队名单 / 打印计划单 / 打印结算单 / 打印毛利表` 工具条，团名改为独立整行完整展示，团队标签独立成行，不再挤压长团名。2026-06-28 继续按 9.5 分目标打磨第一屏视觉，工具条增加 `团队工具` 标识并降权为轻量按钮，团队信息区收敛为标题、标签、内联执行进度、指标四层，成本表、分类导航、图标入口和分区表格统一压缩高度、弱化边框、减少强蓝面积，提升第一屏数据可见度。2026-06-28 进一步将执行进度从右侧独立卡片改为顶部信息区内联横向流程条，和 `团队管理 / 查看行程` 快捷按钮同一行展示，解决右侧大面积空白、嵌套卡片感和按钮分散问题；团队指标条改为正式页专用 4 列稳定网格，避免 `已付`、`预算利润` 等指标在不同宽度下不规则换行和分割线错位。2026-06-28 本次继续按用户反馈调整正式团队安排页：保留顶部 9 个团队工具按钮全部直接显示，不再折叠为 `更多打印`；顶部摘要区、右侧阶段条、`团队管理 / 查看行程` 按钮、指标条、分类 Tabs 和图标分类入口改回产品团队安排页同款布局，只保留正式团队阶段文案 `团队信息 / 安排配置 / 发团 / 结算 / 完成`，避免正式团队页继续维护一套独立视觉；由于正式团队名称通常比产品模板名称更长，标题区在正式页适配为团名、业务标签、指标三层展示，避免团名和标签挤在同一行；指标条在正式页固定为单行等宽展示，`预算利润` 不再换到第二行；分类 Tabs 增加正式页分组容器，Tabs 行按实际按钮宽度收缩，图标分类入口保持整宽，避免 Tabs 右侧出现大块空白框。2026-06-29 将产品模板安排页和正式团队安排页的分类 Tabs 与图标快捷入口统一为共享 `arrangement-tabs-block` 结构，删除正式团队页专用 `formal-arrangement-tabs-block` 覆盖，两个页面的 tab 行、图标入口、边框衔接和高度行为完全复用同一套 CSS。2026-06-29 按最终确认口径改为“以正式团队安排页为样式基准”，产品团队安排页套用正式团队页的大标题、标签分行、单行指标条、成本总览标题和压缩分区表格样式；正式团队页继续保留顶部 `团队工具`，产品页不增加该业务工具条。
- 关键业务逻辑：新系统仍以正式团队为执行主体。直接建团创建的是团队，不是产品模板；现阶段由于团队列表、团队操作页仍依赖产品字段，团队基础信息和行程说明仍沿用既有快照机制，但团队安排页面不把安排明细保存到产品安排表，也不调用产品安排保存 API。团队安排属于正式团队执行数据，入口必须使用 `sales_teams.id`，不能使用 `sales_products.id`；本阶段暂不新增正式排团表，`无需 / 完成` 仅为页面临时状态，弹窗保存统一提示“正式排团数据表设计后接入保存”。产品模板安排 `/sales/product/team-arrangement/{productId}` 只作为产品默认安排参数，不承载真实团队成本、打印、备用金和毛利。创建团页面不维护客户类型、成人价、儿童价、老人价和附加费；团队价格后续应在团期价格维护或收客订单价格链路中录入。散拼团号沿用 `CS-SP-BK-yyMMddX`，整团和散团使用 `CS-BK-yyMMddX`；团号后缀按同租户、同日期、同前缀查重，匹配 `sales_teams(tenant_id, team_no)` 唯一约束，不能按新建产品快照隔离。单项业务不走本接口，后续按单项订单、客户、价格、游客名单链路单独开发。
- 验证结果：先补 `SalesTeamScheduleServiceTest#directCreateShouldCreateProductAndTeamWithoutDefaultPriceForZhengtuan` 失败测试，再实现接口和页面；后续补 `directCreateShouldAvoidTeamNoCollisionAcrossProductSnapshots` 覆盖同日同前缀跨产品快照团号冲突，补 `directCreateShouldPersistProductDescriptionAndItineraryTabs` 覆盖产品说明、每日行程和路书点位保存，补 `directCreateShouldUseTeamNoInProductSnapshotName` 覆盖直接建团快照名追加团号，补 `directEditDetailShouldLoadTeamProductDescriptionAndRoadbook` 覆盖修改页回显团队、产品说明、行程和路书，补 `directUpdateShouldUpdateExistingTeamAndProductSnapshotWithoutCreatingNewTeam` 覆盖修改团队只更新原团队和原快照；补 `operation-layout.test.ts` 页面结构测试确认创建/修改页使用四页签、不包含 `业务类型管理`、不包含客户类型价格区、包含关联酒店/路书/高德路线能力、团队安排按 `teamId` 跳转到 `/sales/team/arrangement/{teamId}` 且不再跳产品模板安排页；2026-06-28 继续补测试确认团队安排页为页面结构版，不出现 `产品模板` 文案，不调用产品安排保存 API，并包含完整安排分类、`无需 / 完成 / +` 入口和“正式排团数据表设计后接入保存”提示；2026-06-28 重新启动前端并用 Playwright 登录验证 `/sales/team/arrangement/22`，最终截图为 `output/playwright/team-arrangement-polish-v3.png`，按信息层级、首屏效率、风格统一、可读性、操作清晰度和细节自评 9.5/10；本次执行进度改为内联横向流程条，补充 `formal-inline-progress-row / formal-inline-progress-head / formal-inline-progress-actions / formal-inline-progress-rail` 结构断言，并确认不再包含 `formal-progress-panel`，最终截图为 `output/playwright/team-arrangement-progress-inline.png`；本次指标条稳定网格补充 `formal-team-metric-grid / formal-team-metric-item` 结构断言，最终截图为 `output/playwright/team-arrangement-metric-grid.png`；本次 UI 打磨先补充 `formal-print-more-button / formal-section-nav / formal-cost-overview-title` 结构断言并确认旧结构失败，再实现并移除正式页 `arrangement-icon-grid / compact-category-strip` 重复分类入口，最终截图为 `output/playwright/team-arrangement-ui-polish-final.png`；本次按用户反馈改回产品页同款布局，补充 `command-side / workflow-rail / stage-flow-item / arrangement-icon-grid compact-category-strip` 结构断言，并确认不再包含 `formal-step-segment-* / formal-inline-progress-* / formal-print-more-button / primaryToolActions / printToolActions / 更多打印`，最终截图为 `output/playwright/team-arrangement-product-layout-aligned.png`；2026-06-29 先补结构测试确认产品页缺共享 `arrangement-tabs-block` 时失败，再实现共享容器，验证 `/sales/team/arrangement/22` 和 `/sales/product/team-arrangement/15` 的 DOM 均为 `arrangement-tabs-block -> arrangement-overview-tabs + arrangement-icon-grid`，均为 11 个 tab、10 个快捷入口且无 `formal-arrangement-tabs-block`，截图保存为 `output/playwright/team-arrangement-shared-tabs.png` 和 `output/playwright/product-arrangement-shared-tabs.png`；2026-06-29 按“以团队安排页为准”补结构测试并实现后，Playwright 验证 `/sales/product/team-arrangement/15` 已使用 `formal-team-title-line / formal-team-badges / formal-team-metric-strip / formal-cost-overview-title`，7 个指标项均带 `metric-*` class，且无 `formal-arrangement-tool-strip`；`/sales/team/arrangement/22` 保留 `formal-arrangement-tool-strip`，两页核心布局截图保存为 `output/playwright/product-arrangement-team-style-final.png` 和 `output/playwright/team-arrangement-team-style-final.png`；`cd backend && mvn -q -Dtest=SalesTeamScheduleServiceTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/product/product-form-utils.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：单项业务创建入口尚未开发；正式团队安排总览当前只完成页面结构，导游、大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加等资源明细保存、真实成本表、应付付款、导游报账和财务审核后续按正式团队安排模块单独设计。

### 2026-06-29 计调操作 / 导游排班与请假

- 状态：已完成后端核心接口、数据库脚本、本地页面接入和验证。
- 前端页面：`dispatch/guide-schedule/index.vue` 导游排班汇总；`dispatch/guide-leave/index.vue` 导游请假管理；`guide/my-leave/index.vue` 导游本人请假；`sales/team/arrangement.vue` 正式团队安排页导游分区。
- 后端接口：`GET /dispatch/guide-schedule/calendar`；`GET /dispatch/guide-leaves/page`；`POST /dispatch/guide-leaves/direct`；`POST /dispatch/guide-leaves/{leaveId}/approve`；`POST /dispatch/guide-leaves/{leaveId}/reject`；`GET /guide/my-leave`；`POST /guide/my-leave`；`POST /guide/my-leave/{leaveId}/withdraw`；`GET /sales/team/{teamId}/guides`；`POST /sales/team/{teamId}/guides/create`；`POST /sales/team/{teamId}/guides/{recordId}/field`；`POST /sales/team/{teamId}/guides/{recordId}/delete`。
- 数据库表：新增 `dispatch_team_guides` 和 `dispatch_guide_leave_records`，本地 SQL 为 `db/015_dispatch_guide_schedule_schema.sql`，说明文档为 `db/数据库设计-导游排班与请假说明.md`。
- 已实现功能：团队安排页新增真实导游分区，支持添加导游、行内修改导游、导服费、备用金、操作费、上团时间、下团时间、费用说明、导游备注、待定中，并支持删除导游安排；成本总览中的导服、操作费、备用金汇总读取真实导游安排金额。导游排班汇总默认展示所有启用导游，没有团队安排或已通过请假的导游也显示空闲行，空闲日期用弱灰色 `空闲` 标记为可派；有占用时按导游和日期展示团队占用与请假占用，团队占用可点击进入对应团队安排导游分区。后台请假管理支持按导游、状态、日期查询，计调可直接设置不可上团时间，也可审批通过或驳回导游申请。导游端页面支持本人提交请假申请和撤回待审批记录。
- 关键业务逻辑：导游排班不是独立维护表，行数据先来自 `enterprise_guides` 中启用且未删除的导游档案，再聚合 `dispatch_team_guides` 和已通过的 `dispatch_guide_leave_records`；时间冲突按 `existing.start_at < new.end_at AND existing.end_at > new.start_at` 判断。新增或修改团队导游安排时，必须检查同一导游已有生效团队安排和已通过请假；审批通过请假和计调直接设置请假时，必须检查同一导游已有生效团队安排；待审批请假不阻断团队安排，审批通过时再硬拦截；导游账号通过 `enterprise_guides.username` 绑定到当前登录账号。团队导游安排采用老系统类似的单字段保存模式，但第一版暂未接入导游报账或备用金已支付后的导游姓名锁定规则。
- 验证结果：先补 `DispatchGuideScheduleServiceTest` 覆盖团队安排冲突、请假审批冲突、计调直设请假、排班聚合和空闲启用导游展示；先补前端 `guide-dispatch-feature.test.ts` 确认两个请假页面、团队导游分区和空闲状态标识缺失时失败，再实现页面；`cd frontend-preview && pnpm vitest run apps/web-antd/src/views/dispatch/guide-schedule/guide-dispatch-feature.test.ts --dom` 通过，4 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd backend && mvn -q test` 通过；已执行 `db/015_dispatch_guide_schedule_schema.sql` 到当前联调库，schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`；重启后端后，登录态调用 `GET /dispatch/guide-schedule/calendar?startDate=2026-06-29` 返回 200。
- 待确认问题：导游端当前复用管理后台路由和登录体系，后续如有独立导游小程序或 H5，需要复用这些接口或单独做移动端鉴权；导游备用金审批、导游报账、导游结算和企业码额度锁定仍在后续模块处理。

### 2026-06-29 销售管理 / 团队安排弹窗复用产品排团弹窗

- 状态：已完成前端组件复用和结构验证。
- 前端页面：`sales/product/team-arrangement.vue`、`sales/team/arrangement.vue`，共享组件 `sales/components/ArrangementEditorModal.vue`，共享模型 `sales/components/arrangement-editor-model.ts`。
- 后端接口：无新增接口；产品模板页继续走产品安排原有保存逻辑，正式团队安排页暂不接真实保存接口。
- 数据库表：无变更；正式团队大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加明细表仍待后续统一设计。
- 已实现功能：将产品团队安排页原有的大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加输入弹窗抽成 `ArrangementEditorModal`，产品团队安排页和正式团队安排页共用同一套字段、布局和样式。正式团队安排页点击各分区 `+` 后打开和产品模板一致的老系统字段弹窗；产品页传入 `editorMode="product"`，正式团队页传入 `editorMode="team"`，避免正式团队页继续维护一套简化弹窗。共享弹窗补回通用资源选择块，景区、餐厅等分类按 `resourceLabel` 动态渲染资源名称；自费弹窗标签统一为 `景区/项目名称`。
- 关键业务逻辑：本次只统一前端输入弹窗和事件传递，不把正式团队安排写入产品模板表，也不新增正式排团表。产品页仍由产品安排保存函数处理；正式团队页只保留本地录入状态和“正式排团数据表设计后接入保存”提示，后续设计正式排团表时再接入真实保存、应付、导游报账和财务审核联动。
- 验证结果：`cd frontend-preview && pnpm test:unit apps/web-antd/src/views/sales/product/product-form-utils.test.ts --reporter=dot` 通过，58 项通过；`cd frontend-preview && pnpm test:unit apps/web-antd/src/views/sales/team/operation-layout.test.ts --reporter=dot` 通过，4 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：正式排团资源明细表、单据状态、应付付款、导游报账和计调审核仍需结合财务统计口径单独设计，不能直接沿用产品模板安排表。

### 2026-06-29 销售管理 / 正式团队安排弹窗下拉数据修复

- 状态：已完成前端修复和真实接口核验。
- 前端页面：`sales/team/arrangement.vue`，共享弹窗 `sales/components/ArrangementEditorModal.vue`。
- 后端接口：无新增接口；复用 `GET /sales/team/{teamId}/operation`、`GET /purchase/supplier/all`、`GET /enterprise/expense-item/all`、`GET /purchase/resource/page`、`GET /purchase/relation/page`、`GET /enterprise/employee/all`、`GET /dispatch/vehicle-quote-rules/all`。
- 数据库表：无变更。
- 已实现功能：修复正式团队安排页点击大交通、住宿、用车等 `+` 后，弹窗中的 `订单信息` 只有“不关联订单”、`供应商` 下拉为空的问题。正式团队安排页现在打开弹窗时会按安排类型懒加载供应商、费用项目、资源、资源供应商关系、责任员工和用车座位规则；订单信息下拉从当前团队操作详情中的订单生成，过滤已拼出的来源留痕订单。共享弹窗新增 `orderOptions` 入参，并将各分类的订单下拉统一改为合并后的订单选项。2026-06-29 继续优化订单下拉展示，订单选项按 `订单号 / 客户单位 / 联系人 / 预订日期与预订人 / 人数` 展示，避免只显示预订日期和人数导致计调无法判断客户来源。
- 关键业务逻辑：正式团队安排页仍不保存资源明细到数据库，本次只补齐录入弹窗的数据来源；景区、自费、用餐、购物类安排先选择资源，再按采购关系过滤供应商，避免资源和供应商错配；大交通、住宿、用车、地接、其它、附加按供应商分类直接取供应商。
- 验证结果：先补 `operation-layout.test.ts` 失败用例确认缺少 `loadEditorOptions`、订单选项和供应商/资源/费用项目加载，再实现修复；订单下拉客户信息优化时，先补失败断言确认 `orderInfo` 未优先于 `bookingInfo` 展示，再实现前端 label 顺序调整；`cd frontend-preview && pnpm test:unit apps/web-antd/src/views/sales/team/operation-layout.test.ts --reporter=dot` 通过，5 项通过；`cd frontend-preview && pnpm test:unit apps/web-antd/src/views/sales/product/product-form-utils.test.ts --reporter=dot` 通过，58 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。真实接口核验：team 25 的 `/sales/team/25/operation` 返回 2 条订单，`orderInfo` 分别为 `上海江南会务`、`宁波海丝国际旅行社`；`/purchase/supplier/all?category=traffic` 返回 4 条大交通供应商；`/enterprise/expense-item/all?resourceType=traffic` 返回 9 条大交通费用项目。
- 待确认问题：正式团队安排明细保存、是否联动导游报账和计调审核，仍等正式排团表设计后处理。订单关联后的成本归属和多订单均摊规则已按旧系统真实页面实测确认，见下方 `计调操作 / 团队安排成本归属和多订单均摊调研`。

### 2026-06-29 计调操作 / 团队安排成本归属和多订单均摊调研

- 状态：已完成旧系统真实页面实测确认。
- 前端页面：旧系统 `GroupOverview.aspx?GroupId=260563` 团队安排弹窗；新系统后续对应正式团队安排资源明细弹窗。
- 后端接口：本次无新系统接口变更；仅记录旧系统行为。
- 数据库表：本次无表结构变更；后续正式团队安排表设计必须支持订单成本归属明细。
- 已实现功能：无代码实现，本次为业务规则记录。
- 关键业务逻辑：`全团/订单均摊` 选择 `=不关联订单=` 时，成本作为团队公共成本，只计入团总成本；选择某个订单时，保存为 1 条成本记录并整笔归属该订单。`多订单均摊成本` 只在新增时出现，订单信息变成多选，且价格信息只能保留 1 条；保存后旧系统拆成多条单订单成本记录，编辑时只能编辑拆分后的单条记录。`按订单均摊` 按订单数量平均拆分，实测 300 元选择 3 个订单后生成 100、100、100 三条。`按人数均摊` 按订单人数比例拆分，实测 210 元选择 14 人、4 人、3 人订单后生成 140、40、30 三条。
- 验证结果：使用旧系统 demo01 登录，在真实父页面 layer 弹窗中完成 `其它` 成本新增测试；确认单独打开弹窗会因缺少父页面刷新函数产生误导，因此最终以父页面真实弹窗提交结果为准。
- 待确认问题：后续新系统正式实现时，需要决定是否保留分摊来源批次或原始总额字段，用于财务追溯拆分来源；但多订单均摊能力本身已确认必须支持。

### 2026-06-30 计调操作 / 导游报账同步与现结规则调研

- 状态：已完成旧系统真实页面和导游小程序行为核对。
- 前端页面：旧系统 `GroupOverview.aspx`、`GuidePlan.aspx`、`ShopPlan.aspx`、`guide/GroupBills.aspx`；导游端微信小程序团队计划和购物报账页。
- 后端接口：本次无新系统接口变更；仅记录旧系统行为。
- 数据库表：本次无表结构变更；后续正式团队安排、导游报账、计调审核、财务审核、导游备用金和导游结算表设计必须引用该规则。
- 已实现功能：无代码实现，本次为业务规则记录。
- 关键业务逻辑：团队安排弹窗的 `无需导游报账，同步更新导游报账和计调审核数据` 表示导游不需要手工报账，系统自动生成导游报账和计调审核同步记录；该选项不是导游付款或导游垫付的含义。旧系统要求勾选该项时 `现结金额` 必须为 0，否则保存拦截。四种组合口径为：`不勾选 + 现结 > 0` 表示导游或现场人员需要报账，通常来自备用金垫付或个人垫付；`不勾选 + 挂账 > 0` 通常不是导游垫钱，但可能需要导游确认、上传凭证或补报消费、提成、返佣信息；`勾选 + 现结 = 0` 表示导游不参与报账，系统同步生成导游报账和计调审核流水；`勾选 + 现结 > 0` 是业务冲突，旧系统不允许保存。已进入导游报账或计调审核链路的安排明细，旧系统不再显示删除按钮；未进入报账/审核链路的原始安排明细才允许删除。导游手机端购物、自费等项目没有导游报账记录时显示 `报账` 按钮，已有导游报账或计调审核记录时只展示报账/审核结果。
- 验证结果：使用旧系统 demo01 登录核对 `GroupId=254145` 购物安排和团队报账页，确认 `车购测试`、`机场路游客服务中心` 已有导游报账和计调审核记录，手机端不再显示报账按钮；`金殿（玉器）` 无报账/审核子记录，手机端显示 `报账` 按钮且后台操作列保留删除按钮；`购物娃哈哈` 已有导游报账记录，后台不显示删除按钮。使用 `GuidePlan.aspx?GroupId=272414` 和导游管理页确认导游账号 `yaoyao` 对应 `导游1 / GuideID=6`，团队仅安排导游但无订单时导游端团队计划可能不显示。
- 待确认问题：新系统正式实现时，需要明确付款来源字段，例如公司付款、供应商挂账、导游备用金、导游个人垫付；不要只用一个 `no_guide_report` 字段推断导游结算金额。

### 2026-06-30 计调操作 / 正式团队安排成本底座与全分类接入

- 状态：已完成成本底座和正式团队安排全分类保存接入，已完成核心测试和真实接口逐分类自测。
- 前端页面：`sales/team/arrangement.vue`，共享弹窗 `sales/components/ArrangementEditorModal.vue`，共享模型 `sales/components/arrangement-editor-model.ts`。
- 后端接口：新增 `GET /sales/team/{teamId}/arrangements`、`POST /sales/team/{teamId}/arrangements/save`、`POST /sales/team/{teamId}/arrangements/{arrangementId}/delete`；产品排期继续使用 `POST /sales/team/schedule/batch-create`，但批量生成团队时会复制产品安排模板为正式团队安排快照。
- 数据库表：新增并扩展 `dispatch_team_arrangements`、`dispatch_team_arrangement_price_lines`、`dispatch_team_arrangement_order_allocations`、`dispatch_team_arrangement_flow_records`；本地 SQL 为 `db/016_dispatch_team_arrangement_schema.sql`，说明文档为 `db/数据库设计-正式团队安排成本说明.md`。2026-06-30 本次新增 `settlement_type`、`meal_type`、`fund_included`、`confirmed`、`confirmation_no`、`guide_id`、`guide_name`，用于住宿、用餐、确认号、导游快照等分类专用信息回显。
- 已实现功能：正式团队安排页加载真实团队安排成本列表，成本总览按正式安排记录汇总；大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加费用均支持新增、修改、删除并回写正式团队安排表；弹窗支持 `全团/订单均摊` 和 `多订单均摊成本` 输入方式，多订单模式下订单信息变为多选并显示 `按订单均摊 / 按人数均摊`；购物也已补订单归属入口；产品团期批量生成团队时，会把产品团队安排模板复制成正式团队安排参考快照，只带资源、供应商、项目、备注和参考单价，不带人数、数量、成本、现结和挂账；团队公共成本归属先置 0，计调人工确认数量和金额后才进入正式成本。
- 交互修正：2026-06-30 修正正式团队安排各分类 `当前状态` 展示逻辑。原页面只读取前端临时按钮状态，导致大交通、住宿、用车等已有真实安排明细时仍显示 `未安排`；现改为 `无需/完成` 本地按钮状态优先，否则根据该分类真实安排明细数量自动显示 `已安排` 或 `未安排`。该状态目前仍为页面展示辅助，不是后端流程状态字段。
- UI 打磨：2026-06-30 优化正式团队安排模块区视觉层级。各分类标题区统一为 `图标 + 模块名 + 状态标签 + 单据按钮 + 操作按钮`，状态下方展示 `安排条数、成本、现结、挂账` 摘要 chip；状态使用 `已安排/完成/无需/未安排` 四类轻量标签，和现有蓝灰后台风格保持一致。
- 关键业务逻辑：正式团队安排是团队执行成本，不写回产品模板表；产品排期复制模板只作为初始参考，默认 `allocation_scope=team`，不关联订单，不自动创建导游报账或计调审核流水，也不提前按产品模板人数计算团队成本。人工保存任意分类时，`=不关联订单=` 保存为团队公共成本，选择单个订单时整笔成本归属该订单；多订单均摊新增时按旧系统实测规则拆成多条单订单成本记录，并保存 `split_batch_no`、`original_amount` 和 `allocation_amount` 追溯拆分来源。`no_guide_report=true` 时必须 `cash_amount=0`，保存后同步生成导游报账和计调审核流水；已有流程流水的安排不允许直接删除。真实客户收款、供应商付款、银行现金账、发票、导游备用金和导游结算不进入本表族。
- 验证结果：已补失败测试覆盖非大交通结构化字段保存、购物订单归属、产品模板复制结构化字段；实现后 `cd backend && mvn -q -Dtest=DispatchTeamArrangementServiceTest,SalesTeamScheduleServiceTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm exec vitest run apps/web-antd/src/views/sales/team/operation-layout.test.ts --dom` 通过，6 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。`db/016_dispatch_team_arrangement_schema.sql` 已执行到 `.env.local` 指向的联调库；schema guardian 针对 `dispatch_team_arrangements`、`dispatch_team_arrangement_price_lines`、`dispatch_team_arrangement_order_allocations`、`dispatch_team_arrangement_flow_records` 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。已重启前后端，通过 `http://127.0.0.1:5666/api` 真实接口对 team 25 逐类保存大交通、住宿、用车、景区、用餐、其它、自费、购物、地接、附加：10 个分类全部保存并查回显成功；`其它` 多订单均摊拆成 2 条订单归属记录；购物单订单归属正常；`无需导游报账 + 现结` 被接口拦截；测试数据通过删除接口清理，列表中无 `Codex自测` 残留；`/sales/team/arrangement/25` 页面 HTTP 200。
- 待确认问题：购物返佣、应付付款、导游备用金、导游结算、发票和财务统计仍需独立财务表族支撑；本表族只作为团队安排成本和订单成本归属来源。

### 2026-06-30 销售/计调 / 地接安排一口价口径调整

- 状态：已完成前端交互和保存映射调整。
- 前端页面：`sales/product/team-arrangement.vue`、`sales/team/arrangement.vue`，共享弹窗 `sales/components/ArrangementEditorModal.vue`，共享模型 `sales/components/arrangement-editor-model.ts`。
- 后端接口：无新增接口；继续复用产品安排保存接口和正式团队安排保存接口。
- 数据库表：无表结构变更；继续复用 `sales_product_arrangement_items`、`sales_product_arrangement_price_lines`、`dispatch_team_arrangements`、`dispatch_team_arrangement_price_lines`。
- 已实现功能：地接安排按甲方反馈改为外包地接社整笔结算价录入，产品模板页和正式团队安排页的地接弹窗不再展示多行 `价格信息`，改为单个 `地接结算价` 金额输入；两边地接分区表格移除 `价格信息` 列，只保留成本合计、现结、挂账等金额列。
- 关键业务逻辑：地接结算价是整笔外包地接成本，不按人数、天数或价格项目自动乘算。保存时主金额写入 `totalAmount/costAmount`，同时生成一条隐藏兼容价格明细 `地接结算价 × 1`，用于兼容现有成本归属、多订单均摊、产品排期复制和后续统计链路。编辑旧数据时优先用 `costAmount/totalAmount` 回显地接结算价，旧多行价格明细只作为兜底汇总来源。供应商、日期、全团/订单均摊、多订单均摊、无需导游报账和现结互斥规则保持不变。
- 验证结果：先补失败测试覆盖地接结算价 helper、地接弹窗不再显示价格明细、产品和正式团队地接表格去掉价格信息列，再实现调整；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/components/arrangement-editor-model.test.ts` 通过，6 项通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/product/product-form-utils.test.ts` 通过，58 项通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts` 通过，6 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：地接确认单打印、供应商应付、付款和发票后续仍按财务表族单独接入；本次只调整安排录入和金额保存口径。

### 2026-06-30 计调操作 / 正式团队安排删除锁定规则修正

- 状态：已完成后端规则修正和测试。
- 前端页面：`sales/team/arrangement.vue`，继续读取后端 `canDelete/deleteDisabledReason` 控制删除按钮。
- 后端接口：调整 `GET /sales/team/{teamId}/arrangements`、`POST /sales/team/{teamId}/arrangements/{arrangementId}/delete` 的删除锁定判断。
- 数据库表：无表结构变更；规则基于 `dispatch_team_arrangement_flow_records.sync_source`。
- 已实现功能：录错团队安排时，如果下游流水仅为 `sync_source=no_guide_report` 的系统自动同步记录，允许删除安排，并同步软删除价格明细、订单归属和自动同步流水；如果已有人工导游报账、计调审核或财务审核流水，则仍禁止直接删除。
- 关键业务逻辑：`无需导游报账` 保存时自动生成的流水只是系统同步结果，不等于用户已经进入人工报账或审核动作，不能因此让刚录错的安排无法撤销。真正锁定删除的是人工或真实下游流程流水，包括 `sync_source` 为空、`manual`、`guide_submit`、`audit` 等非 `no_guide_report` 来源。
- 验证结果：补充 `deleteShouldAllowNoGuideReportAutoSyncedFlowRecords` 和 `listShouldAllowDeleteWhenOnlyNoGuideReportAutoSyncedFlowsExist` 测试；`cd backend && mvn -q -Dtest=DispatchTeamArrangementServiceTest test` 通过。
- 待确认问题：后续导游报账、计调审核、财务审核正式页面接入后，需要补“已人工提交后通过作废/冲销处理”的专用流程，不能通过物理删除处理财务链路。

### 2026-06-30 计调操作 / 正式团队安排用车测算参考价接入

- 状态：已完成前端接入和回归测试；已补齐正式排团页用车司机/车牌历史候选能力。
- 前端页面：`sales/team/arrangement.vue`，共享弹窗 `sales/components/ArrangementEditorModal.vue`。
- 后端接口：无新增接口；复用 `GET /sales/team/{teamId}/operation` 返回的团队行程路书数据，以及 `POST /dispatch/vehicle-quote-rules/calculate` 车辆报价规则测算接口。
- 数据库表：无表结构变更；本次只回填用车弹窗价格信息，暂不新增正式团队报价快照表。
- 已实现功能：正式团队安排页的用车弹窗不再显示占位提示；`同步路书公里` 会按用车开始/结束天从团队行程路书汇总公里和车程；`测算报价` 会按座位数规则调用车辆报价接口，并把结果显示到 `测算参考价`。正式排团页隐藏产品模板专用的 `编辑路书地图` 和 `应用到价格信息` 按钮，避免计调在正式排团弹窗里误以为可以直接维护产品路书或自动覆盖成本明细；产品团队安排页仍保留这两个辅助按钮。2026-06-30 补齐正式排团页司机信息和车牌号的历史候选加载与保存沉淀，和产品排团页保持一致：打开用车弹窗时加载 `driver_info`、`vehicle_plate` 候选，保存用车安排后记录本次手工输入。
- 交互优化：用车弹窗取消顶部单独的 `座位数` 分组，避免和报价测算区重复。座位数统一在 `报价测算 / 规则座位数` 下拉中选择，选择后同步更新价格信息第一行的费用项目，产品排团页和正式团队安排页共用该交互。
- 关键业务逻辑：测算价仍只是派车询价参考，正式成本以用户应用到价格信息并保存后的团队安排成本为准。当前正式团队安排表族已经承载成本、现结、挂账和订单成本归属；报价规则快照若后续需要审计追溯，再单独设计正式团队用车报价快照表。
- 验证结果：补充 `operation-layout.test.ts` 断言确认正式团队安排页接入 `calculateVehicleQuoteRule`、团队路书天数、测算结果和价格信息回填逻辑，并补充正式排团页司机/车牌历史候选回归断言；补充 `product-form-utils.test.ts` 断言确认用车弹窗不再渲染顶部座位数分组，`规则座位数` 改为可搜索下拉；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts src/views/sales/product/product-form-utils.test.ts` 通过，65 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：正式车调派车、车队询价记录、车辆报价快照审计和供应商应付付款仍需后续按车调/财务链路单独接入。

### 2026-06-30 计调操作 / 正式团队安排景区票务游客 Excel 下载

- 状态：已完成后端导出接口、前端弹窗入口和测试。
- 前端页面：`sales/team/arrangement.vue`，共享弹窗 `sales/components/ArrangementEditorModal.vue`；产品模板页仍只展示模板配置状态，不提供团队游客下载。
- 后端接口：新增 `GET /sales/team/{teamId}/arrangements/scenic-ticket-guests/export`。
- 数据库表：无表结构变更；读取 `purchase_relations`、`purchase_relation_ticket_templates`、`purchase_relation_ticket_template_fields`、`common_attachments`、`sales_orders`、`sales_order_guests`。
- 已实现功能：正式团队安排页打开景区弹窗后，按当前景区资源和供应商读取采购关系游客名单模板；模板状态显示在弹窗内；点击 `下载游客Excel` 后，后端以供应商上传的 Excel 模板为底稿，从 `data_start_row` 开始写入当前团队有效游客，并保留模板原表头和单元格样式。2026-06-30 补充修复模板预留数据行清理：写入游客前先快照模板数据首行样式和 `keep_original` 原值，再清空数据区预留行，避免游客数量少于模板预留行时导出文件底部残留空白行或默认“身份证”等内容。
- 关键业务逻辑：导出游客范围为当前团队有效游客，不按成本归属订单筛选；排除已取消订单和 `order_role=merge_source` 拼团来源留痕订单，保留普通订单和 `merge_child` 拼团目标子订单，避免重复购票。模板来源为 `景区资源 + 供应商` 的启用采购关系；未配置模板、未选择景区供应商或团队无有效游客时直接提示业务错误，不生成通用兜底 Excel。字段映射继续使用现有模板配置的 `tourist_field`、`sequence`、`constant`、`keep_original` 四种填充方式。
- 验证结果：先补 `DispatchScenicTicketGuestExportServiceTest` 红灯测试，覆盖按模板填充、排除无效订单口径、缺模板、无游客错误和清理模板预留数据行；实现后 `cd backend && mvn -q -Dtest=DispatchScenicTicketGuestExportServiceTest,DispatchTeamArrangementServiceTest test` 通过，`cd backend && mvn -q test` 通过。前端先补 `operation-layout.test.ts` 红灯断言，再实现 API、模板状态和下载按钮；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts src/views/sales/product/product-form-utils.test.ts` 通过，66 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：不同景区票务系统后续如要求订单号、团队号、出团日期等更多字段，需要扩展游客名单模板可映射系统字段；本次只复用当前已配置的游客字段。

### 2026-06-30 计调操作 / 座位数报价规则与车队费用项目对齐

- 状态：已完成后端同步、历史数据补齐和前端联动。
- 前端页面：`dispatch/vehicle-quote/index.vue`、`sales/product/team-arrangement.vue`、`sales/team/arrangement.vue`，共享弹窗 `sales/components/ArrangementEditorModal.vue`。
- 后端接口：调整 `POST /dispatch/vehicle-quote-rules/create`、`POST /dispatch/vehicle-quote-rules/update`；无新增接口。
- 数据库表：无表结构变更；新增数据补丁 `db/017_vehicle_quote_rule_expense_item_sync.sql`，用于将已启用座位数报价规则补齐为 `resource_projects(resource_type='vehicle')` 同名费用项目。
- 已实现功能：新增或修改启用的座位数报价规则时，系统自动确保车队费用项目存在同名启用项目，例如新增 `60座` 报价规则会补齐 `60座` 费用项目；已有同名费用项目不重复创建，停用同名项目会恢复启用。产品团队安排页和正式团队安排页选择用车座位数后，价格信息第一行自动切到同名项目；产品页“应用到价格信息”也改为写入当前座位数项目，不再固定写 `车费`。
- 关键业务逻辑：报价规则停用或删除时不删除费用项目，避免影响历史供应商价格、团队安排价格明细和统计口径。未被采购价格、产品排团价格明细、正式团队安排价格明细引用的早期描述型车型种子，例如 `5座轿车`、`7座商务车`、`19座中巴`、`33座大巴`、`45座大巴`，允许通过数据补丁软删除；`车费`、`司机补贴`、`停车费` 等通用成本项目继续保留。
- 验证结果：先补 `VehicleQuoteRuleServiceTest` 红灯测试覆盖新增规则创建费用项目、启用停用费用项目、停用报价规则不创建费用项目；先补 `operation-layout.test.ts` 红灯断言覆盖用车座位数变化同步价格项目；实现后 `cd backend && mvn -q -Dtest=VehicleQuoteRuleServiceTest,EnterpriseExpenseItemServiceTest test` 通过，`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts src/views/sales/product/product-form-utils.test.ts` 通过，64 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd backend && mvn -q test` 通过。已执行 `db/017_vehicle_quote_rule_expense_item_sync.sql` 到 `.env.local` 指向的联调库，新增 13 条车队费用项目，校验启用报价规则缺失同名启用费用项目数量为 0。
- 待确认问题：车队费用项目中的旧命名项目是否需要合并、停用或迁移历史价格，后续应单独按数据治理处理。

### 2026-07-01 计调操作 / 正式团队安排预算利润指标

- 状态：已完成前后端接入、文件导出和联调验证。
- 前端页面：`sales/team/arrangement.vue`、`sales/team/gross-profit.vue`。
- 后端接口：新增 `GET /sales/team/{teamId}/gross-profit/preview`、`GET /sales/team/{teamId}/gross-profit/export?format=docx|pdf`；继续复用 `GET /sales/team/{teamId}/operation` 和 `GET /sales/team/{teamId}/arrangements` 支撑团队安排页顶部指标。
- 数据库表：无表结构变更；读取 `sales_teams`、`sales_products`、`sales_orders`、`dispatch_team_arrangements`、`dispatch_team_guides`。
- 已实现功能：正式团队安排页顶部 `预算利润` 不再写死为 `0 元`，改为按旧系统页面和导出 Word 毛利表口径自动计算；点击 `预算利润` 或顶部工具条 `打印毛利表` 跳转到独立团队毛利表预览页 `/sales/team/gross-profit/{teamId}`。预览页按打印友好的旧系统表格口径展示收入、支出、自费、购物、毛利汇总和业务员明细，不展示 `传真号码`；顶部只保留 `Word文件`、`Pdf文件`、`在线打印` 三个文件/打印动作，其中 Word 导出为 `.docx`，PDF 导出为真实 PDF 文件，在线打印调用浏览器打印。
- 关键业务逻辑：团队级预算利润口径为 `订单应收合计 + 自费加点公司利润 + 购物公司利润 - 普通团队安排成本 - 导服费`。普通团队安排成本只包含大交通、住宿、用车、景区、用餐、其它、地接、附加等执行成本，不把自费和购物成本再重复扣一次；自费公司利润按 `销售额 - 成本 - 导游提成` 计算；购物公司利润按 `人头费 + 公司返佣 - 导游提成` 计算。备用金继续在成本概览展示，但不计入预算利润，因为备用金是导游预支和后续核销款，不是最终成本。旧系统 Word 毛利汇总未单列扣减操作费，新系统本指标和毛利表先按旧系统公式不扣操作费。订单收入排除已取消订单和 `merge_source` 拼团来源留痕订单，仅统计普通订单和 `merge_child` 拼团目标子订单，避免重复收入。
- 验证结果：后端先补 `SalesTeamGrossProfitServiceTest` 覆盖旧系统公式、无效订单排除、Word/PDF 导出；`cd backend && mvn -q -Dtest=SalesTeamGrossProfitServiceTest test` 通过，`cd backend && mvn -q test` 通过。前端补 `operation-layout.test.ts` 断言路由、API、预览页按钮、去除传真号码和预算利润跳转；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts` 通过，11 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。联调验证：`GET /api/sales/team/25/gross-profit/preview` 返回 200，汇总 `grossProfit=68042.31`；`docx` 导出返回 200 且文件识别为 Microsoft OOXML；`pdf` 导出返回 200 且文件识别为 PDF document；前端 `/sales/team/gross-profit/25` 返回 200。
- 待确认问题：`已付` 指标仍未接入正式付款/应付链路；后续财务付款、导游结算和供应商应付完成后再补。业务员毛利明细当前按业务员应收占团队总应收比例分摊团队毛利，后续如果甲方要求按订单成本归属和多订单均摊精确拆分，需要在订单毛利明细表设计时接入成本分摊结果。

### 2026-07-01 生产部署 / mtravel.apexplore.cn 团队安排与毛利表更新

- 状态：已完成线上部署并验证。
- 前端页面：生产构建产物部署到 `/opt/mtravel/frontend/dist`，包含正式团队安排、导游排班、团队毛利表预览页等本批更新。
- 后端接口：后端 jar 部署到 `/opt/mtravel/backend/app.jar`，通过 `mtravel-api.service` 运行。
- 数据库表：本次部署未额外执行 DDL；线上表结构沿用已同步的正式团队安排、导游排班和座位数报价规则相关 SQL。
- 已实现功能：将本地 `main` 最新提交 `48fbba3 开发团队安排与毛利表功能` 构建后上传服务器；上传前已备份旧产物到 `/opt/mtravel/backups/deploy-20260701105042`；替换后重启 `mtravel-api.service` 并重载 Nginx。
- 验证结果：本地构建 `cd backend && mvn -q -DskipTests package` 通过，`cd frontend-preview && pnpm -F @vben/web-antd run build` 通过。线上 `http://mtravel.apexplore.cn/actuator/health` 返回 `{"status":"UP"}`；线上登录接口 `POST /api/auth/login` 返回 `code=0`；线上 `GET /api/sales/team/25/gross-profit/preview` 返回 `code=0` 且 `grossProfit=68042.31`；线上首页和 `/sales/team/gross-profit/25` 路由均返回 200；服务器 `systemctl is-active mtravel-api` 为 `active`，Nginx 配置检查通过。
- 待确认问题：当前站点仍为 HTTP；如需 HTTPS，后续继续在宝塔申请并绑定证书。

### 2026-07-01 销售管理 / 拼团目标团队类型限制

- 状态：已完成前后端修正和回归测试。
- 前端页面：`sales/team/operation.vue`。
- 后端接口：`POST /sales/team/{teamId}/operation/merge`。
- 数据库表：无表结构变更。
- 已实现功能：拼团弹窗的目标团队列表只查询未出团的 `散拼` 团队，转团目标团队列表保持原有口径不变。
- 关键业务逻辑：按老系统 `MergeGroup.aspx` 实测口径，拼团目标团队只能是散拼；整团、散团、单项不能作为拼团目标。前端列表过滤只是交互限制，后端 `mergeOrders` 同步校验目标团队 `team_type=sanpin`，避免绕过页面直接调用接口把订单拼入整团等团队，导致后续收入、成本、毛利归属口径混乱。
- 验证结果：补充前端静态回归断言和后端服务测试；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts` 通过，12 项通过；`cd backend && mvn -q -Dtest=SalesOrderTransferServiceTest test` 通过。
- 待确认问题：`拼团订单` 独立菜单后续开发时也必须沿用“多个订单可拼入同一个散拼团，一个订单不能拼入多个团，目标团只能散拼”的规则。

### 2026-07-01 销售管理 / 订单管理真实列表

- 状态：已完成列表先行版本、订单标记和真实接口联调。
- 前端页面：`sales/order/index.vue`，菜单 `销售管理 / 订单管理` 已显示。
- 后端接口：新增 `GET /sales/booking/orders/page` 查询全局订单管理分页列表；新增 `POST /sales/booking/orders/{id}/tagging` 更新订单管理页标记状态。
- 数据库表：`sales_orders` 新增 `tagging boolean NOT NULL DEFAULT false`，并新增索引 `idx_sales_orders_tenant_deleted_tagging`；本地 SQL 为 `db/013_sales_booking_order_schema.sql`，说明文档为 `db/数据库设计-销售收客订单说明.md`。
- 已实现功能：订单管理页不再使用 mock 数据，按老系统订单管理页口径提供团号、客户团号、预订单位/业务员、出团日期、产品名称、航班号/接送备注、订单金额、下单人、游客名称/证件号、团队类型、订单状态、订单排序、标记状态、订单文件筛选；表格展示发团日期、订单信息、酒店信息、接送备注、客源地、客人、人数、价格详情、应收、已收、余额、费用说明、订单备注、日期/预定人、状态和操作；点击查看/修改进入 `/sales/team/booking/{teamId}/{orderId}`；AI 辅助录入抽屉保留为次要入口。
- 关键业务逻辑：订单管理列表只返回当前页摘要，团队、产品、价格明细、首位游客和订单文件按当前页批量加载，避免 N+1 查询。`tagging` 只用于订单管理页筛选重点订单，不代表订单确认、收款或财务审核状态，也不改变团队归属、应收金额、游客名单或订单状态。订单文件首版按 `common_attachments.business_module='销售收客'` 且 `business_type='订单文件'` 判断。
- 验证结果：`cd backend && mvn -q -Dtest=SalesBookingOrderServiceTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/order/ai-import-layout.test.ts src/router/routes/__tests__/visible-menu.test.ts` 通过，4 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；schema guardian 针对 `sales_orders`、`sales_order_charge_lines`、`sales_order_guests`、`sales_order_transfer_logs` 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`；真实接口 `GET /api/sales/booking/orders/page?page=1&pageSize=5` 返回 200，`total=8`，耗时约 `0.414s`；Playwright 登录后打开 `/sales/order`，页面标题和 `导出名单` 可见，表格显示真实订单，未出现旧 mock 订单号，控制台无错误。
- 待确认问题：全局 `导出报表`、多订单名单压缩导出、全局 `拼团操作` 弹窗、订单文件管理页仍未正式接入；当前按钮按老系统入口保留校验和提示，后续开发时必须继续复用团队操作页已确认的拼团规则。

### 2026-07-01 销售管理 / 团队列表安排状态标记

- 状态：已完成分类状态持久化、列表状态修正和联调验证。
- 前端页面：`sales/team/index.vue` 继续复用 `TeamArrangeStatusIcon.vue`；`sales/team/arrangement.vue` 的各分类 `无需`、`完成` 按钮已接入后端保存和回显。
- 后端接口：增强 `GET /sales/team/page` 返回 `guidePlan`、`trafficPlan`、`hotelPlan`、`vehiclePlan`、`scenicPlan`、`mealPlan`、`otherPlan`、`optionalPlan`、`shoppingPlan`、`groundAgentPlan`；新增 `GET /sales/team/{teamId}/arrangement-section-statuses` 和 `POST /sales/team/{teamId}/arrangement-section-statuses/{arrangementType}`。
- 数据库表：新增 `dispatch_team_arrangement_section_statuses`，本地 SQL 为 `db/016_dispatch_team_arrangement_schema.sql`，说明文档为 `db/数据库设计-正式团队安排成本说明.md`。已同步联调库，并将 team 25 住宿历史测试数据的 `confirmed` 全部重置为 `false`。
- 已实现功能：团队管理列表中的 `导 / 交 / 住 / 车 / 景 / 餐 / 其 / 自 / 购 / 地` 按当前页团队 ID 批量聚合安排状态；团队安排页点击分类 `无需` 或 `完成` 后立即落库，刷新页面仍能回显。新增住宿安排不再默认 `confirmed=true`，避免没有点完成也显示绿色。
- 关键业务逻辑：`dispatch_team_arrangement_section_statuses.status` 只表示计调对该分类的流程处理状态，`pending` 待完成、`none` 无需、`done` 已完成；团队列表中 `done` 显示绿色 `confirmed`，`none` 显示无安排，生效安排但没有分类完成状态时显示黄色 `pending`。`dispatch_team_arrangements.confirmed` 仍只表示单条资源供应商确认，例如酒店确认号，不再作为团队列表分类完成依据。导游安排继续按 `dispatch_team_guides.status=active` 聚合，正式导游为 `confirmed`，待定导游为 `pending`。
- 验证结果：先补后端红灯测试覆盖分类状态保存和 Spring 运行时构造器注入，再补前端静态测试覆盖分类状态 API、页面加载保存和住宿默认不确认；实现后 `cd backend && mvn -q -Dtest=DispatchTeamArrangementServiceTest#saveSectionStatusShouldPersistAndListByTeam test` 通过，`cd backend && mvn -q -Dtest=SalesTeamScheduleServiceTest#globalTeamPageShouldReturnTeamsWithProductInfoWithoutNPlusOne test` 通过，`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过。schema guardian 针对 `dispatch_team_arrangements`、`dispatch_team_arrangement_price_lines`、`dispatch_team_arrangement_order_allocations`、`dispatch_team_arrangement_flow_records`、`dispatch_team_arrangement_section_statuses` 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。真实接口验证 `POST /sales/team/25/arrangement-section-statuses/hotel` 可保存 `none` 和 `done`，最终状态为 `done`；`GET /sales/team/page?keyword=CS-SP-BK-260815A` 返回 team 25 `hotelPlan=confirmed`，同时数据库中 team 25 住宿安排 `confirmed_count=0`，确认列表已不再依赖资源确认字段。
- 待确认问题：旧系统是否有“需处理/异常”类状态来源尚未进入本次逻辑；后续如果计调审核、财务审核或供应商确认失败需要在列表预警，可再把 `warning` 接到对应流程状态。

### 2026-07-01 销售管理 / 团队列表客户导游摘要和安排图标跳转

- 状态：已完成前后端接入和定向验证。
- 前端页面：`sales/team/index.vue`、`sales/team/arrangement.vue`。
- 后端接口：增强 `GET /sales/team/page`，新增请求参数 `customerKeyword`，返回 `customerSummary` 和 `guideSummary`。
- 数据库表：无表结构变更；读取 `sales_orders` 和 `dispatch_team_guides`。
- 已实现功能：团队管理列表 `客户` 列展示该团所有有效订单客户单位，去重后用 `、` 拼接；`导游信息` 列展示该团所有生效导游，格式为 `导游名[Tel:手机号]`。客户单位搜索框不再只停留在前端输入，已传递到后端按客户单位和业务员筛选。列表上的 `导 / 交 / 住 / 车 / 景 / 餐 / 其 / 自 / 购 / 地` 状态图标可点击，直接进入正式团队安排页对应位置。
- 关键业务逻辑：客户摘要只统计未取消订单，并只纳入普通订单、`merge_child` 拼团目标订单或空角色订单；排除 `merge_source` 拼团来源留痕订单，避免原团收入和目标团收入重复展示。导游摘要只统计 `dispatch_team_guides.status=active` 且未删除记录。图标锚点映射为导游 `guide-arrangement`，大交通到地接依次为 `part1` 到 `part9`。
- 验证结果：`cd backend && mvn -q -Dtest=SalesTeamScheduleServiceTest#globalTeamPageShouldReturnTeamsWithProductInfoWithoutNPlusOne,SalesTeamControllerMappingTest#shouldExposeGlobalTeamPageAndDelegateFilters test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，14 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：团队列表 `导游筛选` 输入框当前仍未接后端查询，后续如需按导游筛团队，应在 `dispatch_team_guides` 上按当前页筛选口径补充后端 exists 查询，并注意大数据量索引。

### 2026-07-01 销售管理 / 团队列表客户导游摘要列 UI 修正

- 状态：已完成前端样式修正和真实页面验证。
- 前端页面：`sales/team/index.vue`。
- 后端接口：无变更，继续使用 `GET /sales/team/page` 返回的 `customerSummary` 和 `guideSummary`。
- 数据库表：无表结构变更。
- 已实现功能：客户列改为专用固定宽度列，客户名称最多显示两行并在单元格内裁剪，鼠标悬浮继续显示完整内容；导游信息列改为一行省略号，避免挤压右侧安排状态图标。修复长客户名越过表格边框、覆盖后续列的问题。
- 验证结果：先补前端红灯测试固定客户列、导游列裁剪规则；实现后 `cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，15 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。使用本机 Chrome headless 打开 `http://127.0.0.1:5666/sales/team` 实测，客户单元格 `line-clamp=2` 且文本宽度小于单元格宽度，导游单元格为 `nowrap + ellipsis`。
- 待确认问题：如果后续客户希望客户列显示更多完整名称，可再把客户列改为三行摘要或增加“更多”弹出层；当前按高密度团队管理表格口径先保持两行。

### 2026-07-01 销售收客 / AI 辅助录入文件上传限制修正

- 状态：已完成本地修复、接口重启和真实上传验证。
- 前端页面：`sales/team/booking/form.vue`。
- 后端接口：`POST /common/attachment/upload`，销售收客 AI 辅助录入上传确认单沿用该公共附件接口。
- 数据库表：无表结构变更；验证产生的临时附件记录已软删除。
- 已实现功能：将 Spring multipart 上传上限从默认 10MB 调整为 50MB，并支持通过 `MULTIPART_MAX_FILE_SIZE`、`MULTIPART_MAX_REQUEST_SIZE` 环境变量覆盖；前端在上传前拦截超过 50MB 的文件并提示 `上传文件不能超过50MB`；后端对超限上传返回 413 和同一业务提示，不再表现为服务器内部错误。
- 关键业务逻辑：本次报错根因是上传文件约 10.5MB，超过 Spring/Tomcat 默认 10MB 请求体限制，请求在进入 AI 识别业务前已经被拒绝；因此本次只修上传链路和错误提示，不改变 AI Key、识别模型、确认单解析逻辑。
- 验证结果：`cd backend && mvn -q -Dtest=GlobalExceptionHandlerTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。重启本地后端后，11MB 文件上传 `POST /common/attachment/upload` 返回 200 且 `code=0`；51MB 文件返回 HTTP 413，响应提示 `上传文件不能超过50MB`。
- 待确认问题：如果后续出现 50MB 以内文件上传成功但 AI 识别失败，需要另行排查模型文档解析、文件格式或大模型服务自身限制。

### 2026-07-02 销售收客 / AI 辅助录入常见文件格式兼容

- 状态：已完成本地修复、接口重启和真实 `.doc` 确认单验证。
- 前端页面：`sales/booking/form.vue`。
- 后端接口：`POST /common/attachment/upload`、`POST /sales/booking/ai-import/recognize`。
- 数据库表：无表结构变更；验证产生的临时附件记录已软删除。
- 已实现功能：AI 辅助录入支持常见确认单格式 `doc/docx/xls/xlsx/csv/txt/pdf/jpg/jpeg/png/webp/bmp`。后端新增 `.doc` 老 Word 二进制文件提取，PDF 优先本地提取文本、无文本时再走百炼视觉/OCR，图片继续走视觉/OCR；前端上传前校验文件扩展名并设置文件选择器 `accept`，不支持格式提示 `暂不支持该文件类型，请上传 Word、Excel、PDF、图片或粘贴文本`。
- 关键业务逻辑：识别接口仍以后端附件元数据 `fileExt` 为准，不信任前端传入的 `sourceType`；损坏、加密或无法读取的文件统一提示 `确认单文件读取失败，请检查文件是否加密或损坏`。本次同时补充 `姓名[身份证/电话]、姓名[身份证]` 格式游客名单解析，以及 `成人：1170 * 2 = 2340`、`合计金额：4858.00元` 这类确认单费用文本解析。
- 验证结果：`cd backend && mvn -q -Dtest=BookingAiImportServiceTest,BookingAiImportGoldenSampleTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/booking/booking-form-layout.test.ts --dom` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。使用真实文件 `/Users/woniu/Desktop/7.5臻享迪士尼5天杨文杰2人团队确认单06301714615.doc` 上传并识别返回 `code=0`，识别到游客 2 人：杨文杰、李圣瑶，成人价 `1170`，合计金额 `4858.00`。
- 待确认问题：WPS 专有格式 `.wps/.et` 暂未纳入本轮强保证；扫描 PDF 和图片识别依赖百炼视觉/OCR 配置，配置不可用时会返回明确业务提示。

### 2026-07-02 计调操作 / 团队安排敏感金额后端汇总

- 状态：已完成后端权威汇总、前端接入和本地验证。
- 前端页面：`sales/team/arrangement.vue`。
- 后端接口：新增 `GET /sales/team/{teamId}/arrangements/summary`。
- 数据库表：无表结构变更；读取 `sales_orders`、`dispatch_team_arrangements`、`dispatch_team_guides`。
- 已实现功能：正式团队安排页顶部 `应收/已收/余额`、`预算利润`、成本总览现结/挂账、各安排分类小计改为读取后端权威汇总接口；前端只保留安排弹窗内未保存草稿的临时金额预览。新增 `AGENTS.md` 规则，明确价格、应收、成本、利润、授信等敏感金额正式展示和保存必须以后端计算或校验为准。
- 关键业务逻辑：团队安排汇总接口排除已取消订单和 `merge_source` 拼团来源留痕订单，仅统计普通订单和 `merge_child` 拼团目标订单。预算利润口径继续按旧系统公式：`订单应收 + 自费公司利润 + 购物公司利润 - 普通团队成本 - 导服费`；普通成本不重复扣自费、购物成本；操作费和备用金只在总览展示，不计入预算利润。
- 验证结果：先补后端红灯测试 `DispatchTeamArrangementServiceTest#summaryShouldCalculateSensitiveMoneyOnBackend`，确认缺少 DTO 和 `summary()` 时失败；实现后该测试通过。先补前端红灯测试 `operation-layout.test.ts`，确认缺少 `getTeamArrangementSummary` 时失败；实现后 `cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts` 通过，15 项通过。最终验证 `cd backend && mvn -q -Dtest=DispatchTeamArrangementServiceTest test` 通过，`cd backend && mvn -q test` 通过，`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：成本总览中的 `自费收入` 独立列当前仍沿用原页面展示口径，不在本次接口中新增销售额汇总字段；如甲方后续要求该列展示自费销售额，需要补充后端字段并明确是否等同于自费 `saleAmount` 合计。

### 2026-07-02 销售团期 / 产品排期生成团队安排参考快照修正

- 状态：已完成代码修正、历史数据补丁和本地验证。
- 前端页面：`sales/team/arrangement.vue`。
- 后端接口：无新增接口；修正 `POST /sales/team/schedule/batch-create` 创建团队时复制产品团队安排模板的规则。
- 数据库表：无新增表；新增数据补丁 `db/019_product_team_arrangement_reference_cleanup.sql`，用于清理历史自动带入的模板人数、数量和金额。
- 已实现功能：产品排期批量生成正式团队时，只把产品团队安排复制为资源和参考单价快照，不再提前带入产品模板的人数、数量、成本、现结、挂账、收入、返佣、提成、人头费和消费金额。正式团队安排页对 `quantity=0` 的模板快照显示为 `参考单价`，人数/进店人数显示 `--`，避免用户误认为已经产生正式成本。
- 关键业务逻辑：产品团队安排仍是模板，不是团队执行成本。复制后的正式团队安排默认 `allocation_scope=team`，保留供应商、资源、费用项目、备注和参考单价，主金额和价格明细金额全部为 0，`confirmed=false`，不自动创建导游报账、计调审核或财务审核流水。团队真实人数、住宿间数、门票人数、用餐人数和用车天数必须由计调在正式团队安排页确认后保存，保存后才进入成本、应付和预算利润口径。
- 数据修复结果：执行前已备份 `output/db-backups/team-arrangement-template-reference-cleanup-20260702134917.sql`；补丁执行结果 `UPDATE 9`，已将历史安全候选模板快照的数量、人数和金额清零，并保留/推导参考单价。
- 验证结果：`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts` 通过，16 项通过。数据验证 team 32 中住宿、用餐、景区、用车等历史模板快照已变为 `quantity=0`、`amount=0`，保留参考单价；Playwright 登录后打开 `/sales/team/arrangement/32#hotel`，页面显示 `参考单价` 且成本/现结/挂账均为 `¥0.00`，截图为 `output/playwright/team-32-arrangement-reference-snapshot-20260702.png`。
- 待确认问题：后续如需要“人工创建团队时选择产品模板并复制安排”，必须复用同一参考快照规则，不能再把产品模板人数或金额直接变成正式团队成本。

### 2026-07-06 计调操作 / 团队安排选择导游弹窗优化

- 状态：已完成前后端接入和本地验证。
- 前端页面：`sales/team/arrangement.vue`。
- 后端接口：新增 `GET /dispatch/guide-schedule/availability`。
- 数据库表：无表结构变更；读取 `enterprise_guides`、`dispatch_team_guides`、`dispatch_guide_leave_records`。
- 已实现功能：团队安排页“添加导游”弹窗改为表格选择导游，显示全部启用导游，列出导游姓名、手机号、性别、状态和不能出团原因；默认只看可用导游，也可切换全部导游；去掉“就近安排”；不能出团导游禁用选择，避免手机号挤压导游姓名。
- 关键业务逻辑：导游不可出团原因来自同时间段已生效团队导游安排，或已审批通过的请假记录；保存团队导游安排时后端仍继续做最终冲突校验，防止并发情况下前端列表状态过期。
- 验证结果：`cd frontend-preview && pnpm test:unit apps/web-antd/src/views/sales/team/operation-layout.test.ts` 通过，17 项通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd backend && mvn -q -Dtest=DispatchGuideScheduleServiceTest test` 通过；`cd backend && mvn -q test` 通过。
- 待确认问题：当前“全部导游”只展示启用且未删除导游；停用导游不参与排团安排。

### 2026-07-06 财务管理 / 导游备用金申请与总经理审批

- 状态：已完成备用金计算、申请、总经理审批、付款登记首版接入。
- 前端页面：`finance/guide-advance/index.vue` 改为真实备用金列表、计算预览、申请详情和付款登记；`system/risk-approval/index.vue` 增加“导游备用金审批”类型，保留原客户风控审批。
- 后端接口：新增 `GET /finance/guide-imprests/preview`、`POST /finance/guide-imprests/submit`、`GET /finance/guide-imprests/page`、`GET /finance/guide-imprests/detail`、`POST /finance/guide-imprests/approve`、`POST /finance/guide-imprests/reject`、`POST /finance/guide-imprests/payment`。
- 数据库表：新增 `finance_guide_imprests`、`finance_guide_imprest_calc_lines`、`finance_guide_imprest_payments`；本地 SQL 为 `db/020_finance_guide_imprest_schema.sql`，说明文档为 `db/数据库设计-导游备用金说明.md`；新增配置键 `system_configs.guide_imprest_company_markup_rate`，默认 `70`。
- 已实现功能：计调可按团队和导游预览备用金计算明细并提交申请；同一团队同一导游允许多次申请；总经理审批页可同意或拒绝备用金申请；财务可在审批通过后登记付款，累计付款达到审批金额后状态变为 `paid`。
- 关键业务逻辑：备用金公式由后端计算并固化快照：`建议备用金 = 现付总成本 - Σ[(自费售价 - 自费成本 - 导游成本) × 公司规定加点率] × 团队实收人数`。导游成本支持固定金额和按自费毛利百分比；公式结果小于 0 时，建议备用金为 0，负数绝对值记为导游应上交金额。`guide_turn_in_amount` 只表示本团导游应上交公司金额，不是备用金，不是团队成本，也不写入下次团队备用金公式。备用金独立于团队成本、订单收入和银行现金账，后续导游结算再做核销关联。
- 旧系统参考：用户指定的 `http://www.mtravel.cn/Finance/GuideImprest.aspx` 在未登录和登录 `demo01` 后均直接返回 ASP.NET/SQL Server 错误 `“=”附近有语法错误。`，栈显示在 `ReceptionSys.UI.Finance.GuideImprest..ctor()` 取公司信息时失败；因此本次只参考旧系统已确认的字段口径（团号、导游、计调、合计现付、备用金、已付、余额、申领详情），不复制该页面当前报错行为。
- 验证结果：先补后端红灯测试 `FinanceGuideImprestServiceTest`，确认模块类缺失时失败；实现后 `cd backend && mvn -q -Dtest=FinanceGuideImprestServiceTest test` 通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/router/routes/__tests__/visible-menu.test.ts` 通过；已应用 `db/020_finance_guide_imprest_schema.sql` 到当前联调库，schema guardian 针对三张新增表校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`；真实接口 `GET /api/finance/guide-imprests/page?page=1&pageSize=5` 返回 `code=0,total=0`，`GET /api/finance/guide-imprests/preview?teamId=25&guideId=4` 返回团号 `CS-SP-BK-260815A`、导游 `陈志强`、实收 `27`、现付 `470`、建议备用金 `470`；Playwright 登录后打开 `/finance/guide-advance` 和 `/system/risk-approval`，页面可正常渲染，总经理审批可切换到“导游备用金审批”。
- 待确认问题：团队安排页导游行入口已补充，备用金申请可自动带入团队和导游；当前阶段只保存备用金申请、付款记录和导游应上交金额快照，尚未生成导游欠款余额、历史未结算提示、下次备用金抵扣和导游结算核销流水。后续开发导游结算或导游往来账时，历史应上交款可由财务在付款环节选择是否抵扣，计调只负责查看或备注建议；抵扣必须生成核销流水，不能系统自动扣，也不能无流水直接少付款。导游报账、计调审核、财务审核和导游结算核销仍是后续独立模块。

### 2026-07-06 计调操作 / 团队安排导游备用金计算入口

- 状态：已完成前端接入。
- 前端页面：`sales/team/arrangement.vue` 导游分区。
- 后端接口：复用 `GET /finance/guide-imprests/preview`、`POST /finance/guide-imprests/submit`、`POST /sales/team/{teamId}/guides/{recordId}/field`。
- 数据库表：无新增表，继续使用 `finance_guide_imprests`、`finance_guide_imprest_calc_lines`、`dispatch_team_guides`。
- 已实现功能：团队安排页每个导游行在 `备用金` 字段旁新增 `计算` 按钮；点击后打开 `计算导游备用金` 弹窗，展示后端返回的现付总成本、自费抵扣、实收人数、建议备用金、导游应上交和明细行；支持 `使用此金额` 写回当前导游安排备用金字段，也支持直接 `提交备用金申请` 进入总经理审批流程。
- 关键业务逻辑：备用金金额仍以后端计算和后端申请接口为准，前端只展示预览结果、保存用户确认金额和提交申请，不在页面本地重新计算公式。弹窗展示后端计算结果的解释公式：现付成本行直接计入备用金需求；自费抵扣行按 `(售价 - 成本 - 导游提成) × 公司加点率 × 实收人数 = 抵扣金额` 展示；总公式按 `建议备用金 = max(现付总成本 - 自费抵扣, 0)` 展示，若结果为负数则展示导游应上交金额。
- 验证结果：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，19 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；本地后端 `/actuator/health` 返回 UP，前端 `/sales/team/arrangement/25#guide-arrangement` 返回 200；Playwright 登录后打开该页面，确认 `备用金` 旁显示 `计算` 按钮，点击后弹窗显示现付总成本、自费抵扣、建议备用金、导游应上交和每行计算公式。例如 `自费项目票` 显示 `(¥200.00 - ¥150.00 - ¥10.00) × 70% × 27人 = ¥756.00`，总公式显示 `现付总成本 ¥470.00 - 自费抵扣 ¥756.00 = -¥286.00，所以建议备用金为 ¥0.00，导游应上交 ¥286.00`。
- 待确认问题：后续导游报账、计调审核、财务审核和导游结算核销完成后，需要确认已审批或已付款备用金是否锁定团队安排页的手填备用金字段。

### 2026-07-06 计调操作 / 团队安排自费弹窗字段对齐

- 状态：已完成前端修正和本地验证。
- 前端页面：`sales/team/arrangement.vue`、`sales/product/team-arrangement.vue` 共用 `sales/components/ArrangementEditorModal.vue`。
- 后端接口：无新增接口。
- 数据库表：无表结构变更。
- 已实现功能：自费信息弹窗按老系统字段顺序调整为 `景区名称 / 游玩日期 / 添加景区`、`供应商 / 添加供应商`、`价格信息`、`合计人数/收入/成本/现结/挂账/提成`、`费用合计`、`订单信息`、`备注信息`；自费弹窗不再显示顶部 `全团/订单均摊 / 多订单均摊成本` 页签。
- 关键业务逻辑：自费价格信息中的 `销售价`、`成本价`、`导游提成` 按单价录入，费用合计中的 `收入`、`成本`、`挂账`、`提成` 按 `人数 × 单价` 自动汇总；本次不改变成本归属、订单均摊、无需导游报账或后端金额权威汇总规则。
- 验证结果：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，19 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；本地前端 `/sales/team/arrangement/25#part7` 返回 200，后端 `/actuator/health` 返回 UP；Playwright 登录后点击自费区 `新增安排`，确认弹窗标题为 `添加/修改自费信息`，字段顺序和老系统对齐，且弹窗内无元素超过右边界。实测录入销售价 `350`、成本价 `280`、导游提成 `20`、人数 `8` 后，自动得到收入 `2800`、成本 `2240`、挂账 `2240`、提成 `160`，未提交测试数据。
- 待确认问题：如果甲方要求自费列表列名也从 `景区/项目名称` 改为 `景区名称`，需要再单独调整列表字段文案。

### 2026-07-06 系统设置 / 总经理审批导游备用金详情增强

- 状态：已完成前端修正和本地验证。
- 前端页面：`system/risk-approval/index.vue`。
- 后端接口：无新增接口，继续使用 `GET /finance/guide-imprests/detail` 返回的 `calcLines` 计算快照。
- 数据库表：无表结构变更。
- 已实现功能：总经理审批页切换到“导游备用金审批”后，点击“查看”打开的详情弹窗增加“计算说明”和“费用明细”。费用明细展示现付成本、自费抵扣、项目名称、售价、成本、导游提成、公司加点率、实收人数、本行金额和计算公式，方便总经理审批前核对申请金额来源。
- 关键业务逻辑：审批页只展示后端已固化的备用金计算快照，不在前端重新计算正式金额；现付成本行显示“现付成本直接计入备用金需求”，自费抵扣行按 `（售价 - 成本 - 导游提成）× 公司加点率 × 实收人数 = 抵扣金额` 展示。
- 验证结果：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/system/risk-approval-layout.test.ts --dom` 通过，2 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：当前计算快照不包含供应商、业务日期和来源安排单号；如甲方要求审批明细展示这些字段，需要后续扩展备用金计算快照字段和后端 DTO。

### 2026-07-06 财务管理 / 导游备用金授权余额与作废控制

- 状态：已完成本地实现和专项验证。
- 前端页面：`finance/guide-advance/index.vue`、`system/risk-approval/index.vue`。
- 后端接口：新增 `POST /finance/guide-imprests/cancel`；补充 `GET /finance/guide-imprests/preview`、`GET /finance/guide-imprests/detail` 返回已申请/已批金额、建议余额参考、当前重新计算结果和计算是否变化。
- 数据库表：继续使用 `finance_guide_imprests`、`finance_guide_imprest_calc_lines`、`finance_guide_imprest_payments`；`finance_guide_imprests` 新增作废字段 `cancelled_by`、`cancelled_at`、`cancel_reason`，本地 SQL 已同步到 `db/020_finance_guide_imprest_schema.sql`，说明文档已同步到 `db/数据库设计-导游备用金说明.md`。
- 已实现功能：备用金申请预览展示已申请/已批金额和建议余额参考；建议余额参考只做风险提示，不作为申请硬上限；备用金列表和详情支持作废未付款申请，作废必须填写原因；付款弹窗在团队安排变化时禁止确认付款；总经理审批详情在团队安排变化时提示重新计算并禁用审批通过。
- 关键业务逻辑：只有团队安排中的现付金额 `cash_amount` 进入导游备用金建议计算，挂账金额 `credit_amount` 不占用导游备用金；同一团队同一导游允许多次申请，每次申请都是一张独立授权单并单独审批；系统建议金额不能覆盖现场临时现付、特殊项目和应急备用金需求，因此计调可手填超过系统建议的申请金额，但申请后累计金额超过系统建议金额时必须填写应急或特殊项目说明；审批和付款前后端都会重新按当前团队安排、实收人数和自费抵扣计算，如果和申请快照不一致，审批通过和付款均被拦截，必须作废旧申请后重新提交；已付款申请不能作废，后续需通过导游结算、退款或冲销流水处理。
- 验证结果：`cd backend && mvn -q -Dtest=FinanceGuideImprestServiceTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/system/risk-approval-layout.test.ts src/views/finance/guide-advance-layout.test.ts --dom` 通过，4 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：当前执行环境没有提供数据库连接环境变量，本次只同步了本地 SQL 和说明文档，尚未对当前运行库执行新增作废字段 DDL 和 schema guardian 一致性校验。运行库需要补齐 `finance_guide_imprests.cancelled_by`、`cancelled_at`、`cancel_reason` 后，作废接口才能正常写入。

### 2026-07-07 计调操作 / 团队安排导游备用金按次申请优化

- 状态：已完成后端、前端和文档同步。
- 前端页面：`sales/team/arrangement.vue` 导游分区。
- 后端接口：补充 `GET /finance/guide-imprests/preview` 支持按次传入 `companyMarkupRate`；补充 `POST /finance/guide-imprests/submit` 保存计调手填的 `requestedAmount` 和本次 `companyMarkupRate`；`GET /sales/team/{teamId}/guides` 返回导游备用金审批汇总。
- 数据库表：无新增字段，继续使用 `finance_guide_imprests.company_markup_rate`、`requested_amount`、`approved_amount`、`paid_amount`、`balance_amount`；运行库已补齐此前 SQL 已定义但未应用的 `cancelled_by`、`cancelled_at`、`cancel_reason` 三个作废字段；已同步 `db/020_finance_guide_imprest_schema.sql` 字段注释和 `db/数据库设计-导游备用金说明.md`。
- 已实现功能：团队安排导游行把原“备用金”明确为“计划备用金”，只表示排团手填参考；新增展示“累计已批备用金”和“待审批备用金”，单个导游行展示未申请、待审批、已批未付、部分付款、已付清等状态。备用金弹窗新增“本次公司加点率”和“本次申请金额”，建议备用金只作为参考，本次申请金额由计调手工填写并提交总经理审批。
- 关键业务逻辑：累计备用金口径按审批金额 `approved_amount` 汇总，只统计 `manager_approved`、`paid`、`settled` 的有效申请；`pending_manager` 只进入待审批金额，不计入累计已批。本次公司加点率默认来自系统配置，但计调可按团、按导游、按申请调整，提交后作为申请快照保存；审批和付款变化校验使用申请快照，不受后续全局默认加点率变化影响。
- 验证结果：先补后端红灯测试 `FinanceGuideImprestServiceTest`、`DispatchGuideScheduleServiceTest`，确认缺少按次加点率、手填申请金额和导游行审批汇总时失败；实现后 `cd backend && mvn -q -Dtest=FinanceGuideImprestServiceTest,DispatchGuideScheduleServiceTest test` 通过。前端先补红灯测试 `operation-layout.test.ts`，确认缺少审批汇总字段和本次申请金额时失败；实现后 `cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，19 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd backend && mvn -q test` 通过。用户反馈 90% 重新计算仍回 70% 后，确认原因为后端旧 JVM 未重启；使用 `./scripts/dev-backend-restart.sh` 按 `.env.local` 正确重启后端，并补齐运行库缺失作废字段；`check_schema_consistency.sh` 对三张备用金表返回 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`；真实接口 `GET /finance/guide-imprests/preview?teamId=25&guideId=4&companyMarkupRate=90` 返回 `companyMarkupRate=90`、自费抵扣 `972`、建议备用金 `0`。
- 2026-07-07 追加修复：团队安排备用金弹窗补充展示 `已申请/已批` 和 `建议余额参考 = 建议备用金 - 已申请/已批`；该值仅作风险提示，不限制本次申请。后端禁止提交 0 元备用金申请，防止产生无意义审批单；DTO 校验和服务层均要求申请金额必须大于 0。申请后累计备用金超过系统建议金额时，不拦截金额，但必须填写应急或特殊项目说明，支持现场临时现付和特殊项目备用金。本次真实数据 `teamId=25, guideId=4` 已有已审批申请 `214 + 432 = 646`，当前按 70% 计算建议备用金 `214`，页面提示超建议风险，但允许填写备注后继续申请。验证：后端先补红灯测试 `submitShouldRejectZeroRequestedAmount`、`submitShouldAllowEmergencyApplicationWhenRemarkExplainsOverSuggestedTotal`、`submitShouldRequireRemarkWhenApplicationMakesTotalExceedSuggestedAmount` 后实现；`cd backend && mvn -q -Dtest=FinanceGuideImprestServiceTest test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过；真实接口 0 元提交被拒绝且未写入记录。
- 2026-07-07 追加修复：总经理审批和财务付款发现团队安排变化时，不再只提示通用文案，改为展示申请快照和当前重新计算结果的具体差异，例如 `现付总成本从 ¥2105.00 变为 ¥1605.00；建议备用金从 ¥1349.00 变为 ¥849.00`。审批页 Toast、黄色提示和后端异常都使用同一份 `calculationChangeMessage`。验证：`cd backend && mvn -q -Dtest=FinanceGuideImprestServiceTest test`、`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/system/risk-approval-layout.test.ts --dom`、`cd backend && mvn -q test`、`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 均通过；真实接口 `GET /finance/guide-imprests/detail?id=6` 返回 `calculationChanged=true` 和具体金额差异。
- 2026-07-07 追加修复：备用金审批/付款变化提示继续细化到项目级。后端会读取 `finance_guide_imprest_calc_lines` 中申请时固化的计算快照，逐条对比当前有效 `dispatch_team_arrangements` 和当前自费抵扣计算明细；若来源安排已删除、停用或现付金额变化，会提示具体项目，例如 `楼外楼团队餐：申请时现付 ¥500.00，当前安排已删除或不再生效`，并继续保留 `现付总成本从 ... 变为 ...` 等汇总兜底。当前团队安排表没有 `updated_by` 字段，本次不新增表结构，因此只能定位具体项目和金额变化，不能准确展示修改人。
- 2026-07-07 追加修复：团队安排导游行新增 `审批记录` 入口，点击后按当前团队和导游查询已有备用金申请单，展示申请编号、申请金额、审批状态、申请人/时间、审批人/时间、审批意见、已付金额和余额；`查看详情` 复用备用金详情接口展示计算快照、备注和团队安排变化提示。本次不新增审批流水表，第一版以 `finance_guide_imprests` 申请单记录作为审批记录来源。验证：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，20 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。
- 待确认问题：当前仍未开发导游往来账和下次备用金抵扣核销流水；如果后续要从历史导游应上交金额中抵扣本次付款，必须在财务付款或导游结算模块生成明确核销记录，不能在本弹窗自动扣减。

### 2026-07-07 财务管理 / 购物核对与公司补佣首版

- 状态：已完成后端计算底座、数据库表、团队操作/财务团队审核入口和本地联调；已按甲方最新口径修正为“排团页只做购物计划，真实购物核对和公司补佣放到单团审核/结算入口”；导游报账页、Excel 导入和真实收款入账仍待后续开发。
- 前端页面：`sales/components/ShoppingReconciliationModal.vue` 为复用弹窗；`sales/team/operation.vue` 新增 `购物核对/补佣` 动作；`finance/team-audit/index.vue` 改为真实团队列表并可打开购物核对；`sales/team/arrangement.vue` 不再承载购物实际业绩入口；新增 API 封装 `src/api/finance/shopping.ts`。
- 后端接口：新增 `GET /finance/shopping/team/{teamId}/overview`、`POST /finance/shopping/team/{teamId}/rule-override`、`POST /finance/shopping/team/{teamId}/feedback-lines`、`POST /finance/shopping/team/{teamId}/feedback-lines/{id}/cancel`、`POST /finance/shopping/team/{teamId}/calculate`。
- 数据库表：新增 `finance_shopping_commission_rules`、`finance_shopping_team_rule_overrides`、`finance_shopping_feedback_lines`、`finance_shopping_feedback_detail_lines`、`finance_shopping_settlements`、`finance_shopping_settlement_lines`；`finance_shopping_settlements` 追加 `manual_guide_bonus_amount`、`manual_guide_bonus_remark`；本地 SQL 为 `db/021_finance_shopping_commission_schema.sql`，说明文档为 `db/数据库设计-购物业绩与阶梯佣金说明.md`。
- 已实现功能：支持按团队查看购物参考规则、按团队覆盖人均消费门槛和基础/达标比例；支持人工录入购物店反馈数据，包括购物店、消费日期、进店人数、消费总额、公司返佣、导游现场佣金、人头费和备注；支持计调录入“公司补佣金额/说明”并重新计算购物结算快照，展示实收人数、消费总额、人均消费、是否达标、参考阶梯补差、导游现场佣金加参考补差展示合计、公司补佣和内账购物利润。
- 关键业务逻辑：团队安排中的购物安排仍作为排团计划和预估数据，实际购物店反馈进入 `finance_shopping_feedback_lines`，入口放在团队操作/财务团队审核的单团核对流程，不在排团购物分区直接录真实业绩；默认参考规则为人均消费门槛 `5000`、基础比例 `8%`、达标目标 `10%`，达标后按全团消费测算参考补差，即 `参考补差 = 全团购物消费总额 × (目标比例 - 基础比例)`；参考补差不自动生成正式成本，不自动写入团队账单。导游从购物店现场取得的佣金只做业务核对，不作为公司成本扣减。内部购物利润公式为 `人头费合计 + 公司返佣合计 - 公司补佣金额`；外账购物利润首版默认 `0`，无发票购物金额只进入内部经营利润，不默认进入税务外账。
- 预算利润联动：团队安排汇总接口优先读取当前有效购物结算快照的 `internal_company_profit_amount` 作为购物实际利润；没有结算快照时回退到团队安排购物预估利润。购物结算仍不是银行流水，也不是收款记录。
- 验证结果：首版 `cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；已应用 `db/021_finance_shopping_commission_schema.sql` 到当前联调库，schema guardian 校验 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。2026-07-07 追加修正先补红灯测试 `FinanceShoppingCommissionCalculatorTest` 和 `FinanceShoppingCommissionServiceTest`，确认旧公式错误扣减导游现场佣金和参考阶梯补差；实现后 `cd backend && mvn -q -Dtest=FinanceShoppingCommissionCalculatorTest,FinanceShoppingCommissionServiceTest test` 通过，`cd backend && mvn -q test` 通过，`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；运行库已补齐 `manual_guide_bonus_amount`、`manual_guide_bonus_remark`，schema guardian 复核 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。真实接口 `POST /finance/shopping/team/25/calculate` 传入公司补佣 `0` 后返回 `人头费 540 + 公司返佣 12000 - 公司补佣 0 = 内账购物利润 12540`；后续入口调整已补前端和后端红灯测试，确认排团页不再出现购物实际业绩入口，团队操作/财务团队审核可打开 `购物核对/补佣`。本次验证 `pnpm -C frontend-preview exec vitest run --dom apps/web-antd/src/views/sales/team/operation-layout.test.ts apps/web-antd/src/router/routes/__tests__/visible-menu.test.ts` 通过 25 项，`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过，`cd backend && mvn -q test` 通过。
- 2026-07-07 追加修复：购物店反馈支持 `总额返佣 / 按品类返佣` 两种录入口径，但后端统一按消费详情计算。总额返佣自动生成一条 `综合` 明细；按品类返佣支持同一家购物店下录入多条品类明细，例如茶叶、茶多酚同店但公司返点和导游现场提成比例不同。父表 `finance_shopping_feedback_lines` 只保存购物店、导游、日期、进店人数和汇总金额，子表 `finance_shopping_feedback_detail_lines` 保存品类、消费金额、公司返佣比例/金额、导游提成比例/金额、现结和备注；父表金额由后端按子表汇总，前端合计仅作录入预览。运行库已新增 `finance_shopping_feedback_lines.rebate_calc_mode` 和 `finance_shopping_feedback_detail_lines`，并把历史购物反馈回填为 `综合` 明细；schema guardian 对 6 张购物表返回 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。
- 待确认问题：Excel 导入购物店反馈、导游报账页购物业绩对比、购物返佣应收/已收/余款、银行现金账、内外账报表和购物收款分摊尚未接入；后续开发时不能把购物结算快照当成真实收款或银行流水。

### 2026-07-07 销售管理 / 团队内部备注按老系统字段对齐

- 状态：已完成后端、前端、运行库 DDL 和本地验证。
- 前端页面：`sales/team/operation.vue`、`sales/team/arrangement.vue`；API 类型文件 `src/api/sales/team.ts`。
- 后端接口：继续使用 `GET /sales/team/{teamId}/operation` 返回内部备注和三个团队级测算值；继续使用 `POST /sales/team/schedule/team/save` 保存团队内部备注和测算值。
- 数据库表：`sales_teams` 新增 `per_capita_pit_amount`、`optional_markup_rate`、`per_capita_shopping_amount`，本地 SQL 同步到 `db/012_sales_team_schema.sql`，说明文档同步到 `db/数据库设计-销售团队团期说明.md`。
- 已实现功能：按老系统 `/sale/pop/InsideMemo.aspx?GroupId=...` 实测口径，将“内部备注”从单文本改为 `内部备注文本 + 人均坑位 + 自费加点率 + 人均购物`。团队操作页点击“内部备注”打开弹窗可编辑这四项；团队安排总览页顶部内部备注区域展示真实值，点击编辑内部备注也可打开同口径弹窗。内部备注为空时，编辑弹窗按老系统预置 `>导游要求：`、`>控房要求：`、`>控车要求：`、`>用餐要求：`、`>其它要求：` 五行模板；已有内部备注不覆盖。内部备注编辑弹窗单独加宽到 760px，文本域最少 10 行，方便填写多项要求；其它团队属性小弹窗仍保持原紧凑宽度。团队安排页内部备注展示区改为完整文本块，保留原始换行并默认全部展开，不做标签拆分、截断或内部滚动，便于填写和查看较长要求。
- 关键业务逻辑：这三项是团队级字段，按 `GroupId/teamId` 保存，只影响当前团队，不反写产品模板，也不是购物实际结算、不是银行流水。`自费加点率` 按百分数保存，例如 `70` 表示 `70%`；导游备用金计算未手工指定本次公司加点率时，优先取当前团队的自费加点率作为默认锚点，团队未设置或为 `0` 时再回退系统配置。三个数值字段后端和数据库均限制不能小于 0。购物实际反馈和公司补佣仍走“购物核对/补佣”流程。
- 老系统证据：老系统团队总览内部备注编辑入口为 `#aInsideMemo`，弹窗 URL 为 `/sale/pop/InsideMemo.aspx?GroupId=274202`，字段名分别为 `InsideMemo`、`PAvgKengWei`、`ZiFeiRate`、`PAvgShop`，保存前校验三个数值必须为数字。
- 验证结果：先补红灯测试确认后端详情、保存接口和前端页面缺少三项字段；实现后 `cd backend && mvn -q -Dtest=SalesTeamScheduleServiceTest#operationDetailShouldAggregateTeamProductDescriptionsAndPrices,SalesTeamScheduleServiceTest#saveTeamShouldPersistOperationDictionaryFields,SalesTeamScheduleServiceTest#saveTeamShouldClearProfileFieldsWhenBlankValueIsSubmitted test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，23 项测试通过；`cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`check_schema_consistency.sh` 对 `sales_teams,sales_team_prices,sales_team_status_logs,sales_team_no_logs` 返回 `columns=match`、`constraints=match`、`indexes=match`、`comments=match`。2026-07-07 追加验证内部备注默认模板：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，23 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过。2026-07-08 追加导游备用金锚点测试：先补红灯测试确认未传 `companyMarkupRate` 时后端仍取系统默认 `70%`、前端打开备用金弹窗未带出团队自费加点率；实现后团队 `optional_markup_rate > 0` 时作为备用金默认公司加点率，手工传入本次加点率仍优先，团队值为 `0` 时回退系统配置。
- 待确认问题：`自费加点率` 已作为导游备用金默认公司加点率锚点；`人均坑位` 和 `人均购物` 目前仍作为团队内部备注和测算参考展示。若后续要参与预算利润、购物预期抵扣或经营统计，需要单独明确公式和后端权威计算口径。

### 2026-07-08 销售管理 / 团队安排成本总览金额可读性优化

- 状态：已完成前端展示优化和本地验证。
- 前端页面：`sales/team/arrangement.vue`、`sales/product/team-arrangement.vue`，共享样式 `sales/team-arrangement-layout.css`。
- 后端接口：无变更，继续使用现有团队安排汇总和产品团队安排本地汇总数据。
- 数据库表：无变更。
- 已实现功能：成本总览增加重点金额摘要条，展示成本合计、现结合计、挂账合计、备用金、自费收入、导服、操作费；保留原分类明细矩阵。摘要区按主金额、次级金额、辅助金额分层，`成本合计` 使用主卡片和 `20px` 金额字号，现结、挂账、备用金使用次级卡片；下方矩阵只保留各分类现结/挂账明细，不再重复展示合计、自费收入、导服、操作费、备用金列，避免挤压分类数据。矩阵金额字号从正式团队页约 `10.8px` 提升到 `12.5px`，`¥0.00` 弱化为灰色，非零金额加粗。
- 验证结果：先补红灯测试确认成本总览缺少摘要条和金额视觉状态类；实现后 `cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，25 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；使用 Playwright 打开 `http://127.0.0.1:5666/sales/team/arrangement/25`，确认 `.cost-overview-summary` 已渲染，摘要金额计算样式为 `17px/900`，明细金额为 `12.5px`，合计列为浅蓝底。
- 待确认问题：当前仅优化显示层，不改变后端金额计算口径；如后续要把“自费收入”接入真实自费收入汇总，需要另行明确数据来源。

### 2026-07-08 销售管理 / 团队安排、产品团队安排、团队操作视觉统一

- 状态：已完成前端样式统一和本地验证。
- 前端页面：`sales/team/arrangement.vue`、`sales/product/team-arrangement.vue`、`sales/team/operation.vue`；新增共享头部组件 `sales/components/FormalTeamPageHeader.vue`；共享样式继续使用 `sales/team-arrangement-layout.css`。
- 后端接口：无变更。
- 数据库表：无变更。
- 已实现功能：以正式团队安排页为视觉基准，抽出统一的团队页头组件，统一产品团队安排页和团队操作页的标题、阶段条、标签、指标、内部备注、右侧动作和顶部工具条样式；团队操作页保留中部业务入口，不把业务动作挤到顶部；团队操作页的产品说明、收客须知和订单区改为跟团队安排页一致的白底卡片结构。
- 2026-07-08 追加修复：团队操作页中部业务按钮区固定为单行横排，普通团队属性弹窗宽度统一为 460px，内部备注弹窗统一为 760px；产品团队安排页内部备注弹窗补齐 `人均坑位 / 自费加点率 / 人均购物`，并和正式排团页一样在空备注时默认带出 `>导游要求：`、`>控房要求：`、`>控车要求：`、`>用餐要求：`、`>其它要求：` 五行模板。产品排期创建正式团队时，产品页维护的内部备注和三项指标会同步复制到正式团队快照。
- 关键业务逻辑：本次只改视觉结构和组件复用，不改变产品模板保存、正式团队安排保存、团队操作拼团/转团/购物核对/内部备注等业务接口和数据口径。
- 验证结果：`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts --dom` 通过，27 项测试通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；Playwright 分别打开 `/sales/team/arrangement/25`、`/sales/product/team-arrangement/16`、`/sales/team/operation/25` 完成页面验证，截图为 `output/playwright/team-arrangement-25-top-after.css.png`、`output/playwright/product-team-arrangement-16.png`、`output/playwright/team-operation-25-after.css.png`。控制台仅出现外部 `api.unisvg.com` 图标资源连接关闭，不影响本地页面渲染。
- 待确认问题：如后续继续新增团队相关页面，应直接复用 `FormalTeamPageHeader.vue` 和 `team-arrangement-layout.css`，避免再复制一套页头样式。

### 2026-07-08 销售管理 / 拼团逻辑全量对齐老系统

- 状态：已完成后端、前端、测试、工程文档、浏览器自测截图和新旧数据对比报告。
- 前端页面：`sales/team/operation.vue`、`sales/order/index.vue`；新增共用弹窗 `sales/components/MergeOrderModal.vue`。
- 后端接口：保留 `POST /sales/team/{teamId}/operation/merge`，新增订单管理全局入口 `POST /sales/team/operation/merge`；两个入口共用 `SalesOrderTransferService.mergeOrders`。
- 数据库表：无新增表结构；继续使用 `sales_orders`、`sales_order_charge_lines`、`sales_order_guests`、`sales_order_transfer_logs`；同步更新 `db/013_sales_booking_order_schema.sql` 注释和 `db/数据库设计-销售收客订单说明.md`。
- 已实现功能：拼团请求支持 `orderIds + targetTeamIds + items + tagFlag` 矩阵提交；团队操作页和订单管理页共用同一弹窗。弹窗按老系统两步交互实现：`选择团期` 支持多选目标团，`拼团操作` 按目标团分组展开来源订单，每个组合可填拼团备注、拼团单价和价格类型，默认单价 `0`、默认类型 `成人`，右上保留 `标记` 和 `执行拼团`。
- 关键业务逻辑：多个订单可以拼到多个目标团，实际生成“来源订单 x 目标团队”目标子订单；同一来源订单可拼多个目标团；重复来源订单和目标团组合跳过，其它组合继续；取消订单和拼入子订单不能作为来源订单再次拼团。来源订单保留在来源团，新拼团不再写入 `merge_source`；历史 `merge_source` 继续参与来源团人数、收入、毛利、团队安排金额汇总和客户摘要。目标子订单写入 `merge_child`，游客名单复制来源订单；目标子订单不复制来源订单原价格明细，只按确认页价格类型和拼团单价生成一条 `base_price`；拼团价 `0` 时目标子订单应收 `0`、已收 `0`、余额 `0`，手填价时目标子订单应收 = 单价 x 来源订单人数、已收固定 `0`、余额等于应收。
- 2026-07-08 追加修复：复测时发现团队安排金额汇总仍按旧设计过滤历史 `merge_source`，已调整 `DispatchTeamArrangementService`，历史拼出来源订单不再从团队安排收入和预算利润中排除，并更新对应单测期望。
- 验证结果：先补红灯测试确认旧实现缺少矩阵 DTO、结果 DTO、目标子单按拼团价计收、来源单不改 `merge_source`、重复组合跳过和历史 `merge_source` 不过滤统计；实现后本轮重新执行 `cd backend && mvn -q test` 通过；`cd frontend-preview && pnpm -F @vben/web-antd run typecheck` 通过；`cd frontend-preview && pnpm -F @vben/web-antd exec vitest run src/views/sales/team/operation-layout.test.ts` 通过，1 个文件、28 项测试。Playwright 独立会话 `mtravel_new_align` 已完成浏览器自测，截图目录 `output/playwright/mtravel-pintuan-new-align-0708/`，复测报告为 `output/playwright/mtravel-pintuan-new-align-0708/新系统拼团对齐老系统复测报告-0708.md`。
- 新旧对比结论：用 `CODex拼团对齐复测-0708-C` 新系统数据逐项对比老系统基线，1 单 -> 1 团价格 0、1 单 -> 1 团手填价 1000、2 单 x 2 团生成 4 子单、同一单拼多个团、重复组合跳过、目标团 0 收入 + 成本负毛利、目标团成本可关联来源订单均已达到一致。
- 待确认问题：拼团逻辑和交互已按老系统事实对齐；后续若要在跨团经营统计中避免来源收入和手填目标收入重复，需要另行定义统计报表的跨团汇总口径，不应修改本次拼团写入口径。

## 后续开发记录模板

复制以下模板追加到对应业务域，或更新上方台账。

```md
### YYYY-MM-DD 模块名称

- 状态：
- 前端页面：
- 后端接口：
- 数据库表：
- 已实现功能：
- 关键业务逻辑：
- 验证结果：
- 待确认问题：
- 下一步：
```
