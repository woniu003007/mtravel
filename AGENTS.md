# Repository Guidelines

## 项目定位
本仓库现在是“旅游地接业务数字化管理平台”的代码与交付资料共存仓库。后续开发以正式应用代码为主，历史调研文档、原型图册、PPT、Excel 仍需保留，不能随意删除或覆盖。

## 项目结构
- `backend/`：Spring Boot 后端，Java 21，Maven，PostgreSQL，MyBatis-Plus，Redis。
- `frontend-preview/apps/web-antd/`：前端管理后台，Vben Admin，Vue，Ant Design Vue。
- `db/`：数据库建表 SQL、表说明和一致性校验资料。线上表结构和本地 SQL/文档必须同步。
- `文档/`：需求、调研、原型图册、汇报 PPT、甲方确认文件等交付资料。
- `scripts/`、`outputs/`、`output/`：文档、图册、PPT 等生成脚本和输出结果。

## 后端开发规范
后端包结构按“业务域 / 子模块 / 技术层”组织，不按全局技术层堆文件。

推荐结构：

```text
com.mtravel.platform.customer.category
  controller
  service
  mapper
  entity
  dto
  enums
```

不要使用这种全局堆放方式：

```text
controller/
dto/
entity/
mapper/
service/
```

职责边界：

- `controller`：只做参数接收、认证上下文解析、响应包装，不写业务规则。
- `service`：写业务规则、租户过滤、状态流转、软删除、查重等判断。
- `mapper`：只做数据库访问，优先使用 MyBatis-Plus 基础能力，复杂 SQL 再补 XML 或自定义方法。
- `entity`：只表达数据库表字段，不混入接口展示逻辑。
- `dto`：请求和响应对象，保持接口协议稳定。
- `enums`：集中管理状态、类型、业务枚举，避免字符串散落在 Service 中。

接口路径保持业务语义清晰，例如 `/customer/categories`。不要为了包结构调整随意改变已有前端调用路径。

## 接口性能与大数据量规则
- 禁止为了省事设计大而全接口。新增、修改、删除必须按业务动作拆分到合适粒度，例如修改一条团队安排不能重建整个产品模板。
- 前端禁止页面或弹窗一次性加载无关下拉数据；必须按业务场景懒加载。弹窗只加载当前弹窗需要的供应商、员工、资源、字典等数据。
- 修改单条业务记录时，后端只能更新该记录及其必要子表，不能重建整个模块、整张页面或整组业务数据。
- 列表接口禁止 N+1 查询；关联名称、数量、状态、汇总值必须批量查询后组装，或使用明确的聚合 SQL。
- 保存接口默认只返回操作结果或必要 ID，不默认返回完整详情。如果保存后前端需要最新详情，由前端显式调用详情接口。
- 所有业务列表必须分页，禁止无条件返回全表。分页接口必须限制 `pageSize` 上限，常规列表最大 200；下拉接口必须按业务筛选和状态过滤。
- 大数据表查询必须带租户边界、软删除条件和必要筛选条件；涉及 `tenant_id`、`is_deleted`、状态、业务日期、外键、编号、名称搜索的常用查询必须同步设计索引。
- 新增查询接口前，必须先判断是否会按客户、供应商、团队、订单、日期范围、状态、租户等维度高频查询，并在 SQL、索引和前端筛选项中同步体现。
- 模糊搜索不能默认对大表做无限制 `%keyword%` 全表扫描。需要限制关键词长度、组合状态/日期/类型筛选，必要时后续改全文索引或专用搜索。
- 统计、首页看板、经营报表、财务汇总等大范围聚合不能直接扫明细表实时计算。数据量变大后必须使用汇总表、缓存、物化视图或定时任务。
- Excel 导入、批量生成、批量导出、批量同步第三方系统等耗时任务不能阻塞普通接口；大文件或大批量任务应设计任务表、异步执行、进度查询和失败重试。
- 接口不能在循环中调用远程服务、地图接口、票务接口、文件服务或数据库单条查询。需要批量化、缓存或异步化。
- 前端同一个页面内的字典、下拉、配置类数据应缓存复用；同一弹窗重复打开不得重复请求不变数据。新增资源后需要刷新时，应提供明确刷新动作或清空对应缓存。
- 后端对短时间内重复读取的低频变化配置，例如系统配置、字典、报价规则、地区数据，可使用 Redis 或本地缓存，但必须考虑租户、状态和失效策略。
- 单接口本地开发环境超过 800ms 必须排查；超过 1.5s 必须优化或说明原因。涉及远程数据库、第三方接口、文件处理时，必须说明耗时来源并尽量拆成异步或缓存。
- 发现慢接口后必须定位到具体阶段：前端等待、代理、鉴权、SQL、远程调用、文件处理、响应体过大。禁止只凭感觉改代码。
- 新增或调整关键接口时，测试不仅要验证功能正确，还要覆盖关键性能边界，例如不能逐行查询、不能整组重建、不能返回无关详情。
- 开发完成后如果涉及列表、弹窗保存、下拉加载、统计查询，必须至少用真实接口或 curl 粗测一次响应时间，并把明显慢点记录到 `文档/开发进度与模块记录.md`。

## 数据库规范
- 业务表必须有 `id bigint` 主键，优先使用数据库自增或等价生成方式。
- 多租户业务表必须有 `tenant_id bigint`，查询和修改必须带租户边界。
- 业务数据默认软删除，不直接物理删除。常用字段包括 `is_deleted`、`deleted_at`、`deleted_by`。
- 状态字段可以落库为 `varchar`，但代码层必须用枚举集中管理允许值。
- 表、字段注释要专业描述业务含义，不写“对应老系统”这类交付口径。
- 修改表结构时，同步更新 `db/` 下 SQL 和说明文档，并保证线上表、本地 SQL、文档一致。
- 不要把数据库、Redis、第三方系统密码写进代码、SQL、Markdown 或提交记录。启动时使用环境变量。

## 前端开发规范
- 前端主应用在 `frontend-preview/apps/web-antd/`。
- 真实业务页面放在 `src/views/<业务域>/<子模块>/`，不要继续把正式功能挂在通用原型页上。
- API 封装放在 `src/api/<业务域>/<子模块>.ts`，页面不要直接散写请求地址。
- 路由放在 `src/router/routes/modules/`，菜单标题和路径要与业务模块一致。
- 本地开发默认通过 `/api` 代理到 Spring Boot 后端。需要临时使用 mock 时，必须明确配置 `VITE_PROXY_TARGET`。
- 页面联调时同时验证：页面能打开、接口返回 200、表格/表单能处理真实返回结构。
- 前端所有涉及省份、城市、区县录入的表单，统一使用“所在地”三级级联选择器；允许用户只选到省、市或区县，接口和数据库仍可保存为 `province`、`city`、`district` 三个字段，不要在页面上拆成三个文本输入框。
- 价格、应收、已收、余额、成本、现结、挂账、应付、付款、返佣、提成、毛利、利润、授信占用等敏感金额，正式业务展示、保存、统计和风控判断必须以后端计算或后端校验为准。前端只允许做未保存表单内的临时录入预览，不能把页面数组 `reduce`、`computed` 或本地 JS 公式作为财务、经营、统计、授信结果来源。
- 应收、应付、收款、付款、银行现金账、导游备用金、导游结算、授信占用等金额汇总字段如果采用缓存或汇总表，必须由后端在数据库事务内维护。新增业务明细、写入收付款分摊、更新汇总缓存必须同事务提交或回滚；并发场景必须使用数据库原子加减 SQL、行级锁或唯一幂等键，禁止先查历史值到 Java 再相加后覆盖写回。事务内只做必要数据库读写，禁止远程接口、文件处理、Excel 导入导出、大循环计算或等待用户输入，避免长事务占锁不释放。
- 新增或修改金额相关页面时，必须同步补后端服务测试覆盖核心公式和过滤规则；前端测试应防止重新出现正式业务金额只在前端聚合的实现。

## 前端 UI 统一规范
- 后台业务列表页统一结构为 `Page -> Card -> 查询区 -> 表格 -> 弹窗/抽屉`。普通增删改查页优先使用 `_business/crud/CrudPage.vue`，复杂业务页必须复用 `src/components/business/` 下的业务查询区和按钮组件。
- 查询区按钮顺序固定为 `重置 / 查询 / 新增`。`查询` 和 `新增` 使用实心主按钮，禁止新增按钮随意使用 `ghost`、大号按钮或自定义颜色。
- 列表页新增按钮文案统一为 `新增`，具体对象放在弹窗标题中，例如 `新增资源`、`新增客户单位`。不要在同一类页面中混用 `添加资源`、`新增`、`添加客户` 等不同入口文案。
- 搜索输入框、下拉框、所在地级联等查询控件必须使用统一查询区网格和间距；不要在每个页面复制独立的 `*-search-grid`、`*-search-actions` 样式。
- 表格行操作统一使用 `type="link"`、`size="small"`；删除、停用、作废等危险操作放在最后并使用 `danger`。
- 业务弹窗必须跟随当前系统风格，优先使用 Ant Design Vue 标准 `Modal + Form + Select/Input/Table/List + Textarea` 结构和默认页脚，不要为单个弹窗单独设计一套卡片化、彩色块或复杂分栏视觉系统。
- 高风险业务弹窗只展示必要字段、紧凑摘要和明确备注；除非已有可复用业务组件，否则不要在弹窗内堆叠多层卡片、自定义大面积面板或和系统主风格不一致的按钮样式。
- 新增页面或重构旧页面时，优先抽公共组件，不要继续复制粘贴按钮区、搜索区、表格操作区样式。

## 本地启动与验证命令
后端测试：

```bash
cd backend && mvn -q test
```

后端启动示例，密码只允许放环境变量：

```bash
cd backend
DB_HOST=... DB_PORT=5432 DB_NAME=mtravel DB_USER=... DB_PASSWORD=... \
REDIS_HOST=... REDIS_PORT=6379 REDIS_PASSWORD=... FLYWAY_ENABLED=false \
mvn spring-boot:run
```

前端类型检查：

```bash
cd frontend-preview && pnpm -F @vben/web-antd run typecheck
```

前端启动：

```bash
cd frontend-preview
pnpm -F @vben/web-antd run dev -- --host 0.0.0.0 --port 5666
```

常用联调检查：

```bash
curl -s http://127.0.0.1:8080/actuator/health
curl -s -X POST http://127.0.0.1:5666/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"123456"}'
```

## 代码质量要求
- 改后端接口时，至少跑 `mvn -q test`。
- 改前端页面或 API 封装时，至少跑 `pnpm -F @vben/web-antd run typecheck`。
- 涉及页面联调时，启动前后端并验证浏览器页面或接口代理。
- 不提交临时 demo token、真实密码、个人本机绝对路径到代码或文档。
- 不为了赶进度把 Controller、DTO、Entity 混在一个包里；新增模块必须遵守当前分层规则。
- 不做无关重构。只改当前任务需要的模块和必要公共支撑。

## 开发与文档同步规则
后续正式功能开发时，区分“工程同步文档”和“交付型文档”。工程同步文档影响代码、接口、数据库和联调结果，必须随开发同步；交付型文档主要用于甲方阅读和汇报，可在模块稳定后阶段性整理。

必须随开发同步的内容：

- `db/` 下的数据库 SQL、表结构说明、字段注释、约束和索引说明。
- 后端接口路径、请求参数、响应格式、状态码和错误码。
- 业务枚举值，例如客户状态、供应商分类、合同分类、审核状态、启停状态。
- 权限、登录、租户隔离、软删除、操作日志等公共规则。
- 前后端联调依赖的字段、选项、校验规则和默认值。

可以阶段末统一整理的内容：

- 甲方汇报 PPT。
- Word 原型图册。
- Excel 功能确认表。
- 业务说明文档。
- 页面截图说明。

每完成一个模块，至少记录：

- 完成了哪些功能。
- 新增或调整了哪些接口。
- 数据库是否有变更。
- 前后端是否已联调通过。
- 是否有待甲方确认的问题。

模块完成记录统一维护在 `文档/开发进度与模块记录.md`。每完成一个正式功能模块，必须更新该台账中的状态、前端页面、后端接口、数据库表、关键业务逻辑、验证结果和待确认问题。不要只在聊天中口头说明，否则后续容易忘记模块进度和系统逻辑。

## 代码注释规范
后续新增或修改代码必须写清楚必要注释，尤其是后端权限、登录、状态流转、租户隔离、软删除、金额计算、审核流程等容易影响业务判断的代码。

注释要求：

- `controller`、`service`、`entity`、`dto`、`mapper`、`enums` 等正式类必须有类级注释，说明这个类负责什么业务。
- 对外接口方法、核心业务方法、状态变更方法、安全校验方法必须有方法级注释，说明入参含义、处理规则、返回结果或失败条件。
- `entity` 和重要 `dto` 字段建议使用字段注释，说明业务含义；不要只让字段名自己解释。
- 枚举值必须写注释，说明每个状态或类型在业务上的含义。
- 涉及权限、登录、登出、会话、Token、Redis 在线状态、操作日志的代码，必须说明为什么这样处理，避免后续误改安全逻辑。
- 复杂判断前可以写短行内注释，说明业务原因，例如“只允许未删除账号登录”“同账号只保留一个在线会话”。
- 不写无意义注释，例如“获取用户名”“设置字段值”这类重复代码本身含义的注释。
- 修改旧代码时，如果碰到没有注释的关键逻辑，应顺手补齐同一文件内相关注释，不能继续扩大无注释代码。

## Agent 专用说明
- 不要随意删除用户已有调研资料、原型图册、PPT、Excel、Word 等交付文件。
- 调研 MTravel 老系统页面、字段来源、按钮逻辑、列表和编辑页对应关系时，必须优先使用项目内 `.codex/skills/mtravel-old-system-research` 的固定流程；使用独立 Playwright 会话，不操作用户 Chrome，不把账号密码写入文件。
- 变更数据库时必须优先考虑 `mtravel-db-schema-guardian` 规范，并保持 `db/` 文件与线上表一致。
- 设计订单应收、费用变更、收款、应付、付款、成本、发票、银行现金账、导游结算、购物返佣、应收应付冲抵、财务统计、经营统计等表结构前，必须先阅读 `文档/旧系统财务统计菜单调研与表设计依据.md`，不得只按当前页面、本地文档或单个需求口径推断财务统计模型。
- 设计正式团队安排成本、订单成本归属、多订单均摊、订单毛利或团队毛利相关表结构前，必须先阅读 `文档/老系统调研-团队主链路跨模块建表依据.md` 中“团队安排成本归属与多订单均摊实测结论”，并按已实测确认的 `不关联订单`、单订单归属、按订单均摊、按人数均摊规则建模。
- 设计正式团队安排、导游报账、计调审核、财务审核、导游备用金或导游结算表结构前，必须先阅读 `文档/老系统调研-团队主链路跨模块建表依据.md` 中“团队安排导游报账同步与现结规则实测结论”。必须区分 `不勾选 + 现结 > 0`、`不勾选 + 挂账 > 0`、`勾选 + 现结 = 0`、`勾选 + 现结 > 0` 四种业务含义，不能把“不勾选”简单等同于导游个人垫付，也不能允许“无需导游报账”与现结金额同时成立。
- 修改生成物时，尽量同步更新对应脚本、来源说明和关键假设。
- 重命名、删除大文件或调整交付目录结构前，先确认不会影响用户现场汇报材料。
- 用户要求“接接口”“启动页面”“修页面”时，默认需要完成实现、启动服务和验证，不要只停留在建议。
